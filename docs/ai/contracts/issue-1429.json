{
  "contract_id": "D1.4",
  "issue": 1429,
  "phase": "D1.4",
  "parent_contract": "D1.3",
  "date": "2026-08-21",
  "status": "blocked_on_managed_installer",
  "criteria": [
    {
      "id": "automatic-vetting-on-creation",
      "status": "pass",
      "evidence": "tool_install_proposal_lifecycle.test.ts creates through createAndVetToolInstallProposalAsync, observes sandbox-running before its injected vetter executes, and only then observes sandbox-vetted. POST /agent-org-proposals/tool-install is the authenticated production entry point."
    },
    {
      "id": "durable-vetting-lifecycle",
      "status": "pass",
      "evidence": "safe/conditional reports are persisted then re-read before sandbox-vetted; unsafe persists then auto-rejects; a thrown vetter persists unknown/sandbox_error and leaves pending. Client report/verdict fields remain non-authoritative."
    },
    {
      "id": "human-decision-and-concurrency",
      "status": "pass",
      "evidence": "denial rejects without installer invocation; approval checks the durable central policy, CAS-claims sandbox-vetted -> approved before the apply boundary, and concurrent approvals invoke an injected installer exactly once."
    },
    {
      "id": "real-api-and-docker",
      "status": "pass",
      "evidence": "RHYTHM_LIVE_E2E=1 D1.4 test passed 1/1 against tools/dev/sandbox.sh: real Docker safe vet -> sandbox-vetted -> approval -> fixed apply-unavailable failure; real broken Docker candidate -> unknown report -> pending -> denial. No reports/proposals were inserted by the live core path."
    },
    {
      "id": "actual-production-install",
      "status": "blocked",
      "evidence": "No existing managed installer safely accepts arbitrary npm/pip proposal packages. The production boundary returns fixed tool_install_apply_unavailable and transitions approved -> failed; it does not execute shell/package installation or claim a tool was installed. Tests inject a no-op installer only to prove ordering and CAS."
    }
  ],
  "verification": {
    "red": "Before implementation, npx vitest run src/services/__tests__/tool_install_proposal_lifecycle.test.ts failed because ../tool_install_proposal_lifecycle did not exist.",
    "focused_matrix": "344 passed, 1 expected env-gated live skip across D1.1-D1.4, proposal repository/state-machine, route/controller, apply, and schema-parity tests; real-Docker vetter subset 59/59.",
    "live": "1/1 passed in 11.46s on isolated API 4098, engine 4097, gateway 4099; sandbox removed and all three ports released; no rhythm-d1-vet containers remained.",
    "static": "Node 22 tsc --noEmit, npm run build, SQLite migration replay, and git diff --check passed.",
    "gitnexus": "UNKNOWN: this worktree is not indexed. No analyze/index was run because it can rewrite AGENTS.md/CLAUDE.md."
  },
  "risks": [
    "The installation outcome remains intentionally blocked until a separately approved managed installer exists. This repair does not install arbitrary host packages.",
    "The in-process vetter dedupe remains process-local; the creation service's proposal lifecycle and approval CAS prevent duplicate apply invocation within the current architecture."
  ]
}
