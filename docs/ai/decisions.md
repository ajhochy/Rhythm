# Decisions

Durable decisions live as one file per decision under `docs/ai/decisions/`.
This file is kept as a compatibility index for workflow tooling that checks
for the historical `docs/ai/decisions.md` context file.

See `docs/ai/decisions/` for the canonical decision records.
