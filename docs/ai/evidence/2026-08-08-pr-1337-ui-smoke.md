---
date: 2026-08-08
repo: Rhythm
branch: ui/desktop-mobile-session-polish
pr: 1337
status: pass
tags: [evidence, ui, smoke, desktop_flutter, mobile]
---

# PR #1337 UI smoke evidence

## Global Navigation

- AJ manually smoke-tested and PASSed all fitting workspace tabs, the single-row `nav48` header, and the restrained selected/focus treatment. Settings remained in the account control rather than the workspace tabs.
- Deterministic `global_navigation_contract_test.dart` coverage passed (9 tests), including wide fitting behavior and 1024px at 200% text scale.

## Agents Pane

- AJ manually smoke-tested and PASSed the `project toolbar24/band26`, compact `SessionRow38/action30` geometry, Agents/Sub Agents controls with default-collapsed subagents, header search, and sticky draggable Tools.
- Tools sizing evidence: `Tools120/180/max376 or443` — initial `180px`, minimum `120px`, and 1024×700 maxima of `376px` with Agents expanded or `443px` collapsed; AJ also tested the high range.
- Focused Agents/controller/splitter repairs passed (81 tests).

## Mobile

- AJ manually smoke-tested the mobile starter-prompt flow and said it works. The fake gateway recorded `prompt_async` POSTs for `session-2` and `session-3`, followed by message, todo, status, and diff refreshes.
- Focused native-prompt regression and prompt-execution-plan tests, mobile lint/typecheck, and the 53-test broader Jest suite previously passed. Mobile Agents session-list focused coverage passed (13 tests).

## Dashboard

- AJ viewed the final paired dashboard live preview and accepted it as “alright good enough”: dial-over-grid left column, right task list, paired laptop layout, and final spacing direction.
- Measured geometry: `dashboard1024 cards459x426, left166/ring166/list261, 1440 list418`; paired cards retain the accepted laptop layout. Focused dashboard contract coverage passed (12 tests).

## Accessibility

- Final UI/UX designer review PASSed with no release-blocking UI or WCAG regression.
- Residual nonblocking manual item: VoiceOver traversal through offscreen dashboard task rows in the `140px` scroll viewport.

## Capture Limitation

- macOS `screencapture` is denied by Screen Recording permissions; no PNG was created or claimed. This artifact preserves AJ’s manual observations, the measured geometry, and the UI reviewer result instead.
