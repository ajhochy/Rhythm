---
date: 2026-09-24
repo: Rhythm
branch: swarm/issue-1568
pr: null
issues: [1568]
status: ready_for_verification
tags: [run, Rhythm]
---

## Files

- Product scope: `apps/api_server/src/services/usage_budget_service.ts` only.
- Contract: `docs/ai/contracts/issue-1568.json`; tests: usage budget, issue 844 routing and isolated live HTTP.

## Checks

- Final repair attempt 2 Phase 0: WAIVED: evidence-only amendments to existing #1568 tests, contract metadata, and run note; verification is focused and full green tests, typecheck, contract inspection, diff guard and isolated live sandbox. No product behavior or source changes authorized. Starting product hash `usage_budget_service.ts`: `24fce709e9a13b2d1f76f0e1bc8ef1fb2ae94025b49811fa1787c9caa5ca92ea`; starting test hash `usage_budget_service.test.ts`: `fec53c9f933f56b690fb1254b39f906465acb799c04dbfc93c6d15fd3f8ae530`. Root `/private/tmp/rhythm-swarm-1568`, branch `swarm/issue-1568`; preexisting dirty files: service, issue_844 test, usage test, live test, contract, run note.
- Final repair attempt 2 Phase 1: Opus triage `1f1534b2-2ead-48c2-a65f-c0d8437dc47f` requires evidence completeness only; earlier attempt 1 and initial implementation receipts below are historical, not this attempt's outputs. GitNexus Rhythm impact `getUsageBudget` LOW, direct route and test callers (2), indirect app (1), affected processes 0; test file path lookup UNKNOWN (not an indexed symbol). No shared product edits. A8 removes serialized nonfinite pseudo-cases; A19 uses raw Response.json for direct nonfinite and edge matrices; A12 exact reason; A13 spies on fresh logger and six console levels, inspect-safe with controls and per-scenario secrets/reasons. Contract c7/c8 explicitly `not_tested`, c9 cache retained.
- Final repair attempt 2 Phase 2 focused: from `apps/api_server`, `npm exec -- vitest run src/services/usage_budget_service.test.ts src/__tests__/issue_844_contract.test.ts && npm exec -- tsc --noEmit -p tsconfig.json` → 2 files, **32 passed (32)**; TypeScript noEmit exit 0. From root, `node -e 'const c=require("./docs/ai/contracts/issue-1568.json");if(c.criteria.length!==9||c.criteria.some(x=>x.status!==(x.mode==="manual"?"not_tested":"pass"))||JSON.stringify(c.not_tested)!==JSON.stringify(["issue-1568-c7","issue-1568-c8"])||!c.criteria.find(x=>x.criterion_id==="issue-1568-c9")?.text.includes("Cache"))process.exit(1);console.log("contract: 9 mapped; c7/c8 not_tested; c9 cache pass")' && git diff --check` → `contract: 9 mapped; c7/c8 not_tested; c9 cache pass`, diff check exit 0.
- Final repair attempt 2 full: from `apps/api_server`, `env -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_LIVE_URL npm test -- --maxWorkers=2` → **667 files passed, 133 skipped (800); 6247 tests passed, 256 skipped (6503)**, exit 0. Existing sourcemap warnings and temporary test worktree creation did not change protected source.
- Final repair attempt 2 live: `bun run build --single` from `apps/opencode_fork/packages/opencode` → smoke test passed `0.0.0-swarm/issue-1568-202609241001` (build reports chunk-size warning); `npm run build` from `apps/api_server` → tsc/postbuild exit 0. Fixture `/private/tmp/rhythm-swarm-1568-fixture-20260924b/{rhythm.db,opencode.json}` verified mode `-r--------`. From repo root, for each sandbox command prefix `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1568-fixture-20260924b RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1568-fixture-20260924b/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-swarm-1568-fixture-20260924b/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1568-sandbox-20260924 RHYTHM_SANDBOX_API_PORT=6098 RHYTHM_SANDBOX_ENGINE_PORT=6097 RHYTHM_SANDBOX_GATEWAY_PORT=6099`: `tools/dev/sandbox.sh up` → `Sandbox ready: http://127.0.0.1:6098 (engine :6097)` (startup transient curl connection refused before readiness); `tools/dev/sandbox.sh status` → owned API :6098 PID78589, engine :6097 PID78610, gateway :6099 PID78589. From `apps/api_server`, `RHYTHM_LIVE_E2E=1 RHYTHM_LIVE_E2E_ISOLATED=1 RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1568-sandbox-20260924 RHYTHM_SANDBOX_API_PORT=6098 npm exec -- vitest run src/__tests__/issue_1568_live.test.ts` → **1 file/1 test passed**, exit 0; synthetic absent/expired/malformed credentials only, no real provider calls. Same sandbox prefix + `tools/dev/sandbox.sh down` → `Sandbox removed`; sanitized diagnostics `/private/tmp/rhythm-swarm-1568-sandbox-20260924.evidence.X0YlKT`.
- Final repair attempt 2 criterion map: c1=A1; c2=A4/A5/A6/A15/A16; c3=A2/A3/A8/A9/A12/A17/A19; c4=A7/A10/A13/A14/A19; c5=issue_844_contract; c6=issue_1568_live; c7/c8=manual `not_tested` (real-account comparison/UI visual); c9=A11/A18. `git diff --check` exit 0; protected SHA256s after builds/live: service `24fce709e9a13b2d1f76f0e1bc8ef1fb2ae94025b49811fa1787c9caa5ca92ea`, issue_844 test `5b6b04c643d5b7d59eadf57d801a00a341013769342e7ff14b86e753d22ed57e`, live test `b8d766e84c12f1fe289e175a784c0490365c76a35cd1ad798606b4ff939c5bb0`. Status same six preexisting paths; only usage test, contract and this note edited in attempt 2. Manager owns detect_changes later; no commit/push/merge/stash/clean.
- Repair attempt 1 Phase 0 RED: `npm exec -- vitest run src/services/usage_budget_service.test.ts -t 'A15|A16'` (apps/api_server) → A15 FAILED: malformed bearer `.payload.sig` returned window (`reason undefined`) instead of fixed `OpenAI token unavailable`; A16 passed, 17 skipped. Added c9 cache contract; c7/c8 remain not_tested. Existing tests/implementation from prior stage preserved.
- Repair attempt 1 Phase 1: GitNexus `impact(buildSnapshot, upstream, repo=Rhythm)` LOW; direct `getUsageBudget`, indirect `usage_budget_routes.ts`/`app.ts`, 0 indexed processes, Services module. `fetchOpenAI` new and absent from existing index; no HIGH/CRITICAL warning. Branch/status verified `swarm/issue-1568`, existing six owned changes preserved.
- Repair attempt 1 Phase 2: one-line JWT segment validation guards all 3 nonempty base64url segments before fetch. New A15/A16 invalid bearer/nonstring guard; strengthened fresh-module logger/console spies across levels with sentinel control, finite/reset/bounds and exact fixed reasons, fake-clock 59999/60001 cache/failure replacement/expired force, and actual frontier-only no-cheaper route temporary config (restored in finally). No product module beyond usage service changed.
- First focused expansion `npm exec -- vitest run src/services/usage_budget_service.test.ts src/__tests__/issue_844_contract.test.ts && npm exec -- tsc --noEmit -p tsconfig.json && npm run build` → 2 test-fixture failures: JSON converted NaN reset to null, and reused consumed Response on cache rebuild. Adjusted boundary mock to return fresh Response and preserve nonfinite values through `response.json()`; no product edits. Repeat same command → 31/31 PASS, tsc noEmit PASS, build/postbuild PASS.
- `env -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_LIVE_URL npm test -- --maxWorkers=2` (apps/api_server) → 667 files pass/133 skipped; 6246 tests pass/256 skipped (6502 total). Worktree tests created disposable worktrees in test temp; no production changes observed.
- Sandbox rerun: fixture paths verified (`/private/tmp/rhythm-swarm-1568-fixture-20260924b/{rhythm.db,opencode.json}` read-only). From repo root, prefix `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1568-fixture-20260924b RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1568-fixture-20260924b/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-swarm-1568-fixture-20260924b/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1568-sandbox-20260924 RHYTHM_SANDBOX_API_PORT=6098 RHYTHM_SANDBOX_ENGINE_PORT=6097 RHYTHM_SANDBOX_GATEWAY_PORT=6099` + `tools/dev/sandbox.sh up` → ready API6098 engine6097; same prefix + `tools/dev/sandbox.sh status` → owned API6098 engine6097 gateway6099. From apps/api_server: `RHYTHM_LIVE_E2E=1 RHYTHM_LIVE_E2E_ISOLATED=1 RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1568-sandbox-20260924 RHYTHM_SANDBOX_API_PORT=6098 npm exec -- vitest run src/__tests__/issue_1568_live.test.ts` → 1/1 PASS, absent/expired/malformed synthetic auth yielded HTTP unavailable envelope. Same prefix + `tools/dev/sandbox.sh down` → removed; sanitized evidence `/private/tmp/rhythm-swarm-1568-sandbox-20260924.evidence.9RSNxZ`. No external ChatGPT calls or real credentials.
- Handoff: c1–c6 and c9 automated PASS, c7/c8 not_tested for real-account comparison and existing UI visual. Verification manager owns detect_changes and further smoke; no commit/push/merge/stash/clean.
- Worktree `/private/tmp/rhythm-swarm-1568`, branch `swarm/issue-1568`, initial git status clean.
- Phase 0: `npm ci --offline --ignore-scripts --no-audit --no-fund` in `apps/api_server` (locked dependencies installed). `npm exec -- vitest run src/services/usage_budget_service.test.ts` RED: 7 failed, 6 passed; A1 expected one GET but got zero, A2 expected one window got zero, A3 expected weekly got zero, A4 malformed top-level `null` threw TypeError; A6 no account header; A7 hardcoded unavailable; A9 empty windows. Initial `npx vitest` startup failed because this worktree lacked dependencies; replaced by installed locked Vitest.
- Phase 1: GitNexus Rhythm `buildSnapshot` upstream impact LOW, direct caller `getUsageBudget`, indirect route `usage_budget_routes.ts` and `app.ts`; 0 indexed affected processes, one Services module.
- Additional LOW impacts: `readAuthJson` and `openAiUnavailable` direct caller `buildSnapshot`, 0 indexed affected processes. No HIGH/CRITICAL warning or shared-path approval requirement.
- `npm exec -- vitest run src/services/usage_budget_service.test.ts src/__tests__/issue_844_contract.test.ts src/__tests__/issue_1568_live.test.ts && npm run build` (in `apps/api_server`): 22 pass, 1 expected env-gated skip, TypeScript build pass (before A11–A14). Final `env -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_LIVE_URL npm exec -- vitest run src/services/usage_budget_service.test.ts src/__tests__/issue_844_contract.test.ts && npm exec -- tsc --noEmit -p tsconfig.json && git diff --check`: 26/26 pass; typecheck and diff check exit 0.
- Synthetic fixture: `npm ci --offline --ignore-scripts --no-audit --no-fund` (root/api/mcp as needed), `npm rebuild better-sqlite3 --offline` (initial fixture generation failed for missing native addon), `bun install --frozen-lockfile --offline` (fork), `node tools/dev/sandbox_fixture.mjs /private/tmp/rhythm-swarm-1568-fixture-20260924b` (synthetic DB and read-only config; no real auth copied). Initial fixture path `...-20260924` failed before DB creation; new fixture generated successfully. No credential values copied from operator HOME.
- Sandbox command from repo root: `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1568-fixture-20260924b RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1568-fixture-20260924b/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-swarm-1568-fixture-20260924b/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1568-sandbox-20260924 RHYTHM_SANDBOX_API_PORT=6098 RHYTHM_SANDBOX_ENGINE_PORT=6097 RHYTHM_SANDBOX_GATEWAY_PORT=6099 tools/dev/sandbox.sh up` → `Sandbox ready: http://127.0.0.1:6098 (engine :6097)`; matching `... tools/dev/sandbox.sh status` → owned listeners API 6098, engine 6097, gateway 6099. `RHYTHM_LIVE_E2E=1 RHYTHM_LIVE_E2E_ISOLATED=1 RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1568-sandbox-20260924 RHYTHM_SANDBOX_API_PORT=6098 npm exec -- vitest run src/__tests__/issue_1568_live.test.ts` (from `apps/api_server`) → 1/1 PASS: absent, expired and malformed synthetic credentials each returned real HTTP 200 envelope with OpenAI unavailable, no secrets. No ChatGPT call made; the live test asserts only failure states. Matching `... tools/dev/sandbox.sh down` → sandbox removed, sanitized diagnostics preserved at `/private/tmp/rhythm-swarm-1568-sandbox-20260924.evidence.0iTpSZ`.
- Initial `npm test` → 6233 pass, 6 unrelated `org_reviewer_harness_isolation` failures caused by ambient `RHYTHM_LIVE_ENGINE_URL=http://127.0.0.1:4096` (fixture requires 4097). `env -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_LIVE_URL npm test` → 6238 pass, 1 unrelated timing assertion `rhythm_telemetry_plugin.test.ts` 21ms < 20ms under parallel load; targeted `env -u ... npm exec -- vitest run src/__tests__/rhythm_telemetry_plugin.test.ts src/services/usage_budget_service.test.ts src/__tests__/issue_844_contract.test.ts && npm exec -- tsc --noEmit -p tsconfig.json` → 34/34 pass. Full clean `env -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_LIVE_URL npm test -- --maxWorkers=2` → 667 test files pass, 133 skip; 6239 tests pass, 256 skip (before test-only A13/A14 assertions; they passed final focused run).
- `git diff --check` exit 0. `git status --short` shows exactly service, two existing tests, new live test, contract JSON and this run note. GitNexus `detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1568)` risk LOW, 7 indexed changed symbols, 0 affected processes; GitNexus does not include untracked files in changed-files count, inspected separately via git status.

## Notes

- User-approved amendment to #1568 supersedes original issue's all-or-nothing primary/secondary requirement: independently validate windows; valid subset survives. No real account request; G1 real-account cross-check and G2 UI visual stay not_tested.
- Explicit routing behavior change: with a valid OpenAI budget, the existing #844 resolver now downgrades Codex from frontier to standard at or below the configurable remaining-fraction threshold; above threshold and unavailable still fail open, explicit model override bypasses downgrade, a tier hint follows existing precedence and cheap tier stays cheap. No resolver edits.
- Security boundary: only read `auth.json`; no refresh/write/keychain/cookies/CLI; fixed OpenAI reasons never contain response/body/statusText/exception/claims, and extras/credits are ignored. No fork, UI, route, database, dependency or production configuration changes. G1 real-account agreement and G2 visual inspection deliberately not tested; no real external credential request was authorized. No commit/push/merge/stash/clean.
