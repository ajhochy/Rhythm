---
date: 2026-09-24
repo: Rhythm
branch: swarm/issue-1547
pr: null
issues: [1547]
status: ready_for_verification
tags: [run, Rhythm]
---

## Files
- Existing product change (not edited in this repair): `apps/api_server/src/repositories/integration_accounts_repository.ts` preserves non-error refresh-bearing credentials on narrow grants, normalizes covering grants and CAS-retries PostgreSQL conflicts; `apps/api_server/src/services/google_oauth_service.ts` opts desktop exchange into preservation.
- This repair strengthened `apps/api_server/src/__tests__/google_desktop_exchange.test.ts` and `google_desktop_postgres.test.ts`; updated `docs/ai/contracts/issue-1547.json` and this note. Package and lockfile are unchanged from HEAD. No Supertest, production Google consent, lock acquisition, or hand-launched server.

## Checks
- Phase 0: WAIVED: This stage reconciles documentation/evidence only and does not change behavior; verification is the JSON contract check and `git diff --check`.
- Root `/private/tmp/rhythm-swarm-1547`, branch `swarm/issue-1547`; starting status was existing modified Google SQLite test/repository/service and untracked Postgres test/contract/run note. No production symbols edited. GitNexus impact on SQLite test and new Postgres test in Rhythm: UNKNOWN, not in index, zero indexed callers/processes; prior product upsert impact LOW (zero indexed callers/processes) and `upsertGoogleAccount` LOW (one caller, zero processes). No HIGH/CRITICAL symbol touched.
- `./node_modules/.bin/vitest run src/__tests__/google_desktop_exchange.test.ts src/__tests__/google_desktop_postgres.test.ts` from `apps/api_server`: **17 passed / 4 gated skipped**, no failures. SQLite seeds expired 2020 rows; asserts exact retained rows, error recovery, expiry on covering grant, different-identity null refresh, and real Express loopback HTTP response shape, session lookup, and no credential leak. Only external Google token/userinfo boundary is fake.
- Disposable PostgreSQL 17.11 Homebrew, isolated random-password local cluster on verified-free port 5786 and random isolated schema; `LC_ALL=C` required by PostgreSQL startup (initial attempt without it failed `postmaster became multithreaded during startup`; diagnostic cluster removed). `RHYTHM_LIVE_PG=1 RHYTHM_LIVE_PG_URL=postgres://postgres:<redacted>@127.0.0.1:5786/postgres ./node_modules/.bin/vitest run src/__tests__/google_desktop_postgres.test.ts` from `apps/api_server`: **4 passed / 0 failed**. `TRUNCATE` before each; exact expiry/identity/no-refresh/error recovery and coherent writer tuples; forced CAS identity switch and rotated-refresh each observed exactly one row-count-zero conflict before retry. Only owned temp cluster stopped and removed. Exact PostgreSQL 16 **not_tested**, not claimed equivalent.
- Astra result `2ee377ee-ec0f-426b-a59f-153eaf496a06`: focused exchange/Postgres suite **17 passed / 4 gated**; PostgreSQL 17 gate **4 passed**; clean full suite **6226 passed / 255 skipped**. API build/TypeScript, security, smoke, and sandbox checks passed. Package and lockfile are clean. The previously reported unrelated #738 timing failure did not recur in this clean full run; supersedes the earlier stale failure/blocked claim.
- Fresh synthetic-only read-only fixture: `node tools/dev/sandbox_fixture.mjs /private/tmp/rhythm-1547-fixture-20260924-001`. Normal `tools/dev/sandbox.sh up`, `status`, `down` with `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-1547-fixture-20260924-001 RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-1547-fixture-20260924-001/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-1547-fixture-20260924-001/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1547-sandbox RHYTHM_SANDBOX_API_PORT=5798 RHYTHM_SANDBOX_ENGINE_PORT=5797 RHYTHM_SANDBOX_GATEWAY_PORT=5799 DB_CLIENT=sqlite RHYTHM_OPTIMIZER_MODE=shadow`: PASS first startup, API 5798 and engine 5797 owned listeners. `curl -sS -i http://127.0.0.1:5798/opencode/health` → 200 `status=ready`, `bridgeLive=true`; `curl -sS -i -X POST http://127.0.0.1:5798/auth/google/desktop-exchange -H 'Content-Type: application/json' --data '{}'` → 400 `BAD_REQUEST`, `code is required`. `down` removed sandbox, preserved diagnostics under `/private/tmp/rhythm-swarm-1547-sandbox.evidence.SEwpro`. No live credential/production engine touched.

## Notes
- Manual targets: #1547-c7 real Google consent and Calendar/Gmail refresh with operator-owned credentials **not_tested**; #1547-c12 exact PostgreSQL 16 **not_tested**. Synthetic test tokens only. Sandbox empty-body rejection is not a real Google success smoke; live real-Google flow remains manual.
- Fresh manager `detect_changes` evidence: LOW, 7 indexed symbols, 0 affected processes. No new product edits in this reconciliation.
- This reconciliation is limited to `docs/ai/contracts/issue-1547.json` and this run note. No commit, push, merge, stash, clean, product/test edit, or project-state/current-plan edit.
- Phase 0 waiver: WAIVED: This stage reconciles documentation/evidence only and does not change behavior; verification is the JSON contract check and `git diff --check`. The Google consent and exact PG16 limits above remain explicit manual gaps.
