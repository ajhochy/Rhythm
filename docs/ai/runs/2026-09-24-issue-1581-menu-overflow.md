---
date: 2026-09-24
repo: Rhythm
branch: swarm/issue-1581
pr: null
issues: [1581]
status: blocked
tags: [run, Rhythm]
---

## Files

- `apps/web/src/components/Shell.tsx`: synchronize both CSS breakpoint queries and remount only the More menu on tier changes, closing its stale open panel.
- `apps/web/src/styles.css`: allow nav overflow only while its panel is open; layer header above workspace. Retain closed clipping for enlarged-text layouts.
- `apps/web/tests/contract/issue-1581.spec.ts`, `docs/ai/contracts/issue-1581.json`: six criteria, 9 browser checks, including built renderer and axe.
- `docs/ai/runs/artifacts/issue-1581-*.png`: durable final Chromium closed/open captures at 1440/1100/780/390 and bundled-dist 1100.
- Repair attempt 1: scoped `.nav-overflow` bounded viewport scrolling and 44px menu items (account/demo unchanged); exact-edge and 550×450 enlarged-text contract, c7 manual parity declaration, and original RED captures recovered from `/var/folders/f0/kwf9lqtx57qgt3j4rbtvg1ym0000gn/T/opencode/triage-1581/base/docs/ai/runs/artifacts/` as `issue-1581-base-red-{1100,780,390}-open.png`. The preexisting Shell.tsx fix is unchanged in this repair.

## Checks

- Repair attempt 1 Phase 0 RED (before CSS edits): `cd apps/web && env -u RHYTHM_LIVE_E2E -u RHYTHM_OPENCODE_ENGINE_PORT -u RHYTHM_MOBILE_GATEWAY_PORT RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npx playwright test tests/contract/issue-1581.spec.ts --grep '550x450' --workers=1` → 1 failed: `panel.y + panel.height` was 458 > viewport 450 at effective 200% text. Contract extended to eight exact boundary widths, each item's 44px hit area, keyboard visibility, focus outline, both-direction edge resize, and open header axe. GitNexus CSS path unindexed (UNKNOWN); related `Shell` LOW, 1 direct caller (`App`), 0 affected processes, 3 transitive symbols. No HIGH/CRITICAL symbol edit.
- Baseline: `pwd && git branch --show-current && git status --short && git rev-parse HEAD` → `/private/tmp/rhythm-swarm-1581`, `swarm/issue-1581`, clean at `e93eac6e7f9007eb4f4be4a434784a5fe7e2d3da`.
- Phase 0 RED: `cd apps/web && RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npx playwright test tests/contract/issue-1581.spec.ts --workers=1` → 3 failed, 4 passed (repeated after correcting test expectations); at 1100 the menu's bottom hit target was covered by the workspace, at 780 menu badge text made the first exact-text assertion incorrect, and resize 780→1100 left `aria-expanded=true`. Initial Chromium screenshot showed the panel clipped to the header; later GREEN captures overwrote the initial same-name file. Corrected the badge assertion to compare destination test IDs rather than unread-count text. The existing 1100 selected destination navigation and keyboard tests already passed.
- GitNexus impact before implementation: `Shell` LOW, direct `App` (1), 0 processes, 3 transitive symbols; `Menu` LOW, direct `Shell` (1), 0 processes; `moveMenuFocus` LOW/0. No HIGH/CRITICAL edits.
- Initial GREEN: same contract command → 7 passed. `npm run typecheck && RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npx playwright test tests/contract/issue-1581.spec.ts tests/shell.spec.ts --workers=1` → typecheck pass, 13 passed (includes bundled-dist contract).
- `RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npm test` → build pass; default fixture stage 467 passed, 49 skipped, 8 failed; later stages short-circuited. One failure (`issue-1519-c6` scrollWidth 1670 > clientWidth 1440 under 200%-text layout) was a real regression from leaving the nav unclipped even when closed. Repaired CSS to conditionally reveal only an open panel. Seven other failures are outside this issue: six `live-demo-state-leak` tests rendered `trusted api expected base must be plain loopback HTTP on an unprivileged port`, and one Hermes splitter test found no Hermes separator. Full default suite was **not** rerun after the targeted repair.
- `RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npx playwright test tests/pages/integrations-list-inspector.spec.ts --grep 'issue-1519-c6' --workers=1` → initially reproduced the 200%-text regression. With CSS repair, contract + this check (`--grep 'issue-1581|issue-1519-c6'`) → 9 passed.
- `RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npx playwright test --config tests/bucket-a-rendered-repair-playwright.config.ts` → 11 passed, 4 failed: screenshot mismatch `issue-1477`, broken-video expectation, and 2 ambiguous `auto-promotion` test IDs. Those test paths do not exercise the nav fix; no baseline run proving preexistence. Did not edit these tests.
- `RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npm run test:electron-slices` → E13/E14/E15/E16/E20/E21/E22/E23/E24/E25A/E25B stages passed (live opt-in skips); stopped on E27 discovery because `apps/api_server` dependencies were not installed (`Cannot find module 'ws'`). Remaining slices not run.
- `RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npm run test:session-opening` → 13 passed.
- Final: `RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npx playwright test tests/contract/issue-1581.spec.ts --workers=1` → **9 passed** (1440, 1100, 780, 390, resize, keyboard, active route, axe, built dist). `git diff --check` → pass. GitNexus `detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1581)` → LOW, 0 affected processes, expected Shell symbols.

## Notes

- Repair attempt 2 (#1581; Opus triage `baa8e05a-3a0a-450c-9a00-af5d610cd208`): confirmed `/private/tmp/rhythm-swarm-1581`, branch `swarm/issue-1581`; pre-existing dirty `Shell.tsx`, `styles.css`, contract/run files and issue screenshots were retained. Inspected `repair-2.patch`; only its one-line `.app-header` restoration and c2 test were applied. No Shell.tsx/nav-overflow edits. GitNexus impact on `apps/web/src/styles.css` returned UNKNOWN (not indexed), 0 reported dependents; no HIGH/CRITICAL result.
- Phase 0 RED, before the CSS repair: `cd apps/web && env -u RHYTHM_LIVE_E2E -u RHYTHM_OPENCODE_ENGINE_PORT -u RHYTHM_MOBILE_GATEWAY_PORT RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npx playwright test tests/contract/issue-1581.spec.ts --grep 'header menus stay above' --workers=1` → **1 failed**, expanded rail covered the Planner menu item (`elementFromPoint` returned another element). After the one-line CSS repair, exact command → **1 passed**, exercising expanded rail/More and inspector/account menu hit-testing at 780px.
- `cd apps/web && npm run typecheck && env -u RHYTHM_LIVE_E2E -u RHYTHM_OPENCODE_ENGINE_PORT -u RHYTHM_MOBILE_GATEWAY_PORT RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npx playwright test tests/contract/issue-1581.spec.ts --workers=1` → typecheck passed; **15 passed**. The c2 test catches rail/inspector stacking without mocking the UI.
- Full suite attempt: `cd apps/web && env -u RHYTHM_LIVE_E2E -u RHYTHM_OPENCODE_ENGINE_PORT -u RHYTHM_MOBILE_GATEWAY_PORT RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npm test` → build completed; default stage timed out after 600 seconds while six live-demo tests inherited ambient `RHYTHM_API_BASE=http://localhost:4001` (`trusted api expected base must be plain loopback HTTP on an unprivileged port`); one Hermes splitter missing-separator failure was observed, but the default suite did **not** finish, so **481 passed/49 skipped/1 failed is not established in this attempt**.
- Retry with both live endpoints unset: `cd apps/web && env -u RHYTHM_LIVE_E2E -u RHYTHM_API_BASE -u RHYTHM_ENGINE_BASE -u RHYTHM_OPENCODE_ENGINE_PORT -u RHYTHM_MOBILE_GATEWAY_PORT RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npm test` → build completed; Playwright aborted because `http://127.0.0.1:4581 is already used`. `lsof`/`ps` showed listeners PID 84494 (`vite --host 127.0.0.1 --port 4581`, parent 84473) and PID 84914 (`node tests/serve-dist.mjs`, parent 84496). Unexpected suite-blocking port conflict: **BLOCKED**; no further suite, bucket, Electron slices, session-opening, review-live or packaged Electron parity claimed in this attempt. No process terminated, no generated screenshots restored, no commit/push/merge/stash/clean. C7 remains **not_tested**; manual smoke target stays packaged host at 1100/390, exact items, visible bounds, keyboard Settings reachability and screenshot parity.
- Final scope check: `git diff --check && git diff -- apps/web/src/styles.css && git status --short` → format clean; CSS diff retains prior nav-overflow/44px changes and omits the header stacking context. `gitnexus_detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1581)` → low risk, 2 indexed Shell symbols from pre-existing changes, 0 affected processes; CSS is not indexed. No unrelated tracked Electron image modifications shown in this status.

- Repair attempt 1 final contract: `cd apps/web && env -u RHYTHM_LIVE_E2E -u RHYTHM_OPENCODE_ENGINE_PORT -u RHYTHM_MOBILE_GATEWAY_PORT RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npx playwright test tests/contract/issue-1581.spec.ts --workers=1` → 14 passed; first full run after CSS had 13 pass/1 fail because End raced the menu's auto-focus; await initial focused item before End fixed test synchronization. `npm run typecheck` passed; same env and `npx playwright test tests/shell.spec.ts tests/pages/integrations-list-inspector.spec.ts --grep 'issue-1519-c6|shell' --workers=1` → 6 passed.
- Broader default: `cd apps/web && env -u RHYTHM_LIVE_E2E -u RHYTHM_OPENCODE_ENGINE_PORT -u RHYTHM_MOBILE_GATEWAY_PORT RHYTHM_E2E_PORT=4581 RHYTHM_DIST_PORT=4582 npm test` → build passed; default stage **474 passed, 49 skipped, 7 failed** (6 live-demo-state-leak trusted-api loopback config and 1 Hermes splitter); downstream stages short-circuited. These are outside the overflow contract, and no base comparison was run in this attempt; previous initial run had the same failure classes plus now-fixed issue-1519-c6.
- Bucket: same env and `npx playwright test --config tests/bucket-a-rendered-repair-playwright.config.ts` → **10 passed, 5 failed**, not the expected 4: issue-1477 screenshot mismatch, gallery video expectation, two auto-promotion ambiguous selectors, plus self-improvement review `proposal-approve-tool-1` unexpectedly present. No clean-base run establishing preexistence, so do not claim all five are baseline or change unrelated code.
- `cd apps/api_server && npm ci --ignore-scripts --no-audit --no-fund` → installed 218 locked packages (no tracked changes); same clean env `cd apps/web && npm run test:electron-slices` → E13/E14/E15/E16/E20/E21/E22/E23/E24/E25A/E25B/E27/E50/E51/E52A and E16 fixture stages passed with live opt-in skips; `npm run test:session-opening` → 13 passed. `git diff --check` passed. GitNexus `detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1581)` → low, 2 indexed Shell symbols, 0 affected processes (CSS not indexed).
- Electron slices generated tracked images in e25b (2), e27 (1), e50 (1), e52a (1) after initial status proved them clean. Explicit scoped `git restore -- docs/ai/runs/artifacts/e25b/inspector-resource.png docs/ai/runs/artifacts/e25b/share-review.png docs/ai/runs/artifacts/e27/live-terminal.png` was **denied by session policy**; none restored, all five remain dirty and must be excluded from this issue. No destructive workaround attempted. Uncommitted issue files plus these generated tracked images remain; no commit/push/merge/stash/clean.
- Contract c7 packaged Electron host parity **not_tested**: manual smoke target is packaged host at 1100/390, compare exact items, visible menu bounds, keyboard Settings reachability, and screenshots with bundled renderer evidence. Astra executable verification follows specialist handoff; no claim of packaged host pass.
- Root cause was CSS clipping, **not** stale `matchMedia` for the initial menu render; separately, open state survived responsive tier transitions. The top-level optional destinations and More contents were already correctly computed before resize. Fixed without new dependency or route/store changes.
- Manual packaged Electron host smoke: **not_tested**; no isolated packaged host or sanitized fixture was provided. Built Vite dist Chromium verifies the same bundled renderer CSS but not the packaged app. Astra UI review: **not_tested**; this specialist stage was instructed not to dispatch peers. Review target is screenshots under `docs/ai/runs/artifacts/issue-1581-*.png`, especially the 1100 and 390 open states.
- Tooling wrote unrelated tracked screenshots in `docs/ai/runs/artifacts/e25b/` and `.last-run.json`/`live-terminal.png` under `e27/` during full Electron slices. Worktree was clean initially; a scoped `git restore -- <four generated files>` was attempted but denied by session tool policy. **Do not include these unrelated generated artifacts in this issue's change set.** No commit, push, merge, stash, or clean.
