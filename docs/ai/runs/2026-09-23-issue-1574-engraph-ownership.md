---
date: 2026-09-23
repo: Rhythm
branch: swarm/issue-1574
pr: null
issues: [1574]
status: ready-for-verification
tags: [run, Rhythm]
---

## Files

- `apps/api_server/src/services/engraph_manager.ts`: same-process authenticated reuse only when executable and canonical root agree; exclusive per-HOME owner reservation before config/index, fail closed on unknown markers. Child relay owns its exact spawned Engraph handle and checks its parent every 250ms, releasing the reservation on child exit. Status reports owned/reused/foreign and count from tracked handles or live marker PID; a foreign process is never signaled.
- `apps/api_server/src/__tests__/issue_1574_engraph_ownership.test.ts`, `apps/api_server/src/__tests__/live_e2e_issue_1574_engraph_process.test.ts`, `apps/api_server/src/__tests__/engraph_manager.test.ts`, `docs/ai/contracts/issue-1574.json`.

## Checks

- Repair attempt 1 Phase 0: `./node_modules/.bin/vitest run --maxWorkers=1 src/__tests__/issue_1574_engraph_ownership.test.ts -t 'disable during indexing'` **FAIL**: expected `{ok:false}`, received `{ok:true}` after Disable raced with indexing. Contract added before repair implementation; verifies disabled state/count and removal of own reservation after settling.

- Root `/private/tmp/rhythm-swarm-1574`, branch `swarm/issue-1574`, initially clean (`pwd && git branch --show-current && git status --short && git rev-parse --show-toplevel`). No real Engraph cleanup or live vault access.
- Initial `npx vitest run src/__tests__/issue_1574_engraph_ownership.test.ts`: infrastructure error (dependencies absent); reused existing dependencies temporarily, then `./node_modules/.bin/vitest run src/__tests__/issue_1574_engraph_ownership.test.ts`: **FAIL** expected `backendCount=1`, received `undefined` (contract before implementation).
- GitNexus upstream impact: `_spawnServe` LOW, direct `_doStart` (1), zero indexed processes; `_doStart` LOW (direct `ensureStarted` 1), `stopManagedProcess` LOW (3), `getStatus` LOW (status route 1), `shutdown` LOW (0). No HIGH/CRITICAL symbols edited.
- `npm ci --ignore-scripts --no-audit --no-fund`, `npm rebuild better-sqlite3 --no-audit --no-fund` in this worktree only; `npm run build` PASS. No repo dependency files intentionally modified.
- `./node_modules/.bin/vitest run --maxWorkers=1 src/__tests__/issue_1574_engraph_ownership.test.ts src/__tests__/engraph_manager.test.ts src/__tests__/engraph_manager_routes.test.ts`: **3 files / 46 passed**.
- `RHYTHM_LIVE_E2E=1 RHYTHM_1574_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1574-sandbox ./node_modules/.bin/vitest run --maxWorkers=1 src/__tests__/live_e2e_issue_1574_engraph_process.test.ts`: **1 passed**. Creates only a synthetic vault, runs the checked-in fake executable, disables once and kills only its own synthetic parent driver; verifies relay-owned marker disappears after both exits. No api_server or engine started, no live ports or processes touched.
- Full API: `npm test -- --reporter=dot` timed out at 360s under concurrent worktree load; `./node_modules/.bin/vitest run --maxWorkers=4 --silent --reporter=dot` timed out at 600s (five failures appeared before timeout, file names/final summary unavailable). **Not a full-suite pass.** No server sandbox was started. Focused route tests use in-memory SQLite and ephemeral listener ports only.
- Final `npm run build` PASS; final focused 3 files / 46 PASS; final env-gated synthetic process test 1 PASS; `git diff --check` PASS; GitNexus `detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1574)` LOW, zero affected indexed processes (untracked files not included in graph diff). Contract re-run 3 PASS + live 1 PASS.

## Notes

- Root cause: each `_doStart` unconditionally rewrote the shared HOME config with a fresh port/key and launched another serve child; only the original `ChildProcess` handle was tracked, and parent SIGKILL left that child orphaned. No PID-based cleanup was performed. Foreign/unverified markers fail closed and require manual inspection; stale markers are never silently deleted.
- Safe operator diagnostic (DOCUMENTED, NOT EXECUTED against AJ's machine): within an approved isolated HOME, inspect `ls -l "$RHYTHM_ENGRAPH_HOME_DIR/.engraph/serve.owner"` and `ps -p <marker-pid> -o pid,ppid,command`; compare executable, approved root and health before any operator decision. If status says `owned` or `reused`, use the app's normal Disable action (or authorized `POST /engraph-manager/disable` in the isolated sandbox on port 5598); if `foreign` or unverified, **do not kill by PID, name, or port or delete the marker**. Escalate for human review; no automatic cleanup command is safe for an unverified marker.
- No #1573 retrieval, live vault, project-state/current-plan, real process cleanup, commit, push, merge, stash or clean. Astra review and complete full-suite qualification remain outstanding.

## Repair attempt 1 — 2026-09-24 (supersedes earlier implementation/checks above)

### Files / design

- `engraph_manager.ts`: generation + abort guard for indexing/health/start; disable/rebuild/shutdown await cancelled work when their promise is awaited; real index child receives the abort signal; pre-ready `finally` releases only this run's nonce reservation. The existing server shutdown caller does not await the manager's returned promise, so abnormal parent exit relies on relay watchdog. A detached relay and its child occupy one process group; relay watches its parent, TERM then bounded verified group KILL on resistant child. Manager escalates only its verified owned group. Marker v1 carries nonce, starting/serving state, canonical root/home, real configured executable, config hash, owner/relay/child PID, OS start/command/ppid/pgid, and port; exclusive link publishes reservation, relay atomically renames takeover, manager publishes serving only after authenticated health. Bounded `/bin/ps` probes validate identities. Foreign/ambiguous markers fail closed; dead starting markers without children are removed without signaling, verified orphan children are boundedly stopped. Status counts only a verified serving backend (starting/stale/foreign = 0).
- `issue_1574_engraph_ownership.test.ts`: disable during held index, same-process racing managers, stale own marker, foreign copied marker, different root. `engraph_manager.test.ts`: exact PID signal allowlist. New `fixtures/fake_engraph_resistant.js`: test-only CLI ignores TERM and can delay listening; production does not load it. `live_e2e_issue_1574_engraph_process.test.ts`: accepts an arbitrary existing `/private/tmp` or `/var/folders` synthetic root; excludes Rhythm Application Support, asserts OS PID+start disappearance for clean disable, late start, resistant disable, synchronous shutdown, and parent SIGKILL normal/resistant. Synthetic driver and fake HTTP listener only; fixture home isolated. On failed cleanup the marker/fixture is left for review, not blindly deleted.
- Existing contract `issue-1574.json` retains four issue criteria, c4 manual diagnostic only; live criterion now passes. No API route handler changed, no real Engraph process inspected or killed. Safe operator diagnostic remains in Notes above and was NOT EXECUTED against AJ's machine.

### Checks / commands

- Confirmed `/private/tmp/rhythm-swarm-1574` branch `swarm/issue-1574`; inherited six files dirty/untracked, all in assigned scope. Phase 0 new test failed before implementation: `./node_modules/.bin/vitest run --maxWorkers=1 src/__tests__/issue_1574_engraph_ownership.test.ts -t 'disable during indexing'` → expected `{ok:false}`, received `{ok:true}`.
- GitNexus `impact` on `EngraphManager` (direct 3), `_doStart` (1), `stopManagedProcess` (3), `getStatus` (1), `_spawnServe` (1), `checkHealthNow` (2), `shutdown` (0): all LOW, zero indexed processes. Standing HIGH approval unused. `detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1574)` LOW, zero affected indexed processes (untracked files not included by tool).
- `npm run build` PASS; `./node_modules/.bin/vitest run --maxWorkers=1 src/__tests__/issue_1574_engraph_ownership.test.ts src/__tests__/engraph_manager.test.ts src/__tests__/engraph_manager_routes.test.ts` **3 files / 49 passed**; `RHYTHM_LIVE_E2E=1 RHYTHM_1574_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1574-sandbox ./node_modules/.bin/vitest run --maxWorkers=1 src/__tests__/live_e2e_issue_1574_engraph_process.test.ts` **1 passed, 0 skipped**, six lifecycle scenarios and zero relay/child survivors. `git diff --check` PASS.
- `node tools/dev/sandbox_fixture.mjs /private/tmp/rhythm-swarm-1574-repair-fixture` generated synthetic read-only DB/config. `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1574-repair-fixture RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1574-repair-fixture/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-swarm-1574-repair-fixture/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1574-repair-sb RHYTHM_SANDBOX_API_PORT=5698 RHYTHM_SANDBOX_ENGINE_PORT=5697 RHYTHM_SANDBOX_GATEWAY_PORT=5699 RHYTHM_OPTIMIZER_MODE=shadow tools/dev/sandbox.sh up` → API+engine ready. Node fetch POST `/engraph-manager/choose-binary`, `/enable` immediately `/disable` → disabled/count0; `/enable` → ready/count1/owned; `/disable` → disabled/count0. `tools/dev/sandbox.sh status` confirmed listeners on 5698/5697/5699; identical sandbox env `tools/dev/sandbox.sh down` removed isolated runtime, sanitized evidence `/private/tmp/rhythm-swarm-1574-repair-sb.evidence.kZQw1v`.
- First full `./node_modules/.bin/vitest run --maxWorkers=4 --reporter=dot`: 7 failed / 6211 passed, 252 skipped; six unrelated reviewer-harness failures because inherited `RHYTHM_LIVE_ENGINE_URL=...:4096` conflicts with hard-pinned test sandbox :4097, plus one #1574 timeout under concurrent load. Focused rerun 47/47 then 49/49 and live 1/1 pass. `RHYTHM_LIVE_ENGINE_URL=http://127.0.0.1:4097 ./node_modules/.bin/vitest run --maxWorkers=1 src/__tests__/org_reviewer_harness_isolation.test.ts` 6/6 pass; **clean complete suite** `env -u RHYTHM_LIVE_ENGINE_URL ./node_modules/.bin/vitest run --maxWorkers=4 --silent --reporter=dot` **668 files passed, 132 skipped; 6220 tests passed, 252 skipped (0 failures)** in 401s. Standard suite skips other unrelated gated tests; the #1574 live test was run separately with zero skips.
- Final shutdown/index cancellation repair: `_runIndex` now forwards AbortSignal to the real `execFile`, manager `shutdown()` returns an awaitable promise, and contract asserts shutdown while indexing releases its reservation. `npm run build` PASS; focused **50/50** PASS; env-gated real-process **1/1 with zero skipped** PASS; `git diff --check` PASS. Final post-change `env -u RHYTHM_LIVE_ENGINE_URL ./node_modules/.bin/vitest run --maxWorkers=4 --silent --reporter=dot` **668 files passed, 132 skipped; 6221 tests passed, 252 skipped, 0 failures** (257.74s).

### Handoff

- READY_FOR_VERIFICATION for independent Astra review (required before #1573). No commit, push, merge, stash, clean, or real machine process cleanup. The existing synchronous server shutdown caller does not await manager work; relay's parent watchdog handles abrupt exit. Awaiting `manager.shutdown()` itself waits for cancellation and verified exit; disable/rebuild do likewise. Operator command remains diagnostic-only for unverified old processes.
- Final GitNexus `detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1574)` LOW / zero affected indexed processes; seven changed paths, all in the assigned allowlist. Dashboard run tracker published with `node publish-to-rhythm.mjs run` (revision 4946→4947); no Rhythm live-artifact tools used.

## Final repair attempt 2 — 2026-09-24 (Opus triage 0fcca13c-75f6-4e35-8a17-ae2124071f7a)

### Files / acceptance

- Root `/private/tmp/rhythm-swarm-1574`, branch `swarm/issue-1574`; initial status showed inherited manager/test/fixture/contract/run-note changes only. No unrelated worktree edits, commit, push, stash, clean, real Engraph/vault/PID access, or production process cleanup.
- Phase 0 contract first: added shutdown/enable race to `issue_1574_engraph_ownership.test.ts`; `./node_modules/.bin/vitest run --maxWorkers=1 src/__tests__/issue_1574_engraph_ownership.test.ts -t 'shutdown closes starts synchronously'` **FAIL** (`expected true to be false`, at line 200). Additional rebuild/index race contract was already green and retained. This proves a synchronous closed gate is necessary, not merely an awaited cleanup.
- Phase 1 GitNexus upstream impacts, indexed repo `Rhythm`: `EngraphManager` LOW (3 direct callers: server, routes, retrieval; zero processes); `ensureStarted` LOW (4 direct, zero processes); `_doStart` LOW (1 direct, zero processes); `rebuild` LOW (1 direct route, zero processes); `shutdown` LOW (0 direct); `server.ts:main` LOW (1 direct, zero processes). New helper `gone`/`reapVerifiedOrphan`/`stopOwnedChild` not individually indexed, covered by indexed manager scope. Standing HIGH approval not exercised; no HIGH/CRITICAL result.
- Scope: manager lifecycle (`gone` only ESRCH or verified recycled start, ambiguous probes fail closed; orphan reaping after verified relay+child gone, exact-identity signals, bounded child exit/grace; re-entrant rebuild/disable and synchronous shutdown closure; config failure classification, early exited-child health); server shutdown typing/await bounded to 1.5s; fixture delay via isolated HOME knob file instead of leaked environment; identity/config/race contracts and nonce-aware live process test. The relay refuses spawn without verified parent identity. `server_shutdown_signal_contract.test.ts` remains unmodified and green. Marker/foreign unknowns remain untouched for manual review.

### Checks / commands (repair loop)

- Initial `npm run build && ./node_modules/.bin/vitest run --maxWorkers=1 src/__tests__/issue_1574_engraph_ownership.test.ts src/__tests__/engraph_manager.test.ts src/__tests__/engraph_manager_routes.test.ts`: build PASS, three test failures in mocked-child health and structural allowlist; repaired synthetic handle's undefined exit/signal handling and updated exact signal allowlist; focused suites then PASS.
- New wrong-identity contract first exposed same-process reuse incorrectly trusting a healthy existing handle: expected `spawn_failed`, received `ok:true`. Required existing marker's verified OS relay and child identities before adoption; rerun focused 53/53 PASS. Config-write-failure contract asserts `permission_denied` and reservation release (no marker after error).
- `env -u RHYTHM_LIVE_ENGINE_URL ./node_modules/.bin/vitest run --maxWorkers=4 --silent --reporter=dot`: first full run **6223 passed / 252 skipped / 1 failed** (`server_shutdown_signal_contract.test.ts` source-order guard: new `process.exit` appeared before `opencodeClient.dispose`). Moved bounded force/finish definitions after disposal. `npm run build && ./node_modules/.bin/vitest run --maxWorkers=1 src/__tests__/server_shutdown_signal_contract.test.ts src/__tests__/issue_1574_engraph_ownership.test.ts src/__tests__/engraph_manager.test.ts src/__tests__/engraph_manager_routes.test.ts`: build PASS, then 59/59 PASS. Final with additional config contract: same focused four files **60/60 PASS**.
- Final full API command `env -u RHYTHM_LIVE_ENGINE_URL ./node_modules/.bin/vitest run --maxWorkers=4 --silent --reporter=dot`: **668 files passed / 132 skipped; 6224 tests passed / 252 skipped, zero failures** (211.47s). The config-failure test was added afterward and rechecked focused 60/60, not included in that full-suite count.
- `RHYTHM_LIVE_E2E=1 RHYTHM_1574_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1574-sandbox ./node_modules/.bin/vitest run --maxWorkers=1 src/__tests__/live_e2e_issue_1574_engraph_process.test.ts` **1/1 PASS ×3** after final nonce assertions and 240s timeout; all six synthetic lifecycle stages assert both exact OS start identities gone and marker released; separate final run 1/1 PASS alongside focused 60/60. No real Engraph executed; synthetic executable and vault only.
- Normal isolated server shutdown path: `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1574-repair-fixture RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1574-repair-fixture/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-swarm-1574-repair-fixture/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1574-repair-sb RHYTHM_SANDBOX_API_PORT=5698 RHYTHM_SANDBOX_ENGINE_PORT=5697 RHYTHM_SANDBOX_GATEWAY_PORT=5699 RHYTHM_OPTIMIZER_MODE=shadow tools/dev/sandbox.sh up` → API/engine ready; Node HTTP choose synthetic binary → `{ok:true}`, enable → accepted, poll status → `ready/1/owned`; same sandbox env `tools/dev/sandbox.sh down` → sandbox removed; sanitized evidence `/private/tmp/rhythm-swarm-1574-repair-sb.evidence.KgVahK`. The sandbox-down output does not retain the child PIDs for an independent post-down survivor assertion; standalone live test above supplies exact PID/start survivor proof. No hand-started api_server or engine.
- `git diff --check` PASS; `git status --short` shows only manager, server, assigned tests, fixture, contract, run note. GitNexus `detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1574)` LOW, 3 tracked changed files (untracked test/contract/run files excluded), zero affected indexed processes.
- Dashboard data-only tracker: `node publish-to-rhythm.mjs run '{"agent":"claude-code","session":"swarm/issue-1574","task":"Rhythm #1574: owned Engraph lifecycle final repair","status":"pass","note":"build, focused 60, full API 6224, synthetic live 3/3; Astra pending"}'` → `rev 5010 -> 5011`. No Rhythm live-artifact tools.

### Handoff

- READY_FOR_VERIFICATION for independent Astra review; #1573 stays blocked until Astra PASS. No commit/push/merge/deploy or real-machine cleanup. Diagnostic-only command and foreign-marker non-action in Notes above remain applicable. The full suite's 252 normal skips were not reported as passes; the gated #1574 live test was run separately three times without skips.

## Repair 3 (2026-09-24, AJ-revalidated)

### Scope / changes

- Launch discipline matched `/private/tmp/rhythm-swarm-1574`, branch `swarm/issue-1574`, HEAD `61718027`. Repair stayed within the user-owned manager, focused tests, fake resistant executable, contract, and this run note; `server.ts` was not changed. No commit, stash, reset, checkout, package mutation, or real Rhythm data/process cleanup.
- Cleared obsolete reuse ownership before a manager reserves/spawns its own backend. `stopManagedProcess()` now continues through local-child cleanup after stopping a reused owner. Status validates the current registered owner before reporting `reused`.
- Indexing now runs below a detached watchdog relay. The marker records `indexStarting`, then exact relay/child PID, OS start, command, PPID, and PGID identities. Cancellation/parent loss signals the relay; resistant children receive bounded identity-gated escalation. A starting reservation is never removed while an index launch or recorded index child is live.
- `getStatus()` takes one process-table snapshot, counts every matching live serve process (healthy/unhealthy owned, reused, foreign-marker, or unmarked legacy stray), and returns per-entry `classification`, `state`, and `action`. Async startup uses a non-blocking process listing to refuse a new spawn when a command-line-verified unmarked backend already matches the configured binary/port/home. Unmarked processes are reported only and never signaled.
- Restored the blanket unsafe-PID kill assertion in addition to the exact expression list. The resistant fake executable can hold and resist the index phase. Contract criterion c2 is marked `UNVERIFIED` for Repair 3 because AJ prohibited the live E2E rerun and assigned it to the orchestrator; c1/c3 are covered by the focused fake-executable integration suite.

### Checks / evidence

- Baseline exact focused command before Repair 3: `npx vitest run src/__tests__/engraph_manager.test.ts src/__tests__/issue_1574_engraph_ownership.test.ts` — **2 files / 47 tests passed**.
- Test-first run after stricter contracts: same command — **44 passed / 7 failed**. Failures exposed foreign/stray invisibility, stale reuse ownership, absent index identities, zero process-snapshot calls, and the initial structural blanket assertion.
- First implementation rerun: **46 passed / 5 failed**; four timeouts were traced to the new relay bypassing intentionally injected index promises, and the remaining failure was an overly narrow structural regex. The injection seam and assertion were corrected without weakening behavior or production ownership gates. Targeted resistant-index rerun: **1 passed / 13 skipped**.
- First typecheck: `npx tsc --noEmit -p tsconfig.json` — **exit 2**, one in-scope union-narrowing error at `engraph_manager.ts:669`; explicit `owned`/`reused` narrowing applied. Immediate exact rerun — **exit 0**.
- Final requested focused command: `npx vitest run src/__tests__/engraph_manager.test.ts src/__tests__/issue_1574_engraph_ownership.test.ts` — **2 files passed / 51 tests passed**, zero failures (15.46s).
- Final requested typecheck: `npx tsc --noEmit -p tsconfig.json` — **exit 0**.
- `git diff --check` before the final verification was clean. GitNexus upstream impact from the latest registered Rhythm graph: `EngraphManager` MEDIUM (3 direct importers, zero indexed processes); `_doStart`, `_runIndex`, `getStatus`, and `stopManagedProcess` LOW. The graph snapshot predates this branch, so current-tree behavior was verified directly by the focused tests.
- Explicitly not run by user direction: `RHYTHM_LIVE_E2E`, `tools/dev/sandbox.sh`, api_server, Opencode engine, Flutter, Electron, package install/update, full API suite, packaged/runtime smoke, or real Engraph/vault/PID inspection. No live or packaged verification is claimed.

## Repair 4 (2026-09-24, bounded old-port candidate repair)

### Files / behavior

- `engraph_manager.ts` discovers serve candidates for the selected executable across loopback ports. Unmarked candidates appear as `stray` / `unknown` / `inspect-manually`, and startup refuses a second spawn while one exists. Signaling remains restricted to an exact owner marker and OS process identity. The pre-reservation process scan is synchronous and fails closed if `ps` fails, preserving same-process manager racing semantics.
- `issue_1574_engraph_ownership.test.ts` adds a separate old-port fake CLI process under a temporary HOME and verifies status, startup refusal, and survival of the foreign child. The inherited gated index parent-death fixture test was run separately before this repair (1 passed, `/private/tmp/rhythm-repair4/1574-index-parent-live.log`); it uses the fake CLI, not the real engine/API.
- `issue-1574.json` records criterion c3 as passing; c2 remains `UNVERIFIED` for real runtime behavior and c4 remains manual review.

### Checks / limits

- RED before repair: `TMPDIR=/private/tmp/rhythm-repair4-engraph-test npx vitest run src/__tests__/issue_1574_engraph_ownership.test.ts -t 'previous random port'` — 14 pass, 1 fail; old-port status returned `[]` (`/private/tmp/rhythm-repair4/1574-contract-red.log`).
- Same targeted command after repair: 1 passed, 14 skipped. Final isolated `TMPDIR=/private/tmp/rhythm-repair4-engraph-test npx vitest run --maxWorkers=1 src/__tests__/engraph_manager.test.ts src/__tests__/issue_1574_engraph_ownership.test.ts`: 52 passed. `npx tsc --noEmit -p tsconfig.json` and `npm run build`: exit 0. `git diff --check`: pass.
- An overlapping first run of route tests, typecheck, and build had 56 pass / 3 fail: two 15-second route setup timeouts and one same-process startup race. The race was repaired and the focused 52-test suite rerun in isolation. Isolated route rerun `TMPDIR=/private/tmp/rhythm-repair4-engraph-test npx vitest run --maxWorkers=1 src/__tests__/engraph_manager_routes.test.ts`: 7 passed. No production Engraph, real vault, process cleanup, api_server, or engine qualification was run here.
