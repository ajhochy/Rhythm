---
date: 2026-07-17
repo: Rhythm
branch: mega/A1-trivia
pr: none (do not push/open per dispatch)
issues: [1099, 1048]
status: implemented; unit + build + live-engine checks green; handing to verification-gate
tags: [run, rhythm]
---

# Cluster A1 (Trivia) — #1099 concurrent cap, #1048 engine session delete

Worktree: `/Users/ajhochhalter/Documents/rhythm-worktrees/mega-A1`
Branch: `mega/A1-trivia` off `origin/mega/opencode-utilization-1042-1108`.

## Files changed

Commit `5c81b84ba` — #1099:
- `apps/api_server/src/services/agent_runner.ts` — default `MAX_CONCURRENT_AGENT_RUNS` 3 → 8 (env still overrides); doc comment updated.
- `apps/api_server/src/__tests__/issue_738_agent_runner.test.ts` — added capacity test: with no env override, a 4th concurrent run is NOT capacity-rejected (proves default rose above old 3). Existing env-override capacity test left intact.

Commit `231108808` — #1048 (OCU-07):
- `apps/api_server/src/services/opencode_client_service.ts` — added `deleteSession(sdkId, directory?)` wrapper: `client.session.delete({ path:{id}, query:{directory} })`, 404-tolerant (envelope error logged, resolves true), returns false only when SDK not initialized. Mirrors existing `getSession` 404 handling.
- `apps/api_server/src/controllers/agent_sessions_controller.ts` — `destroy` (hard delete, `DELETE /:id/hard`) is now async and calls `opencodeClient.deleteSession(sdkSessionId, cwd)` best-effort before removing the local row. `remove` (soft delete, `DELETE /:id`) untouched — does NOT call the engine.
- `apps/api_server/src/__tests__/issue_1048_engine_session_delete.test.ts` — controller contract test (engine mocked): hard delete calls deleteSession exactly once with the SDK id (engine recurses over children → one call), 204 even when engine delete resolves already-gone or throws, soft delete does NOT call it and retains the row.
- `apps/api_server/src/__tests__/live_e2e_1048_engine_session_delete.test.ts` — gated `RHYTHM_LIVE_E2E=1` live test driving the real engine session lifecycle.

## Checks run

Env note: worktree `node_modules` symlinked to the main checkout's (lockfile + manifests identical to `origin/main` — zero diff), so no reinstall. Symlinks are untracked and were never committed.

- GitNexus `impact` upstream, both targets LOW risk: `getMaxConcurrentRuns` (value-only default change), `destroy` (0 upstream callers — Express route handler).
- `npm test -- issue_738_agent_runner issue_1048_engine_session_delete` → 2 files, 17 tests **passed**.
- `npm test -- agent_sessions.test` → 48 tests **passed** (confirms soft-delete path unchanged; that suite's engine mock has no `deleteSession` and never needs it).
- `npm run build` (tsc -p tsconfig.json) → **EXIT=0**, postbuild ran clean.
- Final consolidated: `npm test -- issue_738_agent_runner issue_1048_engine_session_delete agent_sessions.test` → 3 files, **65 tests passed**.

### Live check (#1048) — real running sandbox engine :4097

Sandbox already up (api :4098, engine :4097; did NOT run sandbox up/down). Engine session lifecycle, `directory=$HOME`:

```
POST /session            → 200  id=ses_08f0b1ee7ffeu91F83YR2w4wXF
GET  /session/:id        → 200  (exists)
DELETE /session/:id      → 200  body: true
GET  /session/:id        → 404  {"name":"NotFoundError","data":{"message":"Session not found: ses_..."}}
DELETE /session/:id (2)  → 404  (already gone — tolerated by wrapper)
```

Gated live test run:
`RHYTHM_LIVE_E2E=1 RHYTHM_ENGINE_URL=http://127.0.0.1:4097 npx vitest run __tests__/live_e2e_1048_engine_session_delete.test.ts`
→ **1 passed**. Without the flag → **1 skipped** (won't run in normal suite).

This proves #1048 acceptance: hard delete removes the engine session (GET → 404), and the already-gone DELETE returns 404 which the wrapper tolerates.

## Notes

- Live check is against the engine directly (:4097), not the api_server (:4098): the running sandbox api_server serves the **main checkout's** `dist/`, which does not contain this worktree's `destroy` change, and the dispatch forbids `sandbox.sh up/down`. Driving the real engine's DELETE/GET is the truthful behavioral assertion of the exact contract the new wrapper calls; the controller wiring (hard→deleteSession, soft→no-op) is covered by the mocked contract test.
- SDK contract confirmed from `@opencode-ai/sdk` types: `SessionDeleteData` = `{ path:{id}, query?:{directory} }`, `SessionDeleteResponses` = `{ 200: boolean }`; `session.delete` default `ThrowOnError=false` → 404 surfaces as `raw.error` envelope (non-throwing), matching the live 404 above.
- Not pushed, no PR (per dispatch). Hand off to verification-gate.
