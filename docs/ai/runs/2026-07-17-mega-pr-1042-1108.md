---
date: 2026-07-17
repo: Rhythm
branch: mega/opencode-utilization-1042-1108
pr: (draft — see PR link)
issues: [1042, 1043, 1044, 1045, 1046, 1047, 1048, 1049, 1050, 1051, 1052, 1057, 1058, 1059, 1060, 1061, 1062, 1063, 1064, 1065, 1066, 1069, 1070, 1071, 1072, 1073, 1074, 1075, 1079, 1084, 1088, 1093, 1094, 1099, 1108]
status: green
tags: [run, rhythm, mega-pr, opencode-utilization]
---

# Mega-PR run — Opencode Utilization epic #1042–#1108 (35 issues)

## Outcome

35 issues implemented, verified, and merged into a single integration branch
`mega/opencode-utilization-1042-1108`. Draft PR opened for human smoke + merge.
50 commits, 163 files, +17205/−1169 vs main.

**Verification (final, against sandbox rebuilt on merged code):**
- api_server: `npm run build` (tsc) clean; unit suite **3001 pass / 18 pre-existing
  memory_* fail / 38 skip** — 0 branch-caused failures.
- desktop_flutter: `flutter analyze --no-fatal-infos` **0 errors**; full suite **961 pass**.
- Live e2e (serial, isolated sandbox :4098/:4097, OpenRouter Haiku): **6/6 pass**
  (#1048 session.delete, #1057 worktree, #1070 global SSE, #1073 permission deny,
  #1088 hidden-schedulable, #1094 image-gen grant).

## Execution model

Ran as app-partitioned parallel clusters (cap 2 coding-agents), each in its own
git worktree off the integration branch; orchestrator merged each cluster back as
it landed. Stages:

- **Stage 0** — A1 (#1099, #1048) ‖ A2 (#1084)
- **Stage 1** — Spine B-api (#1042,#1044,#1045,#1049,#1050,#1057,#1058,#1060,#1063–66 api-halves,#1070) ‖ B-flt front (#1047,#1046)
- **Stage 1b** — B-flt remainder (#1063–66 flt-halves, #1061, #1062)
- **Stage 2** — Wave C (#1088,#1073,#1094) + Wave D (#1093 noop, #1108,#1071,#1069,#1072)
- **Stage 3** — E-flt-1 (#1043,#1051,#1052,#1059) ‖ E-flt-2 (#1074,#1079)
- **Stage 4** — Wave F dead-code sweep (#1075)

## Migration runOnce keys added (local SQLite; only #1072 touches Postgres)

issue_1058_worktree_fields, issue_1088_picker_schedule_fields,
issue_1073_permissions_json, issue_1094_image_gen_capability,
issue_1069_tool_events, issue_1072_org_settings (+ postgres_bootstrap org_settings
additive table — the sole prod-schema change, flagged for manual review).

## Fixes made during integration (all test-authoring / merge-surface, not product bugs)

- #1042 mock regression: issue_736/818 contract tests mocked only `respondPermission`;
  #1042's auto-deny path calls `replyToPermission` — added the mock.
- #1088/#1073/#1057 live tests: wrong response shapes ({sessions}/{messages}),
  wrong profile-id field (agentKind not agentId), wrong transcript role ('output'
  not 'assistant'), empty model id → OpenRouter Haiku default; #1057 rewritten to
  drive Rhythm's /opencode/worktrees wrapper (not the raw engine).
- Integration-test fake `_FakeAgentsRepository` updated for the grown AgentsRepository
  interface (files/vcs/find/worktree methods) — restored `flutter analyze` to 0 errors.

## Excluded / deferred (NOT in this PR)

- **#1076 (OCU-35)** — tracking-only (watch list), no code. Left open.
- **#1068 (OCU-27)** — BLOCKED on #1067 (fork SDK regen), not in scope; needs a fork
  rebuild + re-sign. Excluded.
- **#1096** — Engraph service-manager + Settings UI epic; blocker PR #1095 is merged
  but scope is multi-PR (signed-app smoke). Deferred to a follow-up after #1093 settles.
- **#1079 hint** — the "scheduled-but-hidden" advisory hint was deferred (backend
  guard #1039 already prevents the bad state); toggle itself shipped.

## Known operational notes

- Live e2e tests share ONE engine → MUST run with `--no-file-parallelism` (serial).
  Parallel run = worktree/session contention false-failures.
- Sandbox live runs use OpenRouter (`anthropic/claude-haiku-4.5`) because sandbox
  isolation can't reach keychain-bound Anthropic OAuth. Env-overridable via
  RHYTHM_LIVE_MODEL_PROVIDER / RHYTHM_LIVE_MODEL_ID.
- 18 pre-existing `memory_*` unit failures are vault-filesystem test pollution,
  unrelated to this PR (no memory files touched).

## Next step

Human: manual smoke per docs/testing/manual-smoke.md, then merge. No agent merge.
