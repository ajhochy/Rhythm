---
date: 2026-09-23
repo: Rhythm
branch: swarm/issue-1491
pr: null
issues: [1491]
status: ready_for_verification
tags: [run, Rhythm]
---

## Files
- Contract: `docs/ai/contracts/issue-1491.json`; API + Flutter issue-1491 tests.

## Checks
- Root `/private/tmp/rhythm-swarm-1491`, branch `swarm/issue-1491`, clean at entry.
- Phase 0 red: `cd apps/api_server && npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts` — 2 failing assertions: pending taskTitle null (expected Inspect PCO changes); malformed event returned HTTP 200 (expected >=400).
- Phase 0 red: `cd apps/desktop_flutter && /Users/ajhochhalter/development/flutter/bin/flutter test test/features/agents/agent_trigger_watcher_test.dart --plain-name 'issue-1491-c3: webhook poll preserves target and untrusted draft exactly once'` — NoSuchMethodError PendingTrigger.profileId after actual watcher poll.
- Initial environment retries: npm ci --ignore-scripts then npm rebuild better-sqlite3; flutter pub get. Earlier invocations failed due to absent dependencies/Flutter PATH, not accepted as red contract evidence.

## Notes
- API `receive` impact LOW, direct caller agentWebhookRoutes.ts; zero indexed processes. `recordTriggerAsync` LOW, direct caller receive, no indexed processes. `handleIncomingTrigger` LOW, direct caller watcher._poll, no indexed processes. API route graph has no matching route; manually inspected public HMAC route.
- No live ports or production database touched. Payload is external data; do not interpolate as trusted instructions.
- STOP / approval gate: GitNexus impact on `PendingTrigger`, `apps/desktop_flutter/lib/features/agents/controllers/agents_controller.dart`, repo Rhythm, branch `swarm/issue-1491`, returned HIGH (27 direct, 34 total at depth 2; 2 modules Agents/Controllers, zero indexed processes). Intended scope: additive nullable scheduled task/profile/prompt metadata on pending trigger for watcher → banner → session composer; retain legacy task trigger behavior. Direct dependents include `agents_view.dart`, `agent_trigger_watcher.dart`, `app_shell.dart`, `main.dart`, `quick_actions_bar.dart`, agent model picker, session views, and integration smoke test (27 total imports). No AJ informed approval for this exact symbol/scope in dispatch. No implementation edits permitted until approval; API + Flutter test/contract files only. Other planned symbols `receive`, `recordTriggerAsync`, `rowToModel`, `handleIncomingTrigger`, `_PendingTriggerBanner`, `_startAgent`, `dismissTrigger` all LOW.
- Not run while blocked: build, full API suite, format/analyze/Flutter suite, sandbox behavioral test, detect_changes. No commit/push/merge/stash/clean.

## Continuation (2026-09-23)
- Verified `/private/tmp/rhythm-swarm-1491`, branch `swarm/issue-1491`; only the pre-existing Phase-0 Flutter test modification and untracked API contract, JSON contract, and this note were present. No implementation changes.
- Re-ran red: `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts` in `apps/api_server`: 2 failures, `taskTitle` null instead of `Inspect PCO changes`, malformed event HTTP 200 instead of >=400. `/Users/ajhochhalter/development/flutter/bin/flutter test test/features/agents/agent_trigger_watcher_test.dart --plain-name 'issue-1491-c3: webhook poll preserves target and untrusted draft exactly once'` in `apps/desktop_flutter`: failed `NoSuchMethodError: PendingTrigger.profileId`.
- Fresh GitNexus upstream impact for `PendingTrigger` (`apps/desktop_flutter/lib/features/agents/controllers/agents_controller.dart`, repo Rhythm) at depth 2, excluding tests: HIGH, 27 direct, 34 total, 0 processes, Agents/Controllers modules. This matches AJ's approved additive nullable target/profile/prompt scope in the latest handoff.
- The same impact query **including tests** returned CRITICAL, 96 direct, 110 total at depth 2, 0 indexed processes, Agents/Controllers modules. Direct imports include `lib/main.dart`, `lib/app/core/agents/agent_trigger_watcher.dart` (transitively), `integration_test/follow_up_smoke_test.dart`, numerous `test/features/agents/*` consumers, and the production agents/session UI. This expands the disclosed blast radius beyond the approved 27-direct HIGH record. Scope remains additive fields only, retaining existing scheduled-trigger behavior. BLOCKED pending AJ's explicit informed approval of this CRITICAL including-tests impact for this exact symbol/scope; no source implementation edits or further verification run.

## Continuation after AJ's PendingTrigger approval (2026-09-23)
- Verified `/private/tmp/rhythm-swarm-1491`, branch `swarm/issue-1491`, with only the pre-existing modified watcher test and three untracked acceptance artifacts. AJ's new handoff explicitly approves CRITICAL `PendingTrigger`, 96 direct/110 depth-2 including tests, limited to additive nullable target/profile/prompt fields; this supersedes the earlier blocked PendingTrigger finding only.
- Phase 0 red reconfirmed: `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts` (from `apps/api_server`) failed 2/2: `taskTitle` null and malformed event HTTP 200. `/Users/ajhochhalter/development/flutter/bin/flutter test test/features/agents/agent_trigger_watcher_test.dart --plain-name 'issue-1491-c3: webhook poll preserves target and untrusted draft exactly once'` (from `apps/desktop_flutter`) failed on missing `PendingTrigger.profileId` after polling. No implementation source edited.
- GitNexus recheck: `PendingTrigger`, `apps/desktop_flutter/lib/features/agents/controllers/agents_controller.dart`, repo `Rhythm`, CRITICAL 96 direct/110 depth-2, zero indexed processes, two modules, matching approval. API `AgentWebhookController.receive` LOW (1 direct/3 total), `ClaudeTriggersRepository.rowToModel` LOW (2 direct/3 total), Flutter `AgentsController.handleIncomingTrigger` LOW (1 direct/2 total). Route impact lookup found no indexed match; manually inspected HMAC public route.
    - **New, unapproved HIGH impact discovered before editing:** `_PendingTriggerBanner`, `apps/desktop_flutter/lib/features/agents/views/agents_view.dart`, repo `Rhythm`, branch `swarm/issue-1491`, HIGH 28 direct/29 total at depth 2 including tests, zero indexed processes/modules. Scope needed: only banner label/title for configured webhook target and on-click configured profile/session plus one-shot sanitized prompt draft; keep default Secretary/task behavior. Direct dependents are `app_shell.dart`, `integration_test/follow_up_smoke_test.dart`, and 26 `test/features/agents/*` imports (agent selection, composer/session, approvals, navigation, badges, inspectors and rendering); transitive dependent `lib/main.dart`. AJ's approval specifies `PendingTrigger`, not this separate symbol. BLOCKED awaiting AJ's informed approval for this exact banner symbol/scope; no implementation changes or live sandbox commands performed. No commit/push/merge/stash/clean.

## Resumed implementation with AJ's explicit high-risk approval

### Files
- `apps/api_server/src/controllers/agentWebhookController.ts`: bounded registration/receive inputs; resolved target task prompt; untrusted bounded event JSON retained separately from approved instructions; no event/payload logging.
- `apps/api_server/src/repositories/claude_triggers_repository.ts`: read-time scheduled task title and profile join (SQLite/Postgres; no schema change).
- `apps/desktop_flutter/lib/features/agents/controllers/agents_controller.dart`: nullable webhook trigger metadata with existing task constructors preserved.
- `apps/desktop_flutter/lib/features/agents/views/agents_view.dart`: configured target label/profile, one-shot editable draft before selecting the session; webhook does not pass synthetic task ID to session create; guard duplicate click.
- `apps/api_server/src/__tests__/issue_1491_webhook_contract.test.ts`, `apps/api_server/src/__tests__/issue_1491_webhook_live_e2e.test.ts`, `apps/desktop_flutter/test/features/agents/agent_trigger_watcher_test.dart`, `docs/ai/contracts/issue-1491.json`.

### Checks
- `gh issue view 1491 --json title,body`, `git branch --show-current`, `git status --short`: root `/private/tmp/rhythm-swarm-1491`, branch `swarm/issue-1491`, acceptance sourced from #1491; no unrelated edits at start.
- Reconfirmed Phase 0: `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts` → 2 failing assertions (missing task title; malformed event 200). `flutter test test/features/agents/agent_trigger_watcher_test.dart --plain-name 'issue-1491-c3: webhook poll preserves target and untrusted draft exactly once'` → NoSuchMethodError `PendingTrigger.profileId`.
- GitNexus before source edits: `AgentWebhookController.receive` LOW (1 direct/3 depth-2, zero processes), `AgentWebhookController.create` LOW (1/3, zero processes), `PendingClaudeTrigger` LOW (3/7, zero processes), `ClaudeTriggersRepository.rowToModel` LOW (2/3, zero processes), `AgentsController.handleIncomingTrigger` LOW (1/2, zero processes), `_PendingTriggerBanner._startAgent` LOW (1/1, zero processes). API route impact could not index `/agent-webhooks/:id/receive`; inspected route manually (public HMAC). `PendingTrigger` CRITICAL (96 direct/110 depth-2 including tests, 0 processes, Agents/Controllers), `_PendingTriggerBanner` HIGH (28 direct/29 depth-2, 0 processes; app shell and 26 agent tests), exactly AJ's newly approved symbols/scopes in this handoff. No new HIGH/CRITICAL symbol needed.
- `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts && npm run build` → 2/2 pass, tsc pass after one lower-case trust-label repair.
- Final `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts src/__tests__/issue_1491_webhook_live_e2e.test.ts` → 2 pass, 1 live skipped (explicit `RHYTHM_LIVE_E2E` gate); skipping is NOT live evidence.
- `flutter test test/features/agents/agent_trigger_watcher_test.dart` → 13/13 pass after adjusting widget viewport and asserting that the composer consumed the draft; unrelated session setup sends one other message, so test checks **no payload-bearing send** rather than no setup traffic.
- `dart format . --output=none --set-exit-if-changed` → 525 files, 0 changed; `flutter analyze --no-fatal-infos` → exit 0, 319 existing infos; `flutter test` → 1323 passed, all tests passed.
- First `npm test` → 6210 passed, 6 failures in unrelated `org_reviewer_harness_isolation.test.ts`: inherited `RHYTHM_LIVE_ENGINE_URL=http://127.0.0.1:4096` versus fixture 4097. `env -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_LIVE_URL -u RHYTHM_OPENCODE_ENGINE_PORT npx vitest run src/__tests__/org_reviewer_harness_isolation.test.ts` → 6/6 pass. Full `env -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_LIVE_URL -u RHYTHM_OPENCODE_ENGINE_PORT npm test` → 668 files passed / 132 skipped; 6216 tests passed / 252 skipped.
- `PATH=/Users/ajhochhalter/development/flutter/bin:$PATH ai-workflow checks --level issue` → 4/4 passed (analyze, format, API tsc, MCP tsc). Initial invocation without Flutter on PATH failed at launch, not a code failure. `ai-workflow checks --level smoke` → manual checklist pointer `docs/testing/manual-smoke.md`; mounted deterministic fixture tests webhook action and legacy watcher; physical app manual smoke not performed.
- `bun run build --single` in fork → first preload missing; `bun install --frozen-lockfile --ignore-scripts` then `bun run build --single` → fork binary smoke passed. `npm ci --ignore-scripts && npm run build` in `apps/mcp_server` → pass. `node tools/dev/sandbox_fixture.mjs /private/tmp/rhythm-swarm-1491-fixture` → read-only synthetic inputs created. Exact sandbox command (blocked by owned port, no process killed): `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1491-fixture RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1491-fixture/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-swarm-1491-fixture/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1491-sandbox RHYTHM_SANDBOX_API_PORT=5898 RHYTHM_SANDBOX_ENGINE_PORT=5897 RHYTHM_SANDBOX_GATEWAY_PORT=5899 tools/dev/sandbox.sh up` → `sandbox: port :5897 is occupied; refusing to touch it`. `lsof -nP -iTCP:5897 -sTCP:LISTEN`, `ps -p 65094 -o pid,ppid,command`, `lsof -a -p 65094 -d cwd -Fn`: unrelated `opencode serve`, PID 65094, PPID 1, cwd `/Users/ajhochhalter/Documents/Rhythm-electron-flutter-retirement`, persists on retry. Did not stop or repurpose this process or use unapproved ports. Live `RHYTHM_LIVE_E2E=1` contract and sandbox status/down cannot run, so c4 remains UNVERIFIED. No claim of backend runtime completion.
- `gitnexus_detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1491)` → medium, affected symbols in intended API/Flutter paths, no unrelated source changes; graph's broad hunk attribution lists TokenBreakdown but diff shows no change to it. `git diff --check` → clean. No commit/push/merge/stash/clean.
- `git diff --no-index --check /dev/null <each untracked contract/run file>` → no whitespace errors. A final targeted Flutter watcher test rerun passed 13/13 after adding no-payload-bearing-automatic-send assertion; its initial strict zero-send assertion failed because ordinary session setup sends an unrelated message. Full Flutter suite preceded that test-only assertion tightening; focused rerun is green.

### Notes / handoff
- BLOCKED for READY_FOR_VERIFICATION only by unrelated engine listener at assigned port 5897 and unperformed live HMAC/real engine smoke. Run the env-gated `RHYTHM_LIVE_E2E=1 RHYTHM_LIVE_API_URL=http://127.0.0.1:5898 RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1491-sandbox/rhythm.db npx vitest run src/__tests__/issue_1491_webhook_live_e2e.test.ts` after the assigned sandbox starts safely, then `tools/dev/sandbox.sh status` and `down` under the same 5898/5897/5899 environment. Do not kill PID 65094 absent separate owner authorization. Astra final review remains pending.

## Live qualification continuation (2026-09-23)

### Files
- `apps/api_server/src/__tests__/issue_1491_webhook_live_e2e.test.ts`: parameterized the sandbox guard with assigned environment and tightened observable prompt assertions for configured prompt, event type, note path, untrusted label, summary, and single consume; no product edits in this continuation.
- `docs/ai/contracts/issue-1491.json`: c4 changed from UNVERIFIED to pass after real live run.

### Checks and exact commands
- `pwd && git branch --show-current && git status --short && git rev-parse --show-toplevel` from `/private/tmp/rhythm-swarm-1491`: confirmed worktree root and `swarm/issue-1491`; existing issue changes only. `lsof -nP -iTCP:{5997,5998,5999} -sTCP:LISTEN` checked individually: no listeners at entry. Approved synthetic `/private/tmp/rhythm-swarm-1491-fixture/{rhythm.db,opencode.json}` both mode `0400`; fork binary existed; new sandbox path did not exist. Did not interact with PID 65094 or :5897.
- Phase 0 previously captured two API reds and Flutter missing metadata (above). This continuation's `RHYTHM_LIVE_E2E=1 RHYTHM_LIVE_API_URL=http://127.0.0.1:5998 RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1491-live2-sandbox/rhythm.db RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1491-live2-sandbox RHYTHM_SANDBOX_API_PORT=5998 npx vitest run src/__tests__/issue_1491_webhook_live_e2e.test.ts` (in `apps/api_server`) failed 1/1 on the pre-existing hardcoded `5898` guard, before any live launch; test-only guard repaired. GitNexus impact for the new untracked live test returned UNKNOWN/no indexed symbol; no product symbol edited.
- `npm run build` (in `apps/api_server`) passed tsc. `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1491-fixture RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1491-fixture/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-swarm-1491-fixture/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1491-live2-sandbox RHYTHM_SANDBOX_API_PORT=5998 RHYTHM_SANDBOX_ENGINE_PORT=5997 RHYTHM_SANDBOX_GATEWAY_PORT=5999 tools/dev/sandbox.sh up` rebuilt fork/API/MCP and reported `Sandbox ready: http://127.0.0.1:5998 (engine :5997)`. The transient initial curl connection-refused during readiness polling recovered; `tools/dev/sandbox.sh status` with the same sandbox/port variables showed API/gateway listener 73389 and engine 73482.
- `RHYTHM_LIVE_E2E=1 RHYTHM_LIVE_API_URL=http://127.0.0.1:5998 RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1491-live2-sandbox/rhythm.db RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1491-live2-sandbox RHYTHM_SANDBOX_API_PORT=5998 RHYTHM_SANDBOX_ENGINE_PORT=5997 RHYTHM_SANDBOX_GATEWAY_PORT=5999 npx vitest run src/__tests__/issue_1491_webhook_live_e2e.test.ts` in `apps/api_server` passed 1/1 live through real HMAC route, queue, poll and DELETE then empty poll. `tools/dev/sandbox.sh down` with matching sandbox/ports removed sandbox and preserved sanitized diagnostics at `/private/tmp/rhythm-swarm-1491-live2-sandbox.evidence.2uKFyK`.
- Strengthened live assertions for exact configured target prompt, event type and note path; restarted using the same exact `up`, checked `status` (API/gateway 75930, engine 75948), reran the exact env-gated live command: 1/1 passed, then ran matching `down`: sandbox removed; sanitized diagnostics at `/private/tmp/rhythm-swarm-1491-live2-sandbox.evidence.weZJkN`. An attempted chained status/test command from the API working directory failed only because relative `tools/dev/sandbox.sh` was not present there; corrected by running status at worktree root and test at API directory. No runtime failure.
- `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts src/__tests__/issue_1491_webhook_live_e2e.test.ts` (API directory without env gate) → 2 pass, 1 intentionally skipped; not counted as live evidence. `git diff --check` clean. `gitnexus_detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1491, repo=Rhythm)` → medium, 5 tracked changed files from earlier implementation, only indexed flow Build→TokenBreakdown (broad hunk attribution; not an actual TokenBreakdown edit); new test is untracked and not indexed. Full API 6216 and Flutter 1323 suite passes are recorded above and unchanged by this test-only guard/assertion edit.
- Final recheck after assertion/contract edit: `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts src/__tests__/issue_1491_webhook_live_e2e.test.ts && npm run build` → 2 pass, 1 env-gated skip, tsc pass. `git diff --check` and `git diff --no-index --check /dev/null` on the untracked live test, contract JSON, and run note → clean. Repeated `gitnexus_detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1491, repo=Rhythm)` → medium, 5 existing tracked files, no newly indexed source edits; `lsof -nP -iTCP:{5997,5998,5999} -sTCP:LISTEN` individually returned no listeners after `down`.

### Handoff
- READY_FOR_VERIFICATION: #1491 live c4 passed in approved isolated sandbox. Only test and contract/run-note edits in this continuation; sandbox stopped, no commit/push/merge/stash/clean, no unrelated process or live port touched. Astra final review follows; physical Flutter/manual smoke remains separate.

## Repair attempt 1 (Opus review dfc24ef2-20fe-42cb-bdd4-6433351b2cb2)
- Verified root `/private/tmp/rhythm-swarm-1491`, branch `swarm/issue-1491`, existing dirty issue files; no unrelated files touched. Acceptance contract reused from #1491 and expanded for the review blockers.
- Phase 0 RED: `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts` in `apps/api_server`: 3 new failures: missing structural fence, missing fallback fence, unsafe note path returned 200. `flutter test test/features/agents/agent_trigger_watcher_test.dart --plain-name 'issue-1491-c3: scheduled trigger with profile metadata still starts Secretary without draft'` in `apps/desktop_flutter`: red, actual agentId/profileId worship-agent instead of secretary/null. Two-banner test initially green; guard still required for keyed identity. No implementation source edited before these runs.
- Phase 1 impact: `AgentWebhookController.receive` LOW, 1 direct route caller, 3 depth-2, 0 processes. `_PendingTriggerBanner` HIGH, 28 direct/29 depth-2 (app shell and agent UI tests), 0 indexed processes; matching AJ standing informed approval recorded above for this banner and configured-target/legacy scope. `_startAgent` LOW, 1 direct build caller, 0 processes. No shared security helper modifications.

### Repair files and validation
- `agentWebhookController.ts`: use existing scanner and canonical fence helper; escape `<` in JSON before fencing, replace only first literal `{{payload}}` slot with callback (remove duplicate slots), append fence if absent; blocked scan queues warning without payload; reject unsafe note paths. No shared security helpers edited. `agents_view.dart`: keyed banners, webhook-only profile, watched late profile labels, legacy Secretary kept. Owned API/widget/live tests exercise hostile fence markers, `$&`/`$'`, blocked scanner warning and log secrecy, note path rejection, two sequential banners, double tap, late profile load, failed create retry, no-profile webhook and scheduled profile compatibility; live asserts one canonical fence and second DELETE 404.
- First repair validation: `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts && npm run build` (API): 1 pre-existing assertion expected lowercase `untrusted` rather than canonical uppercase directive; updated assertion to require actual fence, then 5/5 pass and `npm run build` pass. `dart format lib/features/agents/views/agents_view.dart test/features/agents/agent_trigger_watcher_test.dart && flutter test test/features/agents/agent_trigger_watcher_test.dart && flutter analyze --no-fatal-infos`: scheduled test initially expected null repository profile but controller binds effective `secretary` (existing #1365 behavior); corrected expectation, focused watcher 17/17 pass, analyze exit 0 (319 infos). `dart format` applied to both owned Flutter files; later test-only addition formatted separately.
- `env -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_LIVE_URL -u RHYTHM_OPENCODE_ENGINE_PORT npm test` in API: 668 passed files/132 skipped; **6219 passed/252 skipped**. `flutter test` in Flutter: **1325 passed**, all tests passed (before last test-only additions); final `flutter test test/features/agents/agent_trigger_watcher_test.dart` **17/17 passed** including additions. `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts src/__tests__/issue_1491_webhook_live_e2e.test.ts && npm run build && git diff --check` in API after final log-secrecy assertion: 5 pass, 1 intentionally gated live skip, build pass, whitespace clean.
- `ls -ld /private/tmp/rhythm-swarm-1491-fixture/{rhythm.db,opencode.json}` verified 0400 sanitized fixtures; `lsof -nP -iTCP:5997/5998/5999 -sTCP:LISTEN` found no listeners. `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1491-fixture RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1491-fixture/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-swarm-1491-fixture/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1491-live2-sandbox RHYTHM_SANDBOX_API_PORT=5998 RHYTHM_SANDBOX_ENGINE_PORT=5997 RHYTHM_SANDBOX_GATEWAY_PORT=5999 tools/dev/sandbox.sh up` → ready API 5998/engine 5997; same sandbox/ports `tools/dev/sandbox.sh status` → listeners 5975 and 5993. `RHYTHM_LIVE_E2E=1 RHYTHM_LIVE_API_URL=http://127.0.0.1:5998 RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1491-live2-sandbox/rhythm.db RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1491-live2-sandbox RHYTHM_SANDBOX_API_PORT=5998 RHYTHM_SANDBOX_ENGINE_PORT=5997 RHYTHM_SANDBOX_GATEWAY_PORT=5999 npx vitest run src/__tests__/issue_1491_webhook_live_e2e.test.ts` → **1/1 passed**, including real queue, canonical fence, first consume 204, second 404, empty poll. Matching `tools/dev/sandbox.sh down` → sandbox removed; sanitized diagnostics `/private/tmp/rhythm-swarm-1491-live2-sandbox.evidence.1ek9lM`. No live ports/processes touched.
- `gitnexus_detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1491, repo=Rhythm)` → medium, five tracked issue-owned files, one broad hunk-attributed TokenBreakdown process (no actual TokenBreakdown edit); untracked contract/live/run artifacts excluded by graph. `git diff --check` clean. No commit/push/merge/stash/clean.
- READY_FOR_VERIFICATION; Astra final review required. Full suite evidence predates final test-only assertions but affected files re-ran focused tests green. Physical Flutter/manual smoke remains outside this automated handoff.

## Final repair attempt 2 (2026-09-24, Opus 832ac34b-d8b4-4a3a-a1fe-e3ee22467009)

### Files
- `apps/api_server/src/controllers/agentWebhookController.ts`: scan signed raw JSON, unescaped JSON and whitespace-normalized decoded keys/string leaves BEFORE escaping `<` for the canonical one-fence payload; blocked values produce warning only. No shared scanner edits.
- `apps/api_server/src/database/postgres_bootstrap.ts`: one nullable `agent_config_id` ALTER ahead of the cloud/relay role return; remove redundant late ALTER, no production data rewrite.
- `apps/api_server/src/__tests__/issue_1491_webhook_contract.test.ts`, `issue_1491_postgres_pre_role.test.ts`, `postgres_bootstrap_live.test.ts`, `issue_1491_webhook_live_e2e.test.ts`: route regressions, static guard, disposable real PostgreSQL cloud/relay double bootstrap + repository read, portable sandbox guard and signed blocked payload.
- `apps/desktop_flutter/test/features/agents/agent_trigger_watcher_test.dart`, `integration_test/follow_up_smoke_test.dart`: Enter twice and native macOS named label/title/draft smoke. Contract JSON and this run note updated.

### Checks
- Entry `pwd && git branch --show-current && git status --short && git rev-parse --show-toplevel` in `/private/tmp/rhythm-swarm-1491`: branch `swarm/issue-1491`; eight existing issue-owned modified/untracked files only. Prior acceptance criteria #1491 in existing contract retained. No commit/push/merge/stash/clean.
- Phase 0 RED: `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts src/__tests__/issue_1491_postgres_pre_role.test.ts` (API): after correcting two test harness typos, 2 failing assertions / 6 passing; newline injected summary retained in queued prompt instead of `[BLOCKED:]`; `agent_config_id` DDL at offset 68300 was AFTER role gate at 36427. Earlier acceptance RED and approvals documented above; no source edited before this red.
- GitNexus upstream impact before implementation: `AgentWebhookController.receive` LOW, one direct caller `agentWebhookRoutes.ts`, 3 total depth-2, zero processes; `runPostgresBootstrap` LOW, one direct caller `initDb`, 2 total, zero processes (Database module). API route impact lookup did not index public `/agent-webhooks/:id/receive`; real HTTP route tests cover it. Existing CRITICAL `PendingTrigger` and HIGH `_PendingTriggerBanner` source code from approved prior attempts was not changed in this repair.
- `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts src/__tests__/issue_1491_postgres_pre_role.test.ts && npm run build`: 8/8 passed; initial build failed only because static test used import.meta under CommonJS, changed test to `__dirname`; subsequent `npm run build` passed.
- Docker daemon unavailable (`docker version` could not connect). Owned `/opt/homebrew/opt/postgresql@17/bin/initdb` is version **17.11**; **PG16 not run** (no PG16 container). Owned PG17 cluster `/private/tmp/rhythm-1491-pg17-owned` on 127.0.0.1:65491, exclusively fresh `rhythm_1491` DB, unique throwaway schemas; startup needed `LC_ALL=C` (first attempt fatal locale). `RHYTHM_LIVE_POSTGRES_BOOTSTRAP=1 RHYTHM_LIVE_POSTGRES_URL=postgresql://ajhochhalter@127.0.0.1:65491/rhythm_1491 npx vitest run src/__tests__/postgres_bootstrap_live.test.ts`: cloud + relay new tests **2/2 passed** including missing-column recreation, sentinel preservation, bootstrap twice, and real `ClaudeTriggersRepository.listAllAsync` reading profileId; 3 unrelated pre-existing all/local/default and one repeat-bootstrap test failed due stale expected `agent_sessions` column/index lists (8 newly added existing columns and archived index). Tried updating column expectation, second run showed additional missing index expectation; reverted expectation change as out-of-scope. Total run 5/9 pass (cloud+relay and other tests), 4 unrelated stale-schema assertions red. `pg_ctl -D /private/tmp/rhythm-1491-pg17-owned stop -m smart` → owned PG stopped. **Manual review required** before additive production Postgres `ALTER TABLE` (brief ACCESS EXCLUSIVE lock even with IF NOT EXISTS); no production DB or migration executed.
- `env -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_LIVE_URL -u RHYTHM_OPENCODE_ENGINE_PORT -u DB_PATH npm test` (API): **669 files passed /132 skipped, 6222 tests passed /254 skipped**. `flutter test` (Flutter): **1327 passed**, all green. `dart format . --output=none --set-exit-if-changed` initially identified two new tests, explicit `dart format integration_test/follow_up_smoke_test.dart test/features/agents/agent_trigger_watcher_test.dart`, then whole-project format check clean; `flutter analyze --no-fatal-infos` exit 0, 319 existing infos. `flutter test test/features/agents/agent_trigger_watcher_test.dart` **17/17 pass** (Enter twice one create). `flutter test integration_test/follow_up_smoke_test.dart -d macos --plain-name 'issue-1491-c3 macOS: webhook target title, profile label and editable draft'` built native Rhythm.app and **1/1 passed**, title/label/draft visible; `open` foreground returned 1 but integration test executed successfully.
- Sanitized 0400 fixture `/private/tmp/rhythm-swarm-1491-fixture/{rhythm.db,opencode.json}` and ports 5997/5998/5999 verified. `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-swarm-1491-fixture RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-swarm-1491-fixture/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-swarm-1491-fixture/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1491-live2-sandbox RHYTHM_SANDBOX_API_PORT=5998 RHYTHM_SANDBOX_ENGINE_PORT=5997 RHYTHM_SANDBOX_GATEWAY_PORT=5999 tools/dev/sandbox.sh up` → ready; matching `status` showed API 76142/engine 76203; first live attempt refused inherited production `DB_PATH` (correct safety guard). `env -u DB_PATH RHYTHM_LIVE_E2E=1 RHYTHM_LIVE_API_URL=http://127.0.0.1:5998 RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-swarm-1491-live2-sandbox RHYTHM_SANDBOX_API_PORT=5998 RHYTHM_SANDBOX_ENGINE_PORT=5997 RHYTHM_SANDBOX_GATEWAY_PORT=5999 npx vitest run src/__tests__/issue_1491_webhook_live_e2e.test.ts` (API): **1/1 passed**, allowed event and blocked signed newline injection both real queue/consume. Matching `tools/dev/sandbox.sh down` removed sandbox, saved sanitized evidence `/private/tmp/rhythm-swarm-1491-live2-sandbox.evidence.HRjpmm`.
- `gitnexus_detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1491, repo=Rhythm)` → medium; 8 tracked issue-owned files, process Build→TokenBreakdown broad hunk attribution only, no actual TokenBreakdown edit. No unrelated source edit.

### Handoff
- **BLOCKED** for final PASS due to existing full Postgres suite stale schema assertion failures (4/9) and PG16 gap; focused cloud+relay PG17 coverage passed. Requires manager disposition on the unrelated historical `agent_sessions` expectation drift and PG16 qualification; Astra rerun only after disposition. Production additive ALTER may briefly lock `agent_scheduled_tasks`; schedule/manual review before production application. No commit/push/merge/stash/clean; no production DB/live ports touched.

## Repair 3 (2026-09-24, AJ-revalidated)

### Files
- `apps/api_server/src/repositories/claude_triggers_repository.ts`: joined webhook endpoints into the shared trigger SELECT, resolved the title as task, scheduled task, endpoint name, then endpoint id, and exposed the endpoint name separately for session naming.
- `apps/api_server/src/__tests__/issue_1491_webhook_contract.test.ts`: added the endpoint-only title regression.
- `apps/api_server/src/__tests__/postgres_bootstrap_live.test.ts`: added the eight existing `agent_sessions` columns and archived index to the live schema expectation; removed nothing.
- `apps/desktop_flutter/lib/features/agents/controllers/agents_controller.dart`: added a nullable endpoint-name field and a non-consuming keyed draft read so removal happens after composer placement.
- `apps/desktop_flutter/lib/features/agents/views/agents_view.dart`: made composer text session-scoped, retained occupied-session webhook drafts with a visible pending banner, and named webhook sessions from endpoint identity.
- `apps/desktop_flutter/test/features/agents/agent_trigger_watcher_test.dart`: made fake session ids distinct and added exact per-session draft/name plus occupied-composer retention assertions. The test changes tighten behavior; no assertion was weakened or removed.
- `docs/ai/contracts/issue-1491.json`: dated Repair 3 and returned the unrun Flutter criterion to pending.

### Checks
- RED before implementation: `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts` in `apps/api_server` -> 1 failed / 7 passed; endpoint-only trigger title was `null` instead of `Planning Center callback`.
- Focused after implementation and again after the final endpoint-name field: the same command -> 1 file passed, 8/8 tests passed on both runs.
- `npx tsc --noEmit -p tsconfig.json` in `apps/api_server` -> exit 0 before and after the final endpoint-name field.
- Required combined command, run again after the final source edit: `npx vitest run src/__tests__/issue_1491_webhook_contract.test.ts src/__tests__/issue_1491_postgres_pre_role.test.ts src/__tests__/postgres_bootstrap_live.test.ts src/__tests__/claude_triggers*.test.ts` -> sandbox socket failure, verbatim root error `listen EPERM: operation not permitted 127.0.0.1`; 2 files failed, 1 passed, 1 skipped; 15 tests failed, 1 passed, 9 skipped; 15 unhandled EPERM errors. The PostgreSQL-live file remained skipped without its env flag.
- Dart/Flutter format, analyze, and widget tests were not run, per Repair 3 launch discipline; deferred to the orchestrator.
- No sandbox, api_server, engine, Flutter, Electron, live test, package install, commit, stash, reset, checkout, push, or production data operation was performed.

### Notes
- GitNexus upstream impact was LOW for the repository SELECT, draft consume method, draft-consume/send methods, trigger ingestion, and trigger start method. The enclosing transcript state and trigger banner were HIGH (28 direct/29 total); `PendingTrigger` and `AgentsController` were CRITICAL because those central model/controller types have broad app reach. AJ's explicitly revalidated Repair 3 scope authorized the narrowly bounded additive edits.
- Acceptance contract summary: 4 criteria total, 4 automated criteria, 0 manual; API c1/c2 passed in the focused run, Flutter c3 remains pending for orchestrator execution, and prior live c4 evidence was not rerun or claimed as current live verification.

## Repair 4 acceptance contract (2026-09-24)
- Extended `issue-1491-c3` with the occupied-composer quiescence case: retain existing text and pending webhook draft without repeatedly scheduling frames, then consume once after clear. Added the bounded frame-loop assertion and exactly-once consume assertion to the existing Flutter widget test.
- RED: `flutter test test/features/agents/agent_trigger_watcher_test.dart --plain-name 'occupied composer keeps a webhook draft pending until empty'` from `apps/desktop_flutter` -> failed at `tester.binding.hasScheduledFrame`: expected false, actual true after six bounded pumps. Full output: `/private/tmp/rhythm-repair4/1491-contract-red.log`.
- Minimal source fix for implementer: in `agents_view.dart`'s post-frame pending-draft callback, when the composer already contains user text, return without calling `setState`; the pending banner and draft are already present, and that `setState` is what queues the next callback/frame indefinitely. Clearing the composer still rebuilds and allows the one-time consume path.
- GitNexus impact is UNKNOWN: GitNexus tools were not exposed in this session. The test-only symbol change is local to this widget test; no product symbol was edited.
- Flutter auto-ran dependency resolution/download during the test invocation. No intentional package installation was requested; the contract is red regardless of dependency resolution output.
