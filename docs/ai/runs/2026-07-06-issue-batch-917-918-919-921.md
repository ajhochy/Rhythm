---
date: 2026-07-06
repo: Rhythm
branch: issue-batch-914-923
pr: null
issues: [917, 918, 919, 921]
status: uncommitted
tags: [run, Rhythm]
---

# Run — Issues #917, #918, #919, #921

## Files

- `apps/api_server/src/database/migrations.ts` — idempotent SQLite profile data repairs for MCP tool names, researcher scopes, research skill pruning, model bindings, and tier hints.
- `apps/api_server/src/services/agent_runner.ts` — hardcoded fallback model bumped to `anthropic/claude-sonnet-4-6`.
- `apps/api_server/src/services/opencode_agent_writer.ts` — disabled profile rows are no longer projectable to opencode agent files.
- `apps/desktop_flutter/lib/features/agents/views/agents_view.dart` — pending-trigger banner starts scoped `secretary` sessions instead of raw `claude-code`/`codex`.
- Focused tests updated/added around migrations, disabled projection, Rhythm Setup seed, and fallback model expectations.

## Checks

- PASS: `cd apps/api_server && npx tsc --noEmit`
- PASS: `cd apps/api_server && npx vitest run src/__tests__/agent_configs.test.ts src/services/__tests__/opencode_agent_writer_projection.test.ts src/__tests__/rhythm_setup_seed.test.ts src/__tests__/issue_738_fix_model_and_session.test.ts src/__tests__/issue_738_agent_runner.test.ts src/__tests__/interactive_scope_parity.test.ts` — 6 files, 50 tests.
- FAIL (environment): `cd apps/api_server && npm test` — 1971 passed, 423 failed, 58 skipped; failing suites were dominated by `listen EPERM` / hook timeouts when tests attempted to bind ephemeral HTTP servers.
- FAIL (environment): `cd apps/desktop_flutter && dart format .` and `flutter analyze --no-fatal-infos` both stopped before checking code because the Flutter SDK tried to write `/Users/ajhochhalter/development/flutter/bin/cache/engine.stamp`, outside the writable sandbox.

## Notes

- GitNexus MCP tools were not exposed in this session; the repo-local runner did not return output within 30 seconds. Direct caller searches were used for the edited symbols.
- `foo` was not found as a production repo seed; hits are tests, comments, and fixtures.
- Removing the two stale disabled duplicate files from `~/.config/opencode/agents` was attempted but blocked by filesystem permissions.

## Orchestrator re-verification (port-capable environment)

The two "FAIL (environment)" lines above were sandbox artifacts, not real failures. Re-run outside that sandbox:

- PASS: `cd apps/api_server && npx tsc --noEmit`
- PASS: `cd apps/api_server && npm test` — 2451 passed / 1 skipped (287 files), up from the 2449/1 baseline after cluster B.
- PASS: `cd apps/desktop_flutter && flutter analyze --no-fatal-infos` — 272 issues, matching the documented pre-existing baseline (no new issues).
- Removed the two stale `~/.config/opencode/agents/{32294c7d-...,d74b471f-...}.md` files directly (no permission block here).
