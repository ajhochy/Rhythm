---
date: 2026-07-16
repo: rhythm
branch: workflow/skill-discovery-cost-2026-07-16
pr: pending
issues: [1116, 1098, 1109, 1110, 1111, 1112, 1113, 1114, 1115, 1056, 1053, 1054, 1055, 1090, 1067]
status: verified-green, PR pending
tags: [run, rhythm]
index: "[[Rhythm]]"
---

# Epic #1116 Self-Improvement Engine — mega-PR

One mega-PR resolving all 13 child issues of epic #1116, built from 6 disjoint-ownership
worktree clusters across 2 waves, integrated to `workflow/skill-discovery-cost-2026-07-16`.

## Clusters / waves
- **Wave 1** (4 parallel worktrees): A harvest cost+discovery (#1109 #1110 #1111 #1113 #1112 #1114),
  C skills-UI badges (#1055), D fork SDK regen (#1067), E session-leak (#1090).
- **Wave 2** (2 parallel, after A integrated): B org skill library (#1053→#1054∥#1056),
  F org-optimizer undici timeout (#1115).
- Cluster A ran as 3 sequential sub-agents (A1 #1109/#1110 → A2 #1111/#1113 → A3 #1112/#1114)
  for context/risk management.

## Files changed (by area)
- **api_server harvest/discovery:** skill_extractor, agent_runner, skill_refiner,
  harvested_skill_evaluator, opencode_stream_bridge, org_optimizer_seed, org_optimizer_run_service,
  gap_discovery_scheduler (new), generators/external_discovery_*, agent_capability_gaps_repository,
  agent_org_proposals_repository, org_proposal_appliers_wiring, database/{migrations,postgres_bootstrap}.
- **api_server org library:** routes/org_skills_routes (new), repositories/org_skills_repository (new),
  opencode_plugin_config, org_proposal_apply_service, skill_apply, org_proposals_{routes,controller}, app.ts, server.ts.
- **api_server timeout:** controllers/org_optimizer_run_controller.
- **mcp_server:** api_client (+ explicit undici dep), tools/orgOptimizer.
- **desktop_flutter:** agent_skills_view, agents/data/opencode_skills_data_source, agents/views/_agent_profile_sheet,
  agents/controllers/agents_controller, agents/models/agent_session.
- **opencode_fork:** sdk/openapi.json + sdk/js/src/v2/gen (regen only, 131→133 ops).

## Checks run (integrated mega branch, sandboxed HOME)
- api_server: `tsc` clean, `vitest` 2877 passed / 32 skipped / 0 failed.
- mcp_server: `tsc` clean, `vitest` 80 passed.
- desktop_flutter: `flutter analyze` clean (272 info baseline), `flutter test` 875 passed.
- opencode_fork: `bun test` 366 pass (session suites), `openapi.json` 133 ops incl. skill.reload/config.reload.

## Live e2e (per issue, against spawned api_server + patched fork / Docker Postgres)
- #1109 harvest gating: ≤1 harvest/session + cooldown proven live (session counts).
- #1110 cheapen: cheap model (haiku) proven live by A1; deny-all skills + preface-skip + cheap-tier
  routing proven structurally via 9/9 test hooks on real code. **Exact live token total not captured**
  (sandbox HOME cuts Anthropic creds; stock fork prompt-too-long) — metric only, behavior verified.
- #1111 crons: 4-boot reconciliation cycle (re-enable, dedup, idempotent, respect user intent) via raw DB.
- #1113 + agent_org_proposals parity: live Docker Postgres — table exists, insert→read→resolve,
  persist-across-restart.
- #1112 gap-driven: scheduler fires ~1.5s after gap insert (real compiled code).
- #1114 MCP discovery: live stub registry — external-adoption proposal `proposed`, injection candidate
  dropped, approve→scoped install, gap resolved.
- #1053/#1054/#1056 org library: live — auth on writes, index shape (fork-parser-compatible), engine
  GET /skill lists org skill via real fork, publish→POST/reject→noop/unpublish→DELETE/prod-down→failed.
- #1090 session leak: WS-insert scope predicate (controller + model), mounted-surface tests.
- #1115 timeout: undici headersTimeout>300s proven at runtime with a real slow server.
- #1067 fork regen: types-only, no runtime change; binary builds.

## Decisions / deviations
- Folded `agent_org_proposals` Postgres parity into the epic (user-approved) — same drift #1113 fixed;
  #1114/#1056 proposals now durable on prod. See decisions/2026-07-16-epic-1116-dispatch-decisions.md.
- #1115 stopgap (raise undici timeout) not fire-and-return. #1055 built Wave 1. #1053 public reads (no-secrets test).
- B2 caught + fixed a real bug: #1053 file route path didn't match the fork's `Discovery.pull`
  (`<base>/<name>/<file>`, no `files/` segment) — org library would have silently 404'd every file.
- F caught a real runtime bug: userland `undici` `setGlobalDispatcher` has no effect on Node's global
  `fetch()` (separate vendored undici) — the fix routes through undici's own fetch+Agent.

## Follow-ups filed (out of scope, not folded)
- `task_4cc07f52` — the pre-existing #1039/#1040 undici fix in `api_server/server.ts` may itself be inert
  (same global-fetch pattern F proved doesn't work). Investigate separately.
- Pre-existing test hygiene: `issue_723_mcp_remove_reconcile.test.ts` writes the real
  `~/.config/opencode/opencode.json` (leaky fs mock misses atomic `.tmp`+rename) — pollutes with a
  `foo`/`npx foo-mcp` entry on every full-suite run under real HOME. Run suites under sandboxed HOME
  until fixed. `ensureCuratedMcps` config path also lacks a sandbox override (A3 flag).
- Pre-existing flaky tests (`issue_895_agent_approvals`, a research-job vault test) — 1 intermittent
  fail on ~1/2 full-suite runs; pass on re-run. Watch CI.

## Next
Push mega branch → CI gate (server_ci/desktop_ci/mcp_server_ci) → open PR (Closes #N ×13) →
human merge → trigger desktop_release.yml (patch bump; rebuilds bundled Node + fork).
