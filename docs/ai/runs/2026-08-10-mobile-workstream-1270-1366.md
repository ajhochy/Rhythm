---
date: 2026-08-10
repo: Rhythm
branch: mega-ws/mobile
pr: null
issues: [1270, 1280, 1308, 1311, 1363, 1364, 1366]
status: blocked-verification
tags: [run, Rhythm]
---

# Mobile workstream: #1270, #1280, #1308, #1311, #1363, #1364, #1366

## Files

- Mobile profile fallback, composer/attachment policy, atomic session open,
  discovery fencing, scope/timer ownership, reachability selectors, and focused
  Jest/contract coverage under `apps/mobile/`.
- Per-route gateway JSON limits plus OpenCode HTTP/SSE error classification and
  tests under `apps/api_server/src/`.
- Dry-run-first reviewed session-binding cleanup CLI and audit tests under
  `apps/api_server/src/cli/`.
- Physical-device and human-reviewed cleanup steps in
  `docs/testing/manual-smoke.md`.

## Checks

- `cd apps/mobile && npm run lint && npm run typecheck && npm test -- --runInBand`
  — exit 0; 16 suites, 58 tests passed; lint reported two pre-existing warnings
  and no errors.
- `cd apps/mobile && npm run test:ci:static` — exit 0, including security and
  pairing contract suites.
- `cd apps/mobile && npm run build:web:ci` — exit 0; Expo exported 15 routes.
- `cd apps/api_server && ./node_modules/.bin/tsc --noEmit && npm run build` —
  exit 0.
- `cd apps/api_server && ./node_modules/.bin/vitest run
  src/services/__tests__/mobile_gateway_surface.test.ts
  src/__tests__/issue_1169_mobile_opencode_proxy.test.ts
  src/cli/session_binding_cleanup.test.ts src/cli/doctor.test.ts
  src/cli/setup.test.ts -t "issues #1308/#1311|issue-1311: preserves|issue
  #1363|doctor|setup"` — exit 0; 5 passed, 22 filtered/skipped.
- `ai-workflow checks --level issue` — failed for environment reasons: Flutter
  cannot write its SDK `engine.stamp`, and the workflow's `npx tsc` tries the
  blocked npm registry instead of the installed local TypeScript binary.
- `ai-workflow checks --level pr` — same failures; its API Vitest phase was
  stopped after unrelated real-server tests reproduced the sandbox-wide socket
  prohibition.
- `cd apps/api_server && npm test` — API typecheck/build completed first; the
  suite cannot complete because this worker rejects every loopback `listen`
  with `EPERM`. A minimal Node `net.createServer().listen(0, '127.0.0.1')`
  reproduced the same failure independently of Rhythm code.
- `cd apps/opencode_fork/packages/opencode && bun run build --single` — failed
  before compilation because the worktree lacks the offline dependency
  `@opentui/solid/preload`. `tools/dev/sandbox.sh up` stopped at the same build
  step; `sandbox.sh status` confirmed no API or engine listener existed.

## Notes

- The #1363 apply command was not run. The tool defaults to read-only dry-run;
  apply requires a completely reviewed report, `--apply`, `--approval-file`,
  and a new `--audit-output`. A human must review every candidate, execute the
  apply command, and relaunch desktop/mobile to verify the result.
- #1280 still requires physical-iPhone UIKit composer growth/cap/scroll checks.
- #1364 still requires cold-start latency measurement over a representative
  remote gateway; #1366 native timer suspension, reachability, and
  background/foreground behavior share that device matrix.
- The existing env-gated #1169 live suite exercises the real paired gateway,
  fork engine, prompt file/data handling, and persisted transcript. It could not
  run because the fork could not be built and this worker cannot bind sockets.
- Commits created: `54a20348`, `c35b4dfe`, `4670cdb8`, and the final #1363
  cleanup commit. One Git-index write was transiently denied by the sandbox;
  retrying after verification succeeded. Nothing was pushed.
