---
date: 2026-08-10
repo: Rhythm
branch: mega-ws/flaky
pr: null
issues: [1247, 1310]
status: blocked
tags: [run, Rhythm]
---

# Flaky races #1247 and #1310

## Files

- `apps/mobile/tests/fake-opencode/server.mjs`
- `apps/mobile/tests/contract/issue-1247.test.mjs`
- `apps/opencode_fork/packages/opencode/src/effect/runtime-flags.ts`
- `apps/opencode_fork/packages/opencode/src/session/prompt.ts`
- `apps/opencode_fork/packages/opencode/test/session/prompt.test.ts`
- `docs/ai/contracts/issue-1247.json`
- `docs/ai/contracts/issue-1310.json`

## Checks

### Issue #1247

- Red reproduction: executing the real pre-fix `handleSse` implementation with an already-pending permission produced only `server.connected`; expected `server.connected` followed by `permission.asked`.
- Socket-free real-function regression contract: 5/5 consecutive runs passed after the fix.
- Exact Playwright command: `npx playwright test tests/e2e/flows.spec.mjs --grep 'permission requests unblock the agent flow' --repeat-each=5`.
- Exact Playwright result: 0/5 test runs executed. Playwright's fake web server failed first with `listen EPERM: operation not permitted 127.0.0.1:44096`.
- Mobile lint: pass with 0 errors and 2 pre-existing warnings.
- Mobile typecheck: pass.
- Mobile Jest: 15/15 suites and 53/53 tests passed.
- `verify:foundation`: every stage through corrective tests #1224 and #1225 passed, then the fake-server self-test failed before assertions with `listen EPERM` on `127.0.0.1:4196`.

### Issue #1310

- Red falsification: setting the injected shell kill grace back to 3000 ms made the zero-grace contract fail at 3008 ms against the `<1000` ms assertion.
- Focused cancel-scope proof: 10/10 consecutive runs passed; each run executed both shell cancellation cases, for 20/20 test invocations.
- Fork typecheck: pass.
- Runner cancel suite: 11/11 passed.
- Cross-spawn forced-escalation test: 1/1 passed.
- Full fork session suite: 290 passed, 4 skipped, 1 todo, and 77 failed during listener setup; the sandbox rejects `NodeHttpServer` binding even port 0 with `EADDRINUSE`.
- SDK `build:rhythm`: pass; generated artifacts remained clean.
- Standalone `build --single`: blocked before compilation because the build script could not fetch `https://models.dev/api.json` (`ConnectionRefused`).

### Repository PR gate

- Passed: API lint, fork typecheck, mobile static suite, and mobile contract.
- Environment/setup failures: Flutter cannot update its SDK cache outside the writable workspace; API/MCP dependencies are absent and npm registry access is unavailable; listener-backed fork/mobile tests cannot bind localhost.

## Notes

- #1247 now replays pending permission state to each newly ready SSE subscriber, closing the subscribe-after-broadcast race without rebroadcasting to existing clients.
- #1310 tests now wait for a marker written by the actual child shell before cancelling. The production 3000 ms TERM-to-KILL grace remains unchanged; the test injects zero grace through a runtime flag, and cancellation still awaits the real process-exit finalizer.
- No timeout was increased and no retry was introduced as a fix.
- Verification gate status is **blocked**, not pass: the code-level proofs are green, but the exact #1247 Playwright 5x mandate and all listener/network-dependent project gates cannot execute in this environment.
