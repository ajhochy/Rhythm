---
date: 2026-09-24
repo: Rhythm
branch: swarm/issue-1579
pr: null
issues: [1579]
status: ready_for_verification
tags: [run, Rhythm]
---

## Files

- Final attempt 2, Opus triage `bfb9c11b-9087-4769-aa23-dbe66852648f`: only `apps/electron/test/issue-1579-contract.test.mjs`, `apps/web/tests/electron/issue-1579-notifications.spec.ts`, `apps/web/tests/issue-1579-playwright.config.ts`, new `apps/web/tests/issue-1579-strict-harness.{html,tsx}`, and this run note. The pre-existing product changes and issue contract/statuses were **not edited**. U3/c9–c12 remain frozen `not_tested`.
- WAIVED: test-fixture and evidence-only repair with frozen product behavior; verification is focused host/browser contracts, mutation kills, parity checks, and unchanged product hashes.
- Product SHA-256 at entry and after: Electron main `ebacf42c603de9bbcc9f6b29c427997bf3dabd2dd5d6082bf68ee881d680ac8d`, preload `8b2677cefc3a67ea0388d57b904028996efface81bd75b445d2063ef2ab015d5`; web store `ce0d4fbd7aaccd01b95d5896aa061ef94cf05631f6f01f4d138ec2df3a950311`, Transcript `b18906d16dd26413ff40f8afd47252c40dcd66a24324e4d5749169be71c40c3f`, AgentsWorkspace `f446f9ee5ac37c3369d3e0cac33fec27090be2ae72ee2760059a52a3bc149f61`, helper `74c8174ede67a32a45026e78d2bac3395c2cbbb9361c462ff2e90744bbbf2b18`.

## Checks

- `pwd && git branch --show-current && git status --short && git rev-parse --show-toplevel && git log -1 --format='%h %s'`: `/private/tmp/rhythm-swarm-1579`, `swarm/issue-1579`, head `e93eac6e`; pre-existing U1/U2 dirty files intact. GitNexus `impact` for new unindexed host/browser test files returned UNKNOWN/0 indexed dependents, not HIGH/CRITICAL. `detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1579)` LOW, 7 indexed tracked files and 0 affected processes (pre-existing product changes); untracked test additions reviewed with `git status --short`. `git diff --check` passed.
- Phase 0 waiver above: no implementation edits. Initial new fixture run `node --experimental-vm-modules --test test/issue-1579-contract.test.mjs`: 29/33 with four **test-fixture** failures (tombstone sweep, recorded startup loadURL, repeated ready show/focus), corrected without product edits. Browser `env -i HOME=/Users/ajhochhalter PATH=/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin PLAYWRIGHT_BROWSERS_PATH=/Users/ajhochhalter/Library/Caches/ms-playwright npm exec -- playwright test --config tests/issue-1579-playwright.config.ts`: first 3/4 because StrictMode opened two sockets; addressed current socket, final **4/4 PASS** using sanitized 6389/6388/6387. Real VM Date clock, exact event key whitelist, valid default fetch envelope, normal/StrictMode completion matrix, FIFO/resolved skip, tombstone threshold, rate/cap, valid mismatch, suppression recheck, mutable window state, pre-ready routing, reload/window cleanup and replacement identity are covered by host t1/t8–t16.
- `node --experimental-vm-modules --test --test-concurrency=1 test/issue-1579-contract.test.mjs test/e12a-auth-boundary.test.mjs test/post-m1-phase-7-native-notifications.test.mjs && npm run typecheck` (Electron): **48/48 PASS**, typecheck PASS. `env -i HOME=/Users/ajhochhalter PATH=/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin npm test` (Electron): **168/168 PASS**. `npm run build && npm run typecheck` (web): PASS (1696 modules, pre-existing bundle-size warning).
- VM-only mutation command (no on-disk product edits): `node -e '/* spawn node --experimental-vm-modules --test --test-name-pattern=issue-1579-tN: test/issue-1579-contract.test.mjs with RHYTHM_1579_MUTANT=M1..M11 */'`: **M1–M11 killed 11/11**; test fixture replaces matched source text in its VM module only. Browser response mutation command `node -e '/* spawn npm exec -- playwright test --config tests/issue-1579-playwright.config.ts --grep "electron-e22-harness.html completion matrix" with RHYTHM_1579_WEB_MUTANT=W1..W3 */'`: **W1/W2/W3 killed 3/3** through in-memory Vite response interception, no production edit. Baseline focused suites reran green after these checks.
- `env -i HOME=/Users/ajhochhalter PATH=/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin PLAYWRIGHT_BROWSERS_PATH=/Users/ajhochhalter/Library/Caches/ms-playwright RHYTHM_E2E_PORT=6389 RHYTHM_DIST_PORT=6388 RHYTHM_API_BASE=http://127.0.0.1:65534 RHYTHM_ENGINE_BASE=http://127.0.0.1:65533 npm exec -- playwright test --workers=4 --reporter=dot`: **466 passed/49 skipped/1 failed**, only existing Hermes splitter selector. An initial unbounded default attempt timed out after 240 seconds; its orphaned isolated 6389/6388 Vite/dist servers were identified by `lsof`/`ps` and stopped by PID before the successful bounded parity attempt. No API/engine process was launched.
- Same clean environment and ports: `npm exec -- playwright test --config tests/bucket-a-rendered-repair-playwright.config.ts --reporter=dot`: **11 passed/4 failed**, same existing screenshot/gallery-video/auto-promotion selector failures (bucket config itself uses its own existing 4181). `npm run test:session-opening`: **13/13 passed**; `npm run test:electron-slices`: all fixture slices passed (E14/E23/E27 each one live-only skip); `npm run test:dist-smoke`: passed index and 2 relative assets. No unrelated failures were changed.

## Notes / handoff

- **READY_FOR_VERIFICATION for U1/U2 contract fixture repair only.** This is not a packaged/native U3 PASS. #1579 c9–c12 remain `not_tested`, no contract statuses changed. Astra U1/U2 review next; do not thaw U3 on this report alone.
- Generated tracked screenshot files appeared during the full Electron/browser checks: `docs/ai/runs/evidence/electron-m1-shell.png`, `docs/ai/runs/artifacts/e25b/{inspector-resource,share-review}.png`, `docs/ai/runs/artifacts/e27/live-terminal.png`, `docs/ai/runs/artifacts/e50/focus-and-toast.png`, `docs/ai/runs/artifacts/e52a/results/electron-e52a-transcript-E-1d7e9--repins-without-live-tokens/new-output.png`. **Do not include them**; orchestrator owns restoration. No commit, push, merge, stash, clean or product edits.

## Repair 3 (2026-09-24, AJ-revalidated)

### Files and behavior

- `apps/electron/src/main.mjs`, `apps/electron/src/preload.cjs`: the first completion arm now sends a closed `arm` intent to main. Main runs one fixed-text `Notification.show()` permission probe, records `granted | denied | unknown | unsupported`, and sends the closed result back through `rhythm:agent-notifications:permission`; renderer web-notification permission remains denied.
- "Explicit" is intentionally platform-bounded: Electron has no main-process `requestPermission()` API. On macOS, the first main-owned `Notification.show()` is the OS permission-request trigger; a `show` event is recorded as `granted`, an explicit permission-denied failure as `denied`, no observable result or a platform exception as `unknown`, and only `Notification.isSupported() === false` as `unsupported`. Other supported platforms use the same best-effort show probe without claiming that the OS displays a permission prompt. Every outcome fails soft and does not block session work.
- Completion presentations now use per-arm cycles, ignore duplicate completion events, and retire outside the pending-ask registry on close, successful click routing, or a five-minute TTL. Pending asks retain the prior resolution/tombstone semantics. A regression test closes 100 distinct completion presentations and proves the 101st is admitted.
- Ownership lookup now requests `/agent-sessions/:id?transcriptLimit=0`; loopback-only origin validation, redirect rejection, 1.5-second timeout, response-size cap, and exact session-ID validation remain intact. The regression boundary returns an oversized transcript for any nonzero limit and proves the metadata-only request still presents the notification.
- `apps/web/src/store.tsx` seeds `workingSessions` from adopted list/detail hydration when `session.status === 'working'`. The renderer spec now asserts behavior: an unarmed working-to-idle transition emits no completion; an armed transition emits exactly one; a hydrated working session armed before its idle frame also emits exactly one.
- `docs/ai/contracts/issue-1579.json` restores the original explicit-permission criterion. Contract inventory remains 12 criteria: 8 automated mappings and 4 manual/not-tested U3 or OS-observation criteria.
- Tests were strengthened or corrected, never weakened: the post-M1 source contract now requires the explicit arm/probe/outcome IPC; repeated clicks and duplicate completions must be inert until re-arm; close/TTL capacity and transcript-free lookup have behavioral assertions; the browser source-anchor mutation was removed in favor of user-event/state-transition assertions. One VM cross-realm options object is spread into the host realm before exact title/body equality; this corrects prototype-only false failure while preserving the same value assertion.

### Checks

- Contract-first red phase: `cd apps/electron && node --experimental-vm-modules --test --test-concurrency=1 test/issue-1579-contract.test.mjs test/post-m1-phase-7-native-notifications.test.mjs` exited 1 with **29 passed / 10 failed**. All ten failures were the intended D1/D3/D4 assertions; there were no syntax or harness errors.
- After implementation, the same two-file command first reported **38 passed / 1 failed** due only to cross-realm object prototype equality. After the test-boundary normalization it reported **39 passed / 0 failed**.
- User-specified literal command `cd apps/electron && node --test test/issue-1579-contract.test.mjs test/post-m1-phase-7-native-notifications.test.mjs test/e12a-auth-boundary.test.mjs` exited 1 before VM test collection: **1 passed / 2 failed**. Both failures were `SyntaxError: The requested module 'node:vm' does not provide an export named 'SourceTextModule'` under Node `v22.23.0`; the command omitted the repository-required `--experimental-vm-modules` flag.
- Repository-corrected command `cd apps/electron && node --experimental-vm-modules --test --test-concurrency=1 test/issue-1579-contract.test.mjs test/post-m1-phase-7-native-notifications.test.mjs test/e12a-auth-boundary.test.mjs` exited 0: **53 passed / 0 failed**.
- `cd apps/electron && npm run typecheck`: exit 0. `node --check src/main.mjs && node --check src/preload.cjs`: exit 0.
- `cd apps/web && npm run typecheck`: exit 0.
- `cd apps/web && npm exec -- playwright test --config tests/issue-1579-playwright.config.ts`: no assertions ran; sandbox launch failed verbatim with `Error: listen EPERM: operation not permitted 127.0.0.1:6389` and `Process from config.webServer was not able to start. Exit code: 1`. Static collection `npm exec -- playwright test --config tests/issue-1579-playwright.config.ts --list` exited 0 and listed **5 tests in 1 file**. The orchestrator must rerun the runtime spec outside this sandbox.
- `git diff --check`: exit 0. Branch/SHA at verification: `swarm/issue-1579`, `61d4c171` (changes intentionally uncommitted).
- GitNexus pre-edit impact: indexed `FixtureProvider` was **MEDIUM** (7 upstream, 5 direct, 0 affected processes); newer Electron helpers and `emitAgentNotification` were absent from the 2026-09-21 index and returned UNKNOWN/0, not HIGH/CRITICAL. Post-edit `detect-changes` could not run because `/private/tmp/rhythm-swarm-1579` is not a registered index; indexing it would create out-of-scope files.

### Unrun/manual boundaries

- No Electron launch, packaged app, Notification Center banner/sound/click, System Settings/Focus inspection, live backend, api_server, opencode engine, Flutter, or `RHYTHM_LIVE_E2E` run occurred. Packaged U3 and contract c9-c12 remain manual/not tested; this repair makes no native-delivery or release-qualification claim.
