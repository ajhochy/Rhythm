---
date: 2026-09-12
repo: Rhythm
branch: feature/electron-flutter-retirement
pr: null
issues: [manual-smoke-project-headings-20260912]
status: READY_FOR_VERIFICATION
tags: [run, Rhythm]
---

## Files
- Changed only `apps/web/src/components/SessionRail.tsx`, `apps/web/tests/sessions.spec.ts`, `apps/web/tests/electron-e20-session-ordering.spec.ts`, `docs/ai/contracts/manual-smoke-project-headings-20260912.json`, this note. No CSS/dependency changes.
- Preserving all pre-existing Electron interactive-smoke, sandbox and generated evidence dirt. No runtime contact with candidate PID43749 or live4001/4096.

## Checks
- Phase 0: acceptance-contract invoked first; read AGENTS, project-state, current-plan, testing-guide and targeted implementation/tests. Contract uses unused local manual-smoke slug. New tests catch duplicate dropdown, missing project groups/counts, retained nested status headings, and coupled keyboard collapse.
- GitNexus upstream SessionRail: LOW, 1 direct caller (AgentsWorkspace), 3 total dependents, 0 processes; Components/Gateway. Canonical index predates worktree, corroborated with source.
- Sandbox command from assigned worktree: `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-electron-project-headings tools/dev/sandbox.sh up` PASS, rebuilt fork/API/MCP. `RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-electron-project-headings tools/dev/sandbox.sh status` PASS: API44814/4098, engine44832/4097, gateway44814/4099.
- All web commands run from `apps/web` using `env -i HOME=/private/tmp/rhythm-electron-project-headings/home TMPDIR=/private/tmp/rhythm-electron-project-headings/tmp PATH=/private/tmp/rhythm-electron-project-headings/bin:/Users/ajhochhalter/.local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin PLAYWRIGHT_BROWSERS_PATH=/Users/ajhochhalter/Library/Caches/ms-playwright /bin/zsh -f -c '<command>'`.

## Notes
- Phase 0 complete: `npm exec -- playwright test --config tests/electron-e20-playwright.config.ts --grep project-headings --output /private/tmp/rhythm-project-headings-evidence/red` ran in the owned clean environment: 4 assertion failures (dropdown count 1 instead of 0; absent canonical project button/count; direct project sections 0 instead of 2; absent aria-expanded button). No implementation edits before this red run.
- Manager owns integration/review/rebuild/relaunch; no commit, push, PR, merge or peer dispatch.
- Manual review targets c6-c9: minimal scoped tests/diff, existing styles/no dependencies, isolated commands/screenshots/live evidence and teardown, untouched candidate and unrelated dirt.

### Final checks / handoff
- Phase 1 complete: LOW impact confirmed before implementation. Phase 2 complete: removed project select/state and nested Agents/status disclosures. Project ID keyed expansion defaults open; counts include loaded matching roots/children, including ancestor context. Comparator ordering is retained within each project; heading order follows first sorted root. Cross-project children remain included under their own project (explicit No project child fixture). Existing history/row actions/status metadata preserved.
- Clean-environment command: `npm run typecheck && npm run build && npm exec -- playwright test --config tests/electron-e20-playwright.config.ts --output /private/tmp/rhythm-project-headings-evidence/e20 && npm exec -- playwright test tests/sessions.spec.ts --output /private/tmp/rhythm-project-headings-evidence/sessions && npm run test:dist-smoke` PASS: typecheck/build, 20 E20 tests, 3 lifecycle tests, dist smoke. Existing Vite >500kB chunk warning only. Final contract command rerun after strengthening non-chat/status assertions: 20 + 3 PASS; no repair attempts needed.
- Live check: same clean environment with `RHYTHM_LIVE_E2E=1`, command `npm exec -- playwright test --config tests/electron-e21-playwright.config.ts --output /private/tmp/rhythm-project-headings-evidence/live` PASS 1/1 (13.1s). Existing maintained E21 live test drives real sandbox session API/engine and rendered SessionRail/store through external create, rename/order, archive, delete and selection clearing without prompts. Only unrelated hosted notification/thread boundary is faked. Observed unrelated approvals401 and expected deletion/detail404 logged; all behavior assertions passed. No backend implementation changed.
- Durable screenshot (outside disposable sandbox): `/private/tmp/rhythm-project-headings-evidence/e20/electron-e20-session-order-b07c8-etains-accessible-selection/project-headings.png`. Red failure contexts remain under `/private/tmp/rhythm-project-headings-evidence/red`; final Playwright results under `e20`, `sessions`, `live`. Native keyboard Enter/Space, focus, aria-expanded, exact accessible names/counts and collapse isolation tested.
- `git diff --check` PASS. Scoped diff reviewed; unrelated dirty/generated files remain untouched. Contract c1-c5 automated PASS; c6-c9 explicitly retain manager manual-review gates.
- Teardown: `RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-electron-project-headings tools/dev/sandbox.sh status && RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-electron-project-headings tools/dev/sandbox.sh down` PASS. Same owned PIDs before shutdown; sandbox removed. Sanitized diagnostics preserved at `/private/tmp/rhythm-electron-project-headings.evidence.0INbaH`. No live runtime inspection, contact, signal, relaunch or changes.
- READY_FOR_VERIFICATION: manager integrates/reviews/verifies/rebuilds/relaunches; installed candidate manual smoke remains manager/AJ-owned.

## UI review repair — session33f8c196
- Owned repair files: SessionRail.tsx, electron-e20-session-ordering.spec.ts, this note and the existing contract JSON only. Existing sessions.spec.ts and all runtime/store/gateway/Dashboard/Planner dirt are not owned or edited. No peers, commits, pushes, PRs or merges.
- Phase 0: acceptance-contract invoked first; dispatch provides two explicit P1 criteria. Read repository memory, full rail/test and sandbox instructions. This is the delegated focused repair, not a new orchestration run; workflow-orchestrator is not exposed in this session. Tests exercise the real rendered rail/store/gateway with only HTTP/WebSocket boundaries faked by the existing E20 harness. No backend behavior claim.
- Impact refreshed: SessionRail LOW, one direct caller AgentsWorkspace, zero processes, three dependents total. No HIGH/CRITICAL edits.
- Sandbox up/status from worktree root: `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-project-headings-repair RHYTHM_SANDBOX_API_PORT=4298 RHYTHM_SANDBOX_ENGINE_PORT=4297 RHYTHM_SANDBOX_GATEWAY_PORT=4299 tools/dev/sandbox.sh up`; status uses the same sandbox/port variables. PASS: API47889/4298, engine47908/4297, gateway47889/4299. Distinct ports avoid parallel runtime diagnosis. No contact with live4001/4096 or candidate43749.
- All repair web commands use `env -i HOME=/private/tmp/rhythm-project-headings-repair/home TMPDIR=/private/tmp/rhythm-project-headings-repair/tmp PATH=/private/tmp/rhythm-project-headings-repair/bin:/Users/ajhochhalter/.local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin PLAYWRIGHT_BROWSERS_PATH=/Users/ajhochhalter/Library/Caches/ms-playwright /bin/zsh -f -c '<command>'` from `apps/web`.
- Phase 0 complete: `npm exec -- playwright test --config tests/electron-e20-playwright.config.ts --grep "E20-c9|project-headings-repair-c2" --output /private/tmp/rhythm-project-headings-repair-evidence/red` => 2 assertion failures before implementation. Compact session-z has no visible Working text; equal counts [1,1,1] pass but the accessible/visible Same label (p1) heading is missing. Failure contexts preserved under that output path. Deterministic fixtures include working root/child and resumable root; equal-name/equal-count p1/p2 plus unique-name p3.
- Phase 1 complete: LOW impact reviewed before renderer edits. Shared row renderer covers roots/children through AgentsWorkspace. Collision counts use canonical project metadata rather than filtered group order, so suffixes do not change on sorting/search/scope changes. Full canonical ID is already available; no prefix collision algorithm, CSS or dependency required.
- Phase 2 implementation: compact branch renders existing presentation.label in a visible small element; expanded metadata/preview is unchanged. Only colliding canonical project labels append ` (id)` inside the existing heading span, automatically included in the button accessible name. Collapse remains keyed by canonical ID.
- First validation: `npm run typecheck && npm exec -- playwright test --config tests/electron-e20-playwright.config.ts --grep "E20|project-headings" --output /private/tmp/rhythm-project-headings-repair-evidence/green` => typecheck PASS, 20 passed/1 failed. Both UI repair behaviors passed, but strict network receipt caught two existing GET /global/health probes. One repair attempt: added that exact external health endpoint to the existing test response boundary (confirmed gateway/index.ts:188); no gateway/runtime changes or relaxed unexpected-request assertion.
- Phase 2 complete: `npm run typecheck && npm exec -- playwright test --config tests/electron-e20-playwright.config.ts --grep "E20|project-headings" --output /private/tmp/rhythm-project-headings-repair-evidence/green-final` => PASS typecheck and 21 tests (9.5s). Existing E54 test also matches E20 via its file name; no other test files/suites run. `git diff --check` PASS. Repair criteria c1/c2 now pass; original lifecycle evidence remains from the preceding slice, not rerun or requalified here.
- Screenshot reused existing c5 rail capture with Compact rows checked: `/private/tmp/rhythm-project-headings-repair-evidence/green-final/electron-e20-session-order-b07c8-etains-accessible-selection/project-headings.png`. Opened and visually reviewed: canonical P1/P2 headings distinguish groups; Closed, Starting, Ready to resume and Working are visible without preview/project row metadata. Existing E20-c3 capture also ran unchanged; no new screenshot harness.
- Scoped diff reviewed against initial file reads; only four owned files changed by this repair. Initial unrelated dirty-file inventory is preserved. No build/test suite expansion beyond sandbox bootstrap and assigned focused checks; no runtime diagnosis or candidate verification performed.
- Teardown command: `RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-project-headings-repair RHYTHM_SANDBOX_API_PORT=4298 RHYTHM_SANDBOX_ENGINE_PORT=4297 RHYTHM_SANDBOX_GATEWAY_PORT=4299 tools/dev/sandbox.sh status` then same variables with `tools/dev/sandbox.sh down` => PASS, same owned PIDs47889/47908, sandbox removed. Sanitized diagnostics: `/private/tmp/rhythm-project-headings-repair.evidence.Fz5PQA`. Test/screenshot evidence survives outside sandbox.
- READY_FOR_VERIFICATION: two P1 repairs complete; manager reviews four-file delta and screenshot. Original manual gates c6-c9 remain explicitly UNVERIFIED for manager review; independent runtime diagnosis and installed-app smoke remain outside this UI repair.

## Independent final gate — 2026-09-12

- Verdict: **FAIL — acceptance/accessibility evidence gaps, not a reproduced product regression.** This section supersedes earlier handoff/status claims. Branch `feature/electron-flutter-retirement`, HEAD `82d6b981aecf3eaffe63a5866472d3254da3081d`, with the owned uncommitted SessionRail/test slice. All unrelated concurrent Electron/runtime/design/evidence dirt was treated as declared and preserved.
- Contract parsed during review and every criterion was reconciled against the scoped diff and complete mapped specs. c6-c9 now record completed independent procedural review rather than stale UNVERIFIED flags. c4 is fail because its loading-state clause has no direct assertion of `Loading session history…` or `aria-busy`; existing successful tests assert empty state and expired-cursor reset but cannot catch a missing loading indicator. Do not weaken the criterion; the orchestrator should route the missing assertion to the test owner.
- UI reference applied. Existing c5 proves Enter/Space activation, retained programmatic focus, aria-expanded, independent collapse, and selected-row aria-current. It calls `.focus()` instead of navigating with Tab and does not inspect rendered focus styling. Keyboard reachability and visible-focus evidence required by the UI gate remains missing. Supplied UI re-review `5cf86e6b` PASS was acknowledged, but no fresh Tab/focus receipt exists in the mapped executable evidence.
- GitNexus manager MCP `detect_changes` with explicit worktree was unavailable in this session. Supplied LOW SessionRail impact (1 direct caller, 3 total, 0 processes) is provenance, not a fresh scope-check pass. No substitute local CLI was run.

### Executed commands and output

From worktree root, exported:
```sh
export RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a
export RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a/rhythm.db
export RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a/opencode.json
export RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-project-headings-final-verify
export RHYTHM_SANDBOX_API_PORT=4398 RHYTHM_SANDBOX_ENGINE_PORT=4397 RHYTHM_SANDBOX_GATEWAY_PORT=4399
tools/dev/sandbox.sh up
tools/dev/sandbox.sh status
```
Output: mandatory sandbox fork/API/MCP bootstrap succeeded; `Sandbox ready: http://127.0.0.1:4398 (engine :4397)`. Status: API50166/4398, engine50185/4397, gateway50166/4399. No separate web/package/full-repository build was run.

All web checks below ran from `apps/web` in this clean shell:
```sh
env -i HOME=/private/tmp/rhythm-project-headings-final-verify/home TMPDIR=/private/tmp/rhythm-project-headings-final-verify/tmp PATH=/private/tmp/rhythm-project-headings-final-verify/bin:/Users/ajhochhalter/.local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin PLAYWRIGHT_BROWSERS_PATH=/Users/ajhochhalter/Library/Caches/ms-playwright /bin/zsh -f -c '<command>'
```
- `git branch --show-current && git rev-parse HEAD`: branch/SHA above, captured with both test runs.
- `npm run typecheck`: `tsc -b`, exit0.
- `npm exec -- playwright test --config tests/electron-e20-playwright.config.ts --grep "E20|project-headings" --output /private/tmp/rhythm-project-headings-final-verify-evidence/e20`: **21 passed (9.6s)**. The boundary-mocked API4199/engine4197 URLs in this maintained config are intercepted, not the assigned real sandbox services. Renderer4186 is started and stopped by Playwright, reuse disabled.
- During that run, an independent Node fetch of `http://127.0.0.1:4186/src/components/SessionRail.tsx` required HTTP success plus both `projectNameCounts` and `collapsedProjects`: `STALE-SERVE CHECK: current projectNameCounts/collapsedProjects served, exit 0`, before Playwright's first test. This verifies the edited source, not stale dist.
- With `RHYTHM_E2E_PORT=4386 RHYTHM_DIST_PORT=4387` additionally supplied to the clean shell: `npm exec -- playwright test tests/sessions.spec.ts --output /private/tmp/rhythm-project-headings-final-verify-evidence/sessions`: **3 passed (5.0s)**.
- `git diff --check -- src/components/SessionRail.tsx tests/sessions.spec.ts tests/electron-e20-session-ordering.spec.ts ../../docs/ai/contracts/manual-smoke-project-headings-20260912.json ../../docs/ai/runs/2026-09-12-electron-project-session-headings.md`: exit0 before evidence reconciliation. No unrelated dirt checked or repaired.
- CI routing inspected: no web path-triggered static job found; Electron release workflow is manual-only. Full repo/package checks were excluded by this focused dispatch.
- Live E21 reconciliation **not run**: `electron-e21-playwright.config.ts:3-4` hardcodes4098/4097 and `electron-e21-live.spec.ts:9` hardcodes the HTTP client to4098. No supported environment override for assigned4398/4397. The dispatch makes this run conditional; prior 1/1 receipt is historical, not a fresh gate pass. No test/source rewrites, proxies, or contact with other sandboxes were used.
- Fresh screenshot opened and visually reviewed: `/private/tmp/rhythm-project-headings-final-verify-evidence/e20/electron-e20-session-order-b07c8-etains-accessible-selection/project-headings.png`. Nonblank rail with distinct Same label (P1)/(P2), counts, selected row, and visible Closed/Starting/Ready to resume/Working compact labels; project dropdown and nested Agents/status disclosures absent. This is the changed rail at the test's1280x1200 viewport, not a whole-page responsive or packaged-app qualification.
- Strong regression assertions: repair-c2 checks exact equal-name/equal-count p1/p2 visible labels, unique p3 label without suffix, and sort/collapse stability; c2/c3 assert No project membership and loaded-child count; E20-c9 requires visible and accessible root/child status while preview/project/parent metadata disappears. E20-c2/c3/c4/c7/c8 and lifecycle tests assert ordering, pagination, search ancestors, scopes/archive filtering, empty/error reset, and row-action results.
- Final owned HTTP health check: `/health`200 `{status:ok,service:rhythm-api-server}`; `/opencode/health`200 `{status:ready,message:Opencode SDK ready,bridgeLive:true,...}`; `Owned sandbox health check exit 0`.
- With assigned sandbox/port exports, `tools/dev/sandbox.sh status` again reported the same PIDs, then `tools/dev/sandbox.sh down` exited0: `Sandbox removed: /private/tmp/rhythm-project-headings-final-verify`. Sanitized diagnostics survive at `/private/tmp/rhythm-project-headings-final-verify.evidence.RYWpSX`; screenshots/results survive outside disposable sandbox.
- No failed executable stage, runtime repair, product-code edit, commit/push/PR/merge/peer dispatch, or live candidate/4001/4096 inspection/contact. Only this run note and its contract were edited by the gate. Missing loading/keyboard evidence and unavailable GitNexus scope receipt prevent overall PASS; orchestration owns routing.

## Evidence-only repair — loading and keyboard reachability

WAIVED: Test/evidence-only changes with no product behavior changes; verification is: focused project-heading E20 tests, web typecheck, and scoped diff checks.

- Phase 0 complete: acceptance-contract invoked first; explicit c4/c5 dispatch accepted. Read AGENTS, project-state, current-plan, testing-guide, full E20 spec and SessionRail, focused CSS/config and sandbox instructions. No product implementation or artificial failing assertion required for this non-behavioral evidence repair. Workflow-orchestrator is not exposed; this is an already delegated slice.
- Phase 1 complete: manager supplied fresh GitNexus detect_changes LOW, 14 symbols/12 indexed files, zero processes; SessionRail LOW, one direct caller/zero processes. Receipt is supplied provenance, not a locally rerun tool. No existing symbol or shared product path edited; only two new tests plus the owned contract/run note.
- Tests added: c4 holds the existing live-mode list HTTP boundary during refresh, directly asserts aria-busy=true and visible exact loading status, then releases and requires busy=false, no status/error, canonical heading/count and visible session. c5 clicks stable preceding Compact rows control then uses real keyboard Tab to each project heading, asserting focus, :focus-visible, expanded state and computed solid nontransparent outline at least 2px. Enter collapses each group to expose the next heading in tab order. Existing independent Enter/Space selection/collapse test is unchanged. E20 is boundary-fixtured renderer evidence, not a real-backend reconciliation claim.
- Validation pending. Owned runtime: /private/tmp/rhythm-project-headings-evidence-repair, API4698/engine4697/gateway4699. No product repair, peers or live4001/4096/candidate43749 contact authorized.

### Executed evidence-repair checks / handoff

The following supersedes the preceding pending line and resolves the two independent-gate evidence gaps; it does not claim a new full-suite or packaged-app gate.

From worktree root:
```sh
RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-e02-fixture-20260911-phase0-57ba71255a/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-project-headings-evidence-repair RHYTHM_SANDBOX_API_PORT=4698 RHYTHM_SANDBOX_ENGINE_PORT=4697 RHYTHM_SANDBOX_GATEWAY_PORT=4699 tools/dev/sandbox.sh up
RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-project-headings-evidence-repair RHYTHM_SANDBOX_API_PORT=4698 RHYTHM_SANDBOX_ENGINE_PORT=4697 RHYTHM_SANDBOX_GATEWAY_PORT=4699 tools/dev/sandbox.sh status
```
PASS: launcher bootstrap builds succeeded, initial readiness curl briefly refused then Sandbox ready; API53476/4698, engine53510/4697, gateway53476/4699. No separate build/full suite. E20 serves source at4186 (reuse disabled); its4199/4197 API/WebSocket boundaries are intercepted, not assigned real-backend qualification.

From `apps/web`:
```sh
env -i HOME=/private/tmp/rhythm-project-headings-evidence-repair/home TMPDIR=/private/tmp/rhythm-project-headings-evidence-repair/tmp PATH=/private/tmp/rhythm-project-headings-evidence-repair/bin:/Users/ajhochhalter/.local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin PLAYWRIGHT_BROWSERS_PATH=/Users/ajhochhalter/Library/Caches/ms-playwright /bin/zsh -f -c 'npm run typecheck && npm exec -- playwright test --config tests/electron-e20-playwright.config.ts --grep "project-headings" --output /private/tmp/rhythm-project-headings-evidence-repair-results/green'
```
- First run used output suffix `/e20`: typecheck PASS; 6 passed/1 failed (9.8s). c4 passed; c5 expected p1 first but newest-first fixture timestamp puts p2 first. One test-only repair: assert heading DOM text/count order and traverse p2 then p1. No product edits or relaxed focus assertion.
- Final exact command above: typecheck (`tsc -b`) PASS; **7 passed (6.8s)**. Both new tests and unchanged independent Enter/Space test passed. No second repair needed. Existing c5 screenshot generated at `/private/tmp/rhythm-project-headings-evidence-repair-results/green/electron-e20-session-order-b07c8-etains-accessible-selection/project-headings.png`; not newly visually reviewed. Computed outline evidence is executable, not inferred from this screenshot.
- Phase 2 complete: only `apps/web/tests/electron-e20-session-ordering.spec.ts`, the owned contract JSON, and this note edited. All earlier product/test/parallel dirt preserved. Original broader E20/lifecycle and manual-review receipts remain historical, not rerun here.
- Scoped check from worktree root: `git diff --check -- apps/web/tests/electron-e20-session-ordering.spec.ts docs/ai/contracts/manual-smoke-project-headings-20260912.json docs/ai/runs/2026-09-12-electron-project-session-headings.md` PASS (exit0). `git diff -- apps/web/tests/electron-e20-session-ordering.spec.ts` reviewed against the initial read: this repair adds only the two tests; the remaining displayed delta predates this dispatch. Contract/run are pre-existing untracked files, so Git's whitespace check does not cover their content; their edits were reviewed directly.
- Teardown: same status command again showed PIDs53476/53510, then identical variables with `tools/dev/sandbox.sh down` PASS; sandbox removed. Sanitized diagnostics `/private/tmp/rhythm-project-headings-evidence-repair.evidence.VuUxOv`; results survive outside runtime.
- No hardcoded4098 live test, live4001/4096/candidate43749 contact, product repair, commit/push/PR/merge, tracker publish or peers. READY_FOR_VERIFICATION for manager's scoped evidence review; no installed-app smoke claim.
