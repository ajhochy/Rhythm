---
date: 2026-09-24
repo: Rhythm
branch: codex/1569-accounts-wiring
pr: 1544
issues: [1569]
status: pending
tags: [run, rhythm]
---

## Files

Actual main auth envelope and Accounts IPC composition; view trusted host options/disposal barrier; frozen preload bridge; renderer type declaration `apps/web/src/ai-accounts.d.ts`; Electron test registration; exact security receipt and packaged capability allowlists. Prior S4 helpers unchanged in this step. Supplemental contract keeps original 28 criteria untouched.

## Checks

- Initial actual-wiring command `node --experimental-vm-modules --test --test-concurrency=1 apps/electron/test/hermes-accounts-wiring-contract.test.mjs`: 1 passing control, 7 expected failures; `/private/tmp/rhythm-s4-actual-wiring-red.log`.
- Reviewed follow-up REDs: canonical host home (`/private/tmp/rhythm-s4-wiring-home-red.log`); failed reload disposal (`/private/tmp/rhythm-s4-wiring-reload-red.log`); exact Accounts security receipt (`/private/tmp/rhythm-s4-wiring-receipt-red.log`).
- Final same wiring command: 9/9 pass; `/private/tmp/rhythm-s4-wiring-final-focused.log`.
- Final `cd apps/electron && npm test`: 263/263 pass, exit 0; `/private/tmp/rhythm-s4-wiring-electron-final.log`. Includes real disposable Electron shell fixtures, existing auth/relay/signer/view regressions, S1/S2/helper contracts and security receipt.
- `cd apps/electron && npm run typecheck`: exit 0; `/private/tmp/rhythm-s4-wiring-typecheck-final.log`.
- `node --check apps/electron/test/electron-unsigned-package.test.mjs`: exit 0. Actual packaged binary test requires a rebuilt bundle and was NOT run; no package rebuild authorized for this bounded slice.
- `git diff --check`: clean.
- Screenshot generated by existing Electron suite copied to `/private/tmp/rhythm-s4-wiring-shell.png`; tracked artifact restored.

## Notes

GitNexus impact: invalidateAuthentication two direct callers LOW; persist/restoreAuthentication and validateSecuritySmokeReceipt one direct caller LOW; no indexed view symbols, UNKNOWN. Index warns candidate is 200 commits ahead. Auth/view ownership treated HIGH risk explicitly before edits.

Main derives OS home from native userInfo, canonicalizes default .hermes, and passes the same home to fork host and broker. Existing offline encrypted session policy is reused; stable Accounts generation enriches auth-session.bin. No extra login/network requirement or credential store. Signed-out/missing-home ordinary Hermes attachment preserved. Child application state changes only from synchronous actual-owned receipts.

Failed reload disposal now remains a rejecting ownership barrier for subsequent logout/server change/attach. Event handlers observe errors without converting the barrier to success. Session-specific guards remain installed if child stop fails. A failed identity transition leaves sharing unavailable and refuses a new login.

Fixture corrections retained assertion strength: canonicalize macOS /var temp path before identity comparison; compare empty cross-VM environments by exact own-key list. Native fork real hermes:connection/owned spawn and inherited environment sanitation are companion work, not proved by this VM boundary. Accounts UI, installed/combined smoke and complete shared-agent feature remain outstanding. No real credentials/providers, API/engine servers, commits or pushes.

### Final parent review repair

One representative pending-attachment ownership test (S4-I10) reproduced a lost stop failure before active host publication. All owned-host teardown sites now call stopOwnedHost, which retains failure as a future disposal/attach barrier; no remaining direct dispose swallow sites. GitNexus stopOwnedHost UNKNOWN in stale index. RED `/private/tmp/rhythm-s4-wiring-pending-red.log`. Final bounded command `node --experimental-vm-modules --test --test-concurrency=1 apps/electron/test/hermes-accounts-wiring-contract.test.mjs apps/electron/test/hermes-view.test.mjs apps/electron/test/e12a-auth-boundary.test.mjs`: 37/37 pass, including 10 actual-wiring cases; `/private/tmp/rhythm-s4-wiring-pending-focused.log`. Typecheck repeated after this repair: exit 0. The 263-test aggregate above predates this final teardown repair; parent requested focused replay instead of another aggregate run.

## Parent integration

Parent read all product changes and exact capability allowlist updates, then copied13 slice-owned files. Integrated wiring/view/auth/security receipt replay passed40 tests; Electron typecheck passed. Native fork startup/pool environment and receipt hooks remain separate pending source work, and the artifact pin has not yet changed.

Parent staged GitNexus reports13 files/13 symbols, LOW, no processes. Its stale line map attributes some changed ranges to unrelated symbols; exact staged product diff review remains authoritative for scope.
