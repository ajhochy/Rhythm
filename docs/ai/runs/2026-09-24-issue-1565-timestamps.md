---
date: 2026-09-24
repo: Rhythm
branch: swarm/issue-1565
pr: null
issues: [1565]
status: blocked
tags: [run, Rhythm]
---

## Files

- Contract: `docs/ai/contracts/issue-1565.json`, `apps/web/tests/contract/issue-1565.test.mjs`.
- Durable acceptance source: `docs/ai/plans/2026-09-24-issue-1565-timestamps.md` (preexisting untracked plan, left untouched).

## Checks

- Repair attempt 1 Phase 0: `node --test tests/contract/issue-1565*.test.mjs` in `apps/web` after adding wall-clock, Review Queue and guard regression assertions, before implementation: **FAIL 6 pass / 3 fail** (missing `wallClockInstant`, raw Review Queue timestamps, guard misses expiry/method/ternary). Worktree `/private/tmp/rhythm-swarm-1565`, branch `swarm/issue-1565`, existing dirty files preserved. Opus triage `9acd6c94-f259-49f7-b7ab-6a5d4dc81b22`; R1–R7 per manager dispatch.

- `pwd && git branch --show-current && git status --short && git rev-parse --show-toplevel`: `/private/tmp/rhythm-swarm-1565`, `swarm/issue-1565`, only untracked durable plan.
- Phase 0 `node --test tests/contract/issue-1565.test.mjs` in `apps/web`: **FAIL** 0 pass / 4 assertion failures: missing shared formatter, missing component, Transcript lacks Timestamp, missing AST guard. Tests written before product implementation.
- GitNexus repo `Rhythm`, upstream depth 2: `Transcript`, `ToolWorkspace`, `LiveArtifactsShell`, `MessagesPage`, `LiveMessagesPage`, `timeLabel` (live), `FacilitiesPage`, `LiveFacilitiesPage`, `MobileAccessPage`, `IntegrationsPage`, `AutomationsPage`: each **LOW**, one direct caller, zero indexed affected processes; blast radius 2 symbols per target, one or two modules. No HIGH/CRITICAL approval gate triggered. Manager dispatch records standing approval for #1565 only.
- Further symbol impacts: `LiveSchedulesTool`, `LiveReviewTool`, `formatDate`, `dateTimeLabel`, `displayTime`, fixture `timeLabel`: **LOW**, 1–2 direct callers, zero affected processes. `LiveEmailTool` not indexed under that name; outer `ToolWorkspace` impact above applies.
- `npm ci --no-audit --no-fund` in `apps/web`: installed 80 locked packages, no dependency file changes.
- `node --test tests/contract/issue-1565*.test.mjs` in `apps/web`: **PASS 6/6**, including host LA, DST spring/fall, Kolkata/New York, strict invalid matrix and AST positive/negative fixtures.
- `npm run typecheck`: **PASS**. `npm run build`: **PASS**, 1697 modules; existing >500 kB chunk warning. `npm run test:dist-smoke`: **PASS**, index and two relative assets verified.
- `RHYTHM_E2E_PORT=6381 RHYTHM_DIST_PORT=6382 npm exec -- playwright test tests/facilities-smoke-response.spec.ts tests/secondary-tool-paths.spec.ts --workers=1`: **PASS 4/4**. Initial unsandboxed browser attempt without port overrides rejected because :4173 occupied; no process was touched.
- `RHYTHM_E2E_PORT=6383 RHYTHM_DIST_PORT=6384 npm exec -- playwright test tests/timestamp-1565.spec.ts --workers=1`: **FAIL** twice (repair cap). First attempt selected no thread, so Messages had zero rendered `<time>` elements; second selected the fixture Messages thread and captured earlier surfaces, then waited 90 s for nonexistent Facilities option `Leadership huddle` (actual seed is `Leadership sync` per error-context). Screenshot/trace from failed run: `apps/web/test-results/timestamp-1565-issue-1565--9a7bc-without-horizontal-overflow/`. Browser qualification remains red; no third repair attempt. Locale `en-US` and timezone `America/Los_Angeles` are pinned in `apps/web/playwright.config.ts`, inherited by this spec; only loopback requests are allowed via `openFixture`/`openPage`.
- `node tests/timestamp-source-guard.mjs src/components/Transcript.tsx src/components/ToolWorkspace.tsx src/pages/messages/index.tsx src/pages/messages/live.tsx src/pages/facilities/index.tsx src/pages/facilities/live.tsx src/pages/mobile-access/index.tsx src/pages/integrations/index.tsx src/pages/automations/index.tsx src/pages/dashboard/LiveArtifactsShell.tsx`: **FAIL**, owned Review Queue raw `selected.createdAt ?? 'Unknown'` and `selected.updatedAt ?? 'Unknown'` remain. Explicit non-deferred ledger at `apps/web/tests/timestamp-outstanding-1565.json`. Not a passing guard.
- `git diff --check`: **PASS**. GitNexus `detect_changes({scope:'all',worktree:'/private/tmp/rhythm-swarm-1565',repo:'Rhythm'})`: 10 tracked product files / 14 touched indexed symbols, zero affected indexed processes, risk low. New files are untracked and are not represented in the graph diff count.

## Notes

- Repair attempt 1 Phase 1: GitNexus `impact` upstream depth 2 in indexed `Rhythm` for `LiveFacilitiesPage`, `FacilitiesPage`, `displayTime`, `LiveReviewTool`, `Transcript`, `ToolWorkspace`: all LOW, 1 direct caller each, 0 affected processes, 2 transitive symbols each. Standing manager approval for HIGH `LiveFacilitiesPage` noted; current indexed impact LOW; no expanded HIGH scope. `detect_changes({scope:'all',worktree:'/private/tmp/rhythm-swarm-1565',repo:'Rhythm'})`: risk low, 11 tracked files, 15 touched indexed symbols, 0 affected processes (untracked files not indexed).
- Repair attempt 1 Phase 2: wall-clock reservation helper validates original local date/time with rollover checks, zoned values retain their instant; only Facilities displays consume it. Review Queue selected Created/Updated use `<Timestamp>` or `Unknown`; child Transcript migration/reindent undone. Exact Inspector ledger retained, Review Queue nondeferred ledger cleared. `node --test tests/contract/issue-1565*.test.mjs`: **PASS 9/9** after initial 6/3 red; `npm run typecheck`: **PASS** (first run had `second` default string TS error, fixed and rerun); parser guard command from prior Checks with Inspector and AgentSettings appended: **PASS**; `npm run build` **PASS** 1697 modules (preexisting chunk warning); `npm run test:dist-smoke` **PASS**; `git diff --check` **PASS**.
- `RHYTHM_E2E_PORT=6383 RHYTHM_DIST_PORT=6384 npm exec -- playwright test tests/timestamp-1565.spec.ts tests/facilities-smoke-response.spec.ts tests/secondary-tool-paths.spec.ts --workers=1`: **PASS 5/5**. `RHYTHM_E2E_PORT=6383 RHYTHM_DIST_PORT=6384 npm exec -- playwright test tests/timestamp-1565.spec.ts --workers=1`: **PASS 2/2**, screenshots in Playwright output for eight fixtures plus Kolkata Facilities. Corrected selector `Leadership sync`, no `Time unavailable` in asserted populated surfaces.
- **BLOCKED for full qualification, not READY_FOR_VERIFICATION.** Approved fixture env variables remain unset (`printenv RHYTHM_APPROVED_FIXTURE_ROOT RHYTHM_LIVE_DB_PATH RHYTHM_SANDBOX_OPENCODE_CONFIG RHYTHM_SANDBOX_DIR RHYTHM_LIVE_E2E` emitted nothing). No isolated sandbox startup, gateway populated/empty shape evidence, or real sandbox browser rendering; isolated :6398/:6397/:6399 config/spec added fail-closed but **not run**. Stubbed-live cross-domain shapes, epoch screen, Flutter shape and Astra UI review remain unverified. AgentSettings/Inspector/reset/manual native hover remain outstanding/not_tested. No commit/push/merge/stash/clean.

- Repair attempt 1 qualification snapshot (superseded by the later repair sections): no approved fixture variables were configured, so no 6398/6397/6399 sandbox, gateway-shape probe, forced-local-origin assertion, screenshots, second-timezone pass, or Astra review ran in that attempt. Native title hover remained manual.
- Repair attempt 1 code snapshot (superseded by the later repair sections): Review Queue and Inspector were still outstanding at that point. `issue-1565-c3` and `issue-1565-c4` remained UNVERIFIED/manual; no backend/store/gateway/date-only/editor-input, AgentSettingsTool, usage-reset, project-state/current-plan, dependency, or git-state mutation occurred.

## Final repair attempt 2 — GPT-6 Sol (Opus triage b6b81672-a0ff-4e58-ba76-fa7a254e514e)

- Phase 0: `/private/tmp/rhythm-swarm-1565` on `swarm/issue-1565`, existing dirty work preserved. New `issue-1565-c2` fallback CSS-hook regression test added **before** product changes. `cd apps/web && node --test tests/contract/issue-1565*.test.mjs`: **FAIL 9 pass / 1 fail**, `fallback span must expose a dedicated styling hook` (`Timestamp.tsx` still rendered plain `<span>`).
- Phase 1: indexed `Rhythm` GitNexus upstream depth 2 for `Transcript`, `MessagesPage`, `LiveMessagesPage`: **LOW** each, 1 direct caller each (AgentsWorkspace, App, MessagesPage respectively), zero affected processes, 2 transitive symbols. No HIGH/CRITICAL scope expanded. Ports :6598/:6597/:6599/:4175 verified free with `lsof` before any startup; no occupied process touched. API/fork deps absent in this worktree initially.
- Phase 2: added non-time `data-timestamp-fallback` hook with scoped 8px/muted mono fallback CSS in Transcript and Messages; preserved real `<time>`/title/screen-reader rendering. Live-only config now requires `RHYTHM_LIVE_E2E=1` and exact :6598/:6597 URLs, web :4175 `--strictPort`, synthetic fixture token, loopback production API, pinned en-US/Los_Angeles and separate temp output. Default Playwright ignore is the single #1565 live spec, not all live specs. Live spec uses the real API to create a message thread and messages, modifies only the sandbox runtime DB copy to expose a Z instant and epoch sentinel, asserts raw API values and rendered semantics/fallback in LA and Kolkata, with blocked external origins, critical axe checks, overflow and screenshots. **Live spec is unexecuted; these are contract assertions, not live evidence.**
- `cd apps/web && node --test tests/contract/issue-1565*.test.mjs && npm run typecheck && npm run build && npm run test:dist-smoke`: **PASS 10/10, typecheck, 1697-module build (pre-existing chunk warning), dist smoke**. `RHYTHM_E2E_PORT=6383 RHYTHM_DIST_PORT=6384 npm exec -- playwright test tests/timestamp-1565.spec.ts tests/facilities-smoke-response.spec.ts tests/secondary-tool-paths.spec.ts --workers=1`: **PASS 6/6**, fixture screenshots in Playwright output. `RHYTHM_LIVE_E2E=1 RHYTHM_LIVE_API_URL=http://127.0.0.1:6598 RHYTHM_LIVE_ENGINE_URL=http://127.0.0.1:6597 RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-1565-runtime npm exec -- playwright test --config tests/timestamp-1565-live-playwright.config.ts --list`: **PASS**, 2 discovered live tests; no server started.
- `cd apps/api_server && npm ci --offline --no-audit --no-fund && npm run build`: **PASS**; no lock/manifest diff. `cd apps/opencode_fork/packages/opencode && bun run build --single`: **FAIL** `preload not found "@opentui/solid/preload"`. `cd apps/opencode_fork && bun install --frozen-lockfile --registry http://127.0.0.1:9`: **FAIL**, many uncached packages including `@opentui/solid` / `@playwright/test`, loopback registry ConnectionRefused. No external registry contacted, lockfile unchanged. `node tools/dev/sandbox_fixture.mjs /private/tmp/rhythm-1565-synthetic-final-20260924`: **PASS**, fresh synthetic 0400 DB/config; generator's MCP URL remains :4098 because the 0400 config cannot be edited with the available patch operation (`PermissionDenied`). Fixture was not passed to a sandbox. Since fork cannot build offline and fixture config cannot be safely rewritten, **no sandbox up/status/down or live browser execution occurred**; no real API evidence, no stubbed-live cross-domain evidence. Do not count `--list` as a live pass.
- First guard call included a nonexistent `src/components/AgentSettingsTool.tsx` and failed ENOENT; corrected to `src/components/tools/AgentSettingsTool.tsx`. Corrected `node tests/timestamp-source-guard.mjs src/components/Transcript.tsx src/components/ToolWorkspace.tsx src/pages/messages/index.tsx src/pages/messages/live.tsx src/pages/facilities/index.tsx src/pages/facilities/live.tsx src/pages/mobile-access/index.tsx src/pages/integrations/index.tsx src/pages/automations/index.tsx src/pages/dashboard/LiveArtifactsShell.tsx src/components/tools/AgentSettingsTool.tsx src/components/Inspector.tsx`: **PASS**, deferred ledger preserved. `git diff --check`: **PASS**. GitNexus `detect_changes({scope:'all',worktree:'/private/tmp/rhythm-swarm-1565',repo:'Rhythm'})`: low, 13 tracked files/15 touched indexed symbols, zero affected processes; untracked test/new product files not represented by graph diff. No dependency manifest or lock diff; no commit/push/merge/stash/clean.
- **BLOCKED**: #1565 c3 and c4 remain UNVERIFIED without populated/empty real sandbox and stubbed-live cross-domain proof. Flutter zoneless/Z, facilities and other real API shapes, epoch screen, screenshot/axe live evidence not established. Native hover, AgentSettings, Inspector, reset remain not_tested. #1582 must wait; Astra terminal UI review next. No READY_FOR_VERIFICATION claim.

## Repair 3 (2026-09-24, AJ-revalidated)

### Files

- `apps/web/src/components/Inspector.tsx`: replaced the hard-coded/sliced session clocks and raw share expiry with the shared `Timestamp` component.
- `apps/web/src/components/Transcript.css`: restored valid message timestamps to 11px while retaining the 8px fallback hook.
- `apps/web/src/pages/planner/index.tsx`: documented the date-only UTC pins and made both `LiveTaskCard` instant formatters explicitly 12-hour. `LiveTaskCard` is in this file at the live planner implementation; there is no separate component file.
- `apps/web/tests/timestamp-source-guard.mjs`, `apps/web/tests/timestamp-outstanding-1565.json`, `apps/web/tests/contract/issue-1565-guard.test.mjs`: fixed mixed-expression traversal, made stale exemptions independent failures, removed the repaired Inspector exemptions, and added an actual repo-wide `src` scan.
- `apps/web/tests/contract/issue-1565.test.mjs`, `apps/web/tests/timestamp-1565.spec.ts`: added stricter Inspector/CSS/planner contracts plus exact per-surface Los Angeles labels and an exact Kolkata label. Existing assertions were not weakened or skipped.
- `docs/ai/contracts/issue-1565.json`: retained c3/c4 as manual `UNVERIFIED` and corrected the evidence boundary.

### Checks

- Launch discipline: `pwd && git rev-parse --abbrev-ref HEAD && git rev-parse --short HEAD` → `/private/tmp/rhythm-swarm-1565`, `swarm/issue-1565`, `18f39c11`.
- GitNexus CLI upstream depth 2: `ContextPanel`, `SharePanel`, `Inspector`, `LiveTaskCard`, and `PlannerPage` were LOW risk with one direct caller and zero affected indexed processes. The CLI warned that the `/Users/ajhochhalter/Documents/Rhythm` index is 169 commits behind this sibling checkout; `rawTimestampDisplays` was not indexed.
- Initial `node --test tests/contract/issue-1565.test.mjs tests/contract/issue-1565-guard.test.mjs`: **FAIL 14 pass / 1 fail**. The new repo-wide scan exposed condition-only false positives. Failure triage isolated traversal of ternary conditions and the guard side of `&&`; the guard now skips those non-rendered positions while still rejecting `timestamp || <Fallback />`.
- Final `node --test tests/contract/issue-1565.test.mjs tests/contract/issue-1565-guard.test.mjs`: **PASS 15/15**, 0 skipped/todo.
- `npm run typecheck`: **PASS**, exit 0.
- `npx playwright test tests/timestamp-1565.spec.ts --workers=1`: **NOT RUN past server startup**, exit 1: `Error: listen EPERM: operation not permitted 127.0.0.1:4173`; Playwright reported `Process from config.webServer was not able to start. Exit code: 1`. No screenshots or browser assertions ran.

### Qualification boundary

- `issue-1565-c3` and `issue-1565-c4` remain manual **UNVERIFIED**. No API server, opencode engine, Flutter, Electron, live test, sanitized sandbox, packaged app, or manual/native hover check ran. No commit, push, stash, reset, checkout, package installation, or dependency change was performed.
