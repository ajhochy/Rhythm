---
date: 2026-07-28
repo: Rhythm
branch: mega/memory-1218-1215
pr: null
issues: [1218, 1215]
status: blocked
tags: [run, Rhythm]
---

# Memory retrieval and consolidation prompt

## Files

- Added executable acceptance contracts for #1218 and #1215.
- Classified vault paths under `synthesis/` as `kind: synthesis` without
  rewriting source notes.
- Prioritized non-synthesis memories in FTS prompt retrieval and hybrid RRF
  fusion while retaining synthesis rows for explicit search.
- Added a v2 managed Memory Consolidation prompt, removed the incompatible
  file-memory skill, and added an exact-fingerprint `runOnce` migration.
- Added an env-gated live sandbox test for retrieval ranking, classification,
  provenance, fresh prompt seeding, migrated prompt repair, and a supported
  memory write/search outcome.

## Checks

### Failing-first proof

`cd apps/api_server && npx vitest run src/contract/issue_1218_curated_vs_synthesis.test.ts`

- FAIL as expected: 3 failed, 4 passed.
- Observable failures: synthesis remained `kind: fact`; synthesis ranked before
  the concise curated memory.

`cd apps/api_server && npx vitest run src/contract/issue_1215_consolidation_prompt.test.ts`

- FAIL as expected: 3 failed, 3 passed.
- Observable failures: `anthropic-skills:consolidate-memory` remained in
  `allowed_skills_json`; no versioned repair occurred.

### Passing targeted verification

`cd apps/api_server && npx vitest run src/contract/issue_1218_curated_vs_synthesis.test.ts src/__tests__/memory_retrieval_ranking_smoke.test.ts src/__tests__/memory_vault_sync.test.ts src/__tests__/memory_index_rebuild.test.ts`

- PASS: 4 files, 42 tests.

`cd apps/api_server && npx vitest run src/contract/issue_1215_consolidation_prompt.test.ts src/__tests__/memory_consolidation_seed.test.ts src/__tests__/seed_once_tombstone.test.ts src/__tests__/migrations_replay_guard.test.ts`

- PASS: 4 files, 17 tests.

Combined targeted command including the env-gated live file:

- PASS: 8 files, 59 tests.
- SKIP: 1 live file, 2 tests (correct without `RHYTHM_LIVE_E2E=1`).

`cd apps/api_server && node_modules/.bin/tsc --noEmit`

- PASS.

`cd apps/api_server && npm run build`

- PASS.

### Environment-blocked required gates

`ai-workflow checks --level issue`

- API TypeScript: PASS.
- MCP TypeScript: PASS.
- Flutter analyze/format: BLOCKED before analysis because the managed sandbox
  denies the external Flutter SDK write to
  `/Users/ajhochhalter/development/flutter/bin/cache/engine.stamp`.

`cd apps/api_server && npm test`

- BLOCKED by the managed sandbox denying loopback listeners. Representative
  pre-existing failures: `issue_1170_mobile_realtime_proxy.test.ts`,
  `engraph_manager.test.ts`, `research_vault_notes.test.ts`, and
  `agents_ws_e2e.test.ts`, all with `listen EPERM`. The run was stopped after
  the common environment failure was established.

## Live evidence

Synthetic inputs only:

- Source DB:
  `/private/tmp/rhythm-memory-1218-1215-source/fresh.db`
- Sandbox:
  `/private/tmp/rhythm-memory-1218-1215-fresh`
- Temp HOME:
  `/private/tmp/rhythm-memory-1218-1215-home`
- API/engine ports: `4115` / `4116`
- Vault: sandbox-local `vault/`

The documented launcher rebuilt the worktree fork and API successfully using a
local models snapshot and local dependency cache. API startup applied migrations
and logged:

- `Database initialized`
- `[AgentMemory] Seeded memory consolidation scheduled task`
- `[MemoryIndex] rebuild: cleared=0 indexed=0`

The live HTTP gate could not run because Node failed at the operating-system
boundary:

```text
Error: listen EPERM: operation not permitted 127.0.0.1:4115
```

The sandbox was stopped and removed. `lsof` confirmed no listener on `4115` or
`4116`. No production URL, user database, user Memory Vault, or ports 4000/4001
were accessed.

## Notes

- GitNexus MCP tools were not exposed in this session. The parent
  `.gitnexus/run.cjs` helper launches only the analyzer CLI and returned no
  impact/detect graph output for these commands. Static caller tracing was used
  and the high blast radius of `runMigrations` was explicitly flagged before
  editing.
- No commit was created because the user-defined live verification hard gate
  did not pass.
