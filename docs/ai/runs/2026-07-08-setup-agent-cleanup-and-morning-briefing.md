---
date: 2026-07-08
repo: Rhythm
branch: n/a (config/admin work, no source changes)
pr: n/a
issues: [945, 946, 947]
status: done
tags: [run, Rhythm]
---

## Files
- `~/.config/opencode/rhythm-managed-skills/daily-morning-briefing/SKILL.md` — merged AJ's newly-pasted newsletter/News-card workflow into the existing (more detailed) PCO team-filtered version.
- No source files changed — issue #945 filed for a future dev session instead of touching `apps/api_server` source.

## Checks
- `curl -s http://localhost:4001/agent-configs` — full audit of all 46 rows against `~/.config/opencode/agents/`: 0 orphans, 0 duplicate labels (issue #900 pattern absent).
- `cd apps/api_server && npm run doctor` — 40 passed / 4 attention (Python 3.9.6, Canva/Notion/Supabase MCP 401s) — unchanged from prior session, left untouched per AJ's standing decision.
- Verified both deletions via `GET /agent-configs/<id>` → 404, and confirmed `.md` files were removed from `~/.config/opencode/agents/`.

## Notes
- Ran full config-doctor diagnostic pass (rule 1 fires on first message of a new conversation). Confirmed no new breakage since the prior session's cleanup.
- Deleted duplicate/legacy agent profile `rhythm-setup` ("Rhythm Setup" v1) via `DELETE /agent-configs/rhythm-setup` — "Rhythm Setup Agent v2" (`1dd5f2e3-3267-4d91-960a-80a4e2ac6b09`) is now the only setup agent.
- Deleted leftover test profile `_test-skill-verification-ffb-toolbox` (id `a3b6055c-5893-4f4f-ac5b-4a0fc91d518a`) — its own systemPrompt said it was safe to delete.
- Investigated *why* new agent profiles get UUID ids instead of friendly slugs: `POST /agent-configs` never accepts a client-supplied `id`; `AgentConfigsRepository.insert()` always does `config.id ?? crypto.randomUUID()`. Friendly ids only exist on rows hand-seeded via migration. Filed **issue #945** (source-code fix, not implemented here — no code/research performed per AJ's request, issue-only).
- Filed #945 via `gh issue create` directly (Rhythm's native `rhythm_create_issue` tool is still broken — missing `GITHUB_TOKEN` server-side, tracked as #944).
- Rewrote `daily-morning-briefing` skill: AJ supplied a newer version of the workflow text that added a full Newsletter/News-card pipeline (4 parallel newsletter searches, classification step, dashboard `news` schema, chat-report News section, oversized-HTML error handling). The existing file already had more precise PCO team-ID filtering and team-exclusion logic than AJ's pasted text — merged rather than overwrote, keeping the team-ID specifics and layering in the new newsletter capability. Also deliberately dropped one instruction from AJ's pasted text (`mcp__cowork__update_artifact` for the dashboard write) since that tool doesn't exist in Rhythm's toolset — kept the file's existing direct-file-edit approach instead. Flagged both judgment calls to AJ in-chat.
- `secretary` agent already had `daily-morning-briefing` in its `allowedSkillsJson` toolkit from a prior session — no agent patch needed this time.
- Swept all skills for other `mcp__cowork__update_artifact` calls (the same bug just fixed in `daily-morning-briefing`). Found and fixed 3 more real instances: `rhythm-managed-skills/ffb-daily-dashboard-update/SKILL.md`, `~/.config/opencode/skills/ffb-daily-dashboard-update/SKILL.md`, `~/.config/opencode/skills/daily-morning-briefing/SKILL.md` — all now write the artifact HTML directly instead of calling the nonexistent tool. (Two other "cowork" hits in `multirepo-orchestrator` copies were false positives — an existing warning telling agents *not* to use `mcp__cowork__*`, left untouched.)
- **Discovered a real risk**: `ffb-daily-dashboard-update` and `daily-morning-briefing` each exist as two separate, out-of-sync files — one under `~/.config/opencode/skills/`, one under `~/.config/opencode/rhythm-managed-skills/` — and opencode's skill resolution didn't consistently pick the same directory for both names at session start. Filed Rhythm task `8a83e397-f0bd-4b24-ba3f-e4ecc93cec1d` to investigate resolution order and de-duplicate rather than guessing further mid-session.
- **Root-caused the skill-resolution inconsistency** via direct source reading of `apps/opencode_fork` (gitnexus MCP tools were not in Config Doctor's scope — `allowedMcpsJson` is `["rhythm"]` only, and MCP grants are fixed per-session, so this was flagged rather than self-granted; validated instead via direct file/git-history reading, which the repo's own `docs/ai/repo-map.md`/`architecture.md` don't cover — a documentation gap). Root cause: `skill/index.ts` `discoverSkills`/`loadSkills` merges `SKILL.md` matches from every discovered source (`.claude`, `.agents`, opencode's built-in `~/.config/opencode/{skill,skills}` default, and `config.skills.paths`) into one flat `name:`-keyed map, processed with `Effect.forEach(..., { concurrency: "unbounded" })` — on a name collision, whichever file's async parse finishes last silently overwrites the other (only a `log.warn`, no deterministic precedence). Confirmed via git log the race itself is inherited from upstream `sst/opencode` v1.14.49, not Rhythm-introduced.
- Found `docs/ai/decisions/2026-06-28-unify-skills-source-of-truth.md` ("Unify-2") — confirms `~/.config/opencode/skills` is a documented `ai-workflow sync-globals` target (previously only knew `.claude/skills`/`.codex/skills`), and that Rhythm's managed-dir registration was deliberately additive (never relocates/removes existing skills) specifically to avoid clobbering that sync target. The doc's own "Guards" section promises a "no-skill-lost discovery-count/name check" that could not be found implemented anywhere in `apps/api_server/src/__tests__` or the fork's `test/skill/` suite.
- Diffed all 33 skill names duplicated between `~/.config/opencode/skills/` and `rhythm-managed-skills/`: 5 identical, 10 cosmetic-only (YAML quoting), 3 trivial wording diffs, and ~19 dev-workflow + ~14 ministry/FFB names with real content divergence. Flagged `workflow-orchestrator` as most severe (29 lines in rhythm-managed-skills vs 1020 in the sync-globals copy).
- **Correction from AJ**: the dev-workflow skill duplication in `rhythm-managed-skills` is intentional, not accidental drift — AJ wants those as independently-evolving forks so Rhythm's self-improvement loop (Org Optimizer / prompt-evolver) can work on them, and they were deliberately re-scoped for a clean Agent/Skill separation. My initial recommendation to delete the `rhythm-managed-skills` copies was wrong and retracted.
- Re-diagnosed with that correction: the actual problem isn't duplicate files, it's that opencode's flat `name:`-keyed skill map can't represent "two intentionally independent forks of the same name" — they just race for the same slot, meaning self-improvement-loop edits to a Rhythm-managed dev-workflow skill have a non-deterministic chance of never loading. Proposed fix direction: rename the Rhythm-managed forks to a distinct `name:` (e.g. `rhythm-*` prefix) so they stop colliding, and update every agent profile's `allowedSkillsJson` that references the old names (enumerated: `config-doctor`, `workflow-orchestrator`, `coding-agent`, `failure-triage`, `issue-writer`, `librarian`, `planning-agent`, `project-state-updater`, `smoke-test-writer`, `verification-gate`, `workflow-retrospective`, `AI-Trend-Researcher`, `Theological-Researcher`). Not yet executed — awaiting AJ's confirmation on naming convention and whether `obsidian-*`/`defuddle`/`json-canvas` are meant to be independent forks too.
- Filed **issue #946** (source-code fix for the underlying race condition — not implemented here, per scope: exploratory/diagnostic only).
- **Resolved the FFB/ministry duplicate question.** AJ confirmed the `rhythm-managed-skills` copies with complete content were authored today/recently by direct work. Verified via mtime: every `~/.config/opencode/skills/` copy in this bucket (except the 2 touched earlier today) shared the identical timestamp `2026-06-25 14:48`, and each directory contained a `HERMES_MIGRATION.md` pointing to `~/Library/Application Support/Claude/local-agent-mode-sessions/skills-plugin/.../skills/<name>` — confirming these were a one-time bulk import from a prior system ("Hermes"), not an actively-maintained directory. Cross-checked the same pattern against the 19 dev-workflow names: no `HERMES_MIGRATION.md`, and mtimes spread across many distinct dates (May 13 – June 23) — consistent with an actively-synced directory (`ai-workflow sync-globals`), not a one-time snapshot. This confirms the two buckets need genuinely different treatment, as suspected.
  - **Deleted** the 11 confirmed-stale Hermes-migration directories from `~/.config/opencode/skills/`: `ffb-dynasty`, `ffb-podcast-vibes`, `ffb-roster`, `ffb-tff-vibes`, `ffb-trades`, `ffb-tuesday-refresh`, `ffb-daily-dashboard-update`, `monday-worship-planning`, `pco-song-usage-sync`, `ableton-setlist-build`, `daily-morning-briefing`. `rhythm-managed-skills` is now the sole copy for all 11.
  - **Left alone** (still genuine 10-line stubs in `rhythm-managed-skills`, not yet implemented, same June-28 stub-creation batch as the others): `monday-weekly-plan`, `monthly-gc-report`, `daily-email-triage`. Their `~/.config/opencode/skills` counterparts (58-71 lines) may hold real content worth reviewing/merging in — same treatment `daily-morning-briefing` got — not yet done.
  - Note: no public Rhythm endpoint exists to force-reload engine skill discovery for non-managed directories (`POST /skill/reload` is only called internally by api_server after its own `rhythm-managed-skills` writes) — this session and any other already-running instance won't see the deletions until their next fresh discovery scan; new sessions will.
  - Marked Rhythm task `8a83e397-f0bd-4b24-ba3f-e4ecc93cec1d` done.
- Still open: the dev-workflow rename fix (from the previous exchange) — naming convention TBD, plus whether `obsidian-bases`/`obsidian-cli`/`obsidian-markdown`/`defuddle`/`json-canvas` need the same treatment. Not addressed this pass.
- Still open: implement real content for `monday-weekly-plan`, `monthly-gc-report`, `daily-email-triage` (genuine stubs, not yet done).
- **Architecture decision (AJ-driven, recorded as #947).** Through conversation, AJ reframed the whole skill-management model: eliminate the `rhythm-managed-skills` directory entirely and have Rhythm manage `~/.config/opencode/skills` directly (the dir the engine already scans by default), while `ai-workflow sync-globals` (agent-stack) stops targeting it. One directory, owned by Rhythm, scanned by opencode. This dissolves the constraint the original "Unify-2" decision worked around (sync-globals clobbering) and makes the #946 race unreachable instead of just patching it. Key reframes AJ provided that corrected my initial framing: (a) "the dev-workflow syncing should not write into opencode config anymore" — the conflict source is agent-stack, not Rhythm; (b) "I don't want to just automatically pull all skills from global scans into rhythm" — Rhythm should opt skills in, not inherit everything; (c) "there is no rhythm managed skill db — rhythm just manages the opencode skills list" — collapse, don't preserve.
  - Investigated before deciding: confirmed in `apps/opencode_fork/packages/core/src/flag/flag.ts` that `OPENCODE_DISABLE_EXTERNAL_SKILLS` / `OPENCODE_DISABLE_CLAUDE_CODE_SKILLS` env flags exist (gating `.claude`/`.agents` scans), but the `~/.config/opencode/skills` scan has no flag (hardcoded in `ConfigPaths.directories()`). Audited what would actually be lost by disabling `.claude` scan: 23 "claude-only" names, but 18 are dead dirs (no SKILL.md, never loaded) and the 5 with real SKILL.md (`extract-design`, `find-skills`, `module-audit`, `supabase`, `supabase-postgres-best-practices`) all already have copies in `rhythm-managed-skills` — net zero loss.
  - **Set `OPENCODE_DISABLE_CLAUDE_CODE_SKILLS=1`** in `apps/api_server/.env` (first concrete step of the new direction). Caught + fixed a side effect: the edit tool reset `.env` to `0644` (world-readable) — restored to `0600` immediately since `.env` holds OAuth secrets. Flag takes effect on next engine spawn (this session won't see it until api_server restart).
  - **Filed issue #947** ("arch: make ~/.config/opencode/skills the sole skill source — Rhythm manages it directly, eliminate rhythm-managed-skills"), labeled enhancement/backend/infrastructure. Explicitly **supersedes #946** — eliminates the collision surface instead of patching the race. Covers: `rhythm_managed_skills.ts` one-line change, `opencode.json` `skills.paths` removal, `OPENCODE_DISABLE_EXTERNAL_SKILLS=1` (broader, kills `.agents` too), migration of FFB/ministry skills authored today, and agent-stack coordination (`sync-globals` drops the `~/.config/opencode/skills` target — separate repo, linked from the issue). No code touched (source work = future dev session scope).
  - **Flagged but not done**: `~/.agents/skills/` is the identical situation to `.claude` (3 redundant skills, all in rhythm-managed-skills). The broader `OPENCODE_DISABLE_EXTERNAL_SKILLS=1` would kill both `.claude` and `.agents` in one flag and is included in #947's plan, but not set yet — awaiting AJ's go-ahead to do it now vs. waiting for the full migration.
- **Followup pass (AJ said "do the followup")** — closed both open items:
  - Set `OPENCODE_DISABLE_EXTERNAL_SKILLS=1` in `apps/api_server/.env`, superseding the narrower `OPENCODE_DISABLE_CLAUDE_CODE_SKILLS=1` set earlier. Kills both `.claude` and `.agents` skill scans in one flag. Confirmed `.agents/skills/` had 3 skills (`find-skills`, `supabase`, `supabase-postgres-best-practices`), all redundant with `rhythm-managed-skills` — net zero loss. Mode restored to `0600` after edit (edit tool resets to `0644`).
  - Filled the 3 remaining genuine stub skills (`daily-email-triage`, `monday-weekly-plan`, `monthly-gc-report`) by porting content from their `~/.config/opencode/skills/` counterparts (same treatment `daily-morning-briefing` got earlier). Preserved the better rhythm-managed frontmatter descriptions; fixed stale `~/Documents/Claude Cowork/` path references and a temp-path docx skill reference to be workspace-agnostic. Deleted the redundant `opencode/skills` copies so no name collision remains for these three. Verified all three rhythm-managed copies are now real content (57/60/74 lines respectively, up from 10 each).
