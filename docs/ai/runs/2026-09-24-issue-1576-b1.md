---
date: 2026-09-24
repo: rhythm
branch: sol/1576-b1
pr: null
issues: [1576]
status: ready-for-verification
tags: [run, rhythm]
---

# #1576 Approach B — B1 local dispatch ledger

## Files

- `apps/api_server/src/database/migrations.ts`: one additive SQLite-only table, FK to local `agent_sessions(id) ON DELETE CASCADE`; no Postgres DDL, served-step table, shared session columns or historical backfill.
- `apps/api_server/src/models/model_provenance.ts`, `repositories/model_provenance_repository.ts`: B2/B3 contract below.
- `apps/api_server/src/repositories/model_provenance_repository.ts`: repair-only one-line `typeof string` sessionId guard before regex/SQLite.
- `apps/api_server/src/__tests__/issue_1576_b1_ledger.test.ts`, `docs/ai/contracts/issue-1576-b1.json`: 12 B1 regression checks and explicit deferred boundaries.

## Checks

- Worktree `/private/tmp/rhythm-1576-b1`, branch `sol/1576-b1`, HEAD `e93eac6e7f9007eb4f4be4a434784a5fe7e2d3da`, initially clean. No commit/push/merge/stash/clean.
- Phase 0 RED: `cd apps/api_server && npx vitest run src/__tests__/issue_1576_b1_ledger.test.ts -t 'c1 fresh'` → 1 failed, 4 skipped: `AssertionError: expected undefined to deeply equal { name: 'agent_turn_dispatches' }` (before implementation). First attempt without installed dependencies was a Vitest startup failure; `npm ci --no-audit --no-fund` installed dependencies in this worktree. Then test's missing repository import was moved behind the first assertion for a genuine RED.
- GitNexus (integration indexed repo, including test consumers) `runMigrations` CRITICAL, 62 direct/92 total, 0 indexed processes, 3 modules (Repositories, Release, Database); exact symbol/scope matched AJ's standing informed approval. No edits to `deleteById` or `deleteOlderThan`. An excluding-tests query returned HIGH 14 direct/34 total; the including-tests query reproduced the approved 62/92. `detect_changes(scope=all, worktree=/private/tmp/rhythm-1576-b1)` returned changed_files=1, changed_symbols=0 (the index misses newly untracked files); `git status --short` confirms only B1's migration, new model/repository/test, contract and this log.
- `cd apps/api_server && npx vitest run src/__tests__/issue_1576_b1_ledger.test.ts src/__tests__/migrations_replay_guard.test.ts src/__tests__/migrations_self_heal.test.ts src/__tests__/skill_schema_parity.test.ts src/__tests__/live_postgres_bootstrap.test.ts` → 42 passed, 10 skipped (live Postgres requires opt-in). `npm run build` → exit 0. `git diff --check` → exit 0.
- `cd apps/api_server && npm test` → 6227 passed, 6 failed, 255 skipped; unrelated `org_reviewer_harness_isolation.test.ts` inherited `RHYTHM_LIVE_ENGINE_URL=http://127.0.0.1:4096`, test requires 4097. `cd apps/api_server && env -u RHYTHM_LIVE_ENGINE_URL npm test` → 6233 passed, 255 skipped, 668 files passed, 132 skipped. No test modified for ambient override.
- After late-link idempotence refinement: `cd apps/api_server && npx vitest run src/__tests__/issue_1576_b1_ledger.test.ts src/__tests__/migrations_replay_guard.test.ts src/__tests__/migrations_self_heal.test.ts src/__tests__/skill_schema_parity.test.ts && npm run build` → 42 passed, build exit 0.
- Evidence repair attempt 1, Opus triage `c32baa27-0cd3-43b6-9679-943ebf0186ae`: verified `pwd && git branch --show-current && git status --short && git rev-parse --show-toplevel` → `/private/tmp/rhythm-1576-b1`, `sol/1576-b1`, exactly the six existing B1 paths above. No stash/clean/commit. Phase 0 newly added coercible non-string session ID assertion before repository edit: `cd apps/api_server && npx vitest run src/__tests__/issue_1576_b1_ledger.test.ts -t 'c4 refuses'` → **RED 1 failed/6 skipped**, expected `Invalid dispatch metadata`, received SQLite `Too few parameter values were provided`; proves validation gap, not harness failure.
- Phase 1: GitNexus `impact(target=insert, file_path=apps/api_server/src/repositories/model_provenance_repository.ts, kind=Method, direction=upstream)` returned UNKNOWN/not indexed (new untracked file). `impact(target=runMigrations, file_path=apps/api_server/src/database/migrations.ts, kind=Function, direction=upstream, includeTests=true, summaryOnly=true)` returned CRITICAL, 62 direct/92 total, zero indexed processes, 3 modules (Repositories, Release, Database), identical to the previously approved existing migration scope; **no migration edit in this repair**. Only the newly created repository method and B1 evidence paths were edited. Standing approval does not authorize expanded shared edits.
- Phase 2: single line changed in `insert`, adding `typeof input.sessionId !== 'string'` before regex. Expanded c1 exact one FK, c2 exact full typed/raw field mapping + inverse flags, nulls, 207 same-timestamp rows/default 100/cap 200/limit 0 and negative/foreign and missing cursors, c3 actual `AgentSessionMessagesRepository.deleteBySession` plus hard delete/purge/late FK, c4 12 identifier fields and length/character boundaries, owned enum/reason/outcome matrices, compile-time `@ts-expect-error` prompt key and runtime extra-key drop/no content columns, invalid outcome/link with no mutation, c5 five methods on Postgres with zero observed `prepare` calls. `cd apps/api_server && npx vitest run src/__tests__/issue_1576_b1_ledger.test.ts && npm run build && git diff --check` → GREEN 12/12, TypeScript build and diff check exit 0 (after fixing one test fixture ordering assertion; no second product repair).
- Astra57 focused command `cd apps/api_server && npx vitest run src/__tests__/issue_1576_b1_ledger.test.ts src/__tests__/migrations_replay_guard.test.ts src/__tests__/migrations_self_heal.test.ts src/__tests__/skill_schema_parity.test.ts src/__tests__/live_postgres_bootstrap.test.ts` → 47 passed, 10 skipped (57 total). This is a **focused 57-test command**, not an independent Astra review; Astra must rerun before B2.
- Clean full API: `cd apps/api_server && env -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_OPENCODE_ENGINE_PORT -u RHYTHM_MOBILE_GATEWAY_PORT npm test` → 668 files passed/132 skipped, 6238 tests passed/255 skipped. No port expectations altered.
- Final scope gate: `gitnexus_detect_changes(scope=all, worktree=/private/tmp/rhythm-1576-b1, repo=Rhythm)` → changed_files=1, changed_symbols=0, affected_processes=[], risk low; **index omits all five untracked paths**. `git status --short` independently shows exactly the original six B1 paths (one tracked migration, five untracked repository/model/test/contract/note); `git diff --check` exit 0. JSON parse/criteria check → 8 criteria, 3 explicitly deferred. No other product edit, commit, push, merge, stash or clean.
- Synthetic sandbox: `node tools/dev/sandbox_fixture.mjs /private/tmp/rhythm-1576-b1-fixture-sol` → read-only synthetic DB/config. Default `tools/dev/sandbox.sh up` refused occupied 4098; alternate 4198 attempt refused occupied 4099, then 4198 was occupied. Never touched those listeners. Adjusted only the external synthetic fixture MCP API URL to 4298 (chmod 600, edit, chmod 400); `RHYTHM_APPROVED_FIXTURE_ROOT=/private/tmp/rhythm-1576-b1-fixture-sol RHYTHM_LIVE_DB_PATH=/private/tmp/rhythm-1576-b1-fixture-sol/rhythm.db RHYTHM_SANDBOX_OPENCODE_CONFIG=/private/tmp/rhythm-1576-b1-fixture-sol/opencode.json RHYTHM_SANDBOX_DIR=/private/tmp/rhythm-1576-b1-sandbox-sol RHYTHM_SANDBOX_API_PORT=4298 RHYTHM_SANDBOX_ENGINE_PORT=4297 RHYTHM_SANDBOX_GATEWAY_PORT=4299 RHYTHM_OPTIMIZER_MODE=shadow tools/dev/sandbox.sh up` → API+engine ready. With the same sandbox dir and 4298/4297/4299 port variables: `tools/dev/sandbox.sh status` → owned listeners; `sqlite3 /private/tmp/rhythm-1576-b1-sandbox-sol/rhythm.db "SELECT name FROM sqlite_master WHERE type='table' AND name='agent_turn_dispatches'; SELECT COUNT(*) FROM agent_turn_dispatches; PRAGMA foreign_key_list(agent_turn_dispatches);"` → `agent_turn_dispatches`, `0`, one `session_id→agent_sessions.id ON DELETE CASCADE`; `tools/dev/sandbox.sh restart` → ready; repeated status/table/FK checks unchanged; `tools/dev/sandbox.sh down` → removed owned runtime, diagnostics preserved at `/private/tmp/rhythm-1576-b1-sandbox-sol.evidence.ZscsDH`. Standalone `sqlite3` pragma `foreign_keys` read 0 for its own connection; the integration tests explicitly enable FK and prove cascade/rejection; do not infer the API connection setting from the CLI connection.

## B2 repository contract / notes

- `new ModelProvenanceRepository().insert(DispatchInput)` requires `sessionId` of an existing SQLite local session, `origin` and `requestedSource` server-owned enum values; SDK session/user message may be null. Returns minted UUID `DispatchRecord` with `pending` outcome, timestamp, nullable selection/resolution/final SDK request provider/model/tier, three decision flags, safe reason code and exact nullable predecessor id. No engine/request hooking yet.
- `.setOutcome(id, 'accepted' | 'rejected' | 'unknown', sdkUserMessageId?)` settles once, repeats of the identical result are idempotent. A no-op response must be classified by B2 (never invented by B1). `.linkUserMessage(id, sdkUserMessageId)` attaches a late user ID if unset and not rejected, idempotently. `.get(id)` and `.list(sessionId, {limit?, afterId?})` read stable insertion-order pages (default 100, cap 200). An unknown cursor yields an empty page; pagination is private to B2/B3. Postgres throws explicitly before SQLite access; B2 must catch/log sanitized repository failures so dispatch itself remains fail-open.
- Identifiers and model/tier fields accept only bounded ASCII identifier characters (1–200); reason code is lower snake case, max 64. Never pass provider errors, prompt text, credentials or bodies. Do not treat this ledger as served identity: `served.status` remains `not_recorded` without a trusted engine stamp, which Astra rejected. B1 has no route, UI, fork or live dispatched-turn behavior; synthetic sandbox schema startup/restart only is verified. #1578 is absent and not required. Astra reviews B1 before B2.
- Repair caveats: type shape and dropped extra keys are **not** a secret filter or proof of server-owned values: identifier-compatible secrets may enter an allowed field. Repository has no owner check. Runtime boolean coercion, non-finite pagination limits and rowid-vs-timestamp ordering are ceilings; default 100/cap 200 and same-timestamp rowid order are tested. Engine revert endpoint (only message-repository deletion exercised), served identity, HTTP route and live Postgres remain UNVERIFIED, listed as manual/deferred contract targets. Sandbox verified schema boot/restart, not a dispatched turn. No served/API claim; independent Astra rerun required before B2.

## Repair 2 (2026-09-24)

- Defect: `setOutcome` and `linkUserMessage` relied on regex coercion for runtime identifiers, allowing non-string values to reach SQLite and allowing a null late-link value to report success without creating a linkage. Both methods now require string row ids; `setOutcome` accepts only null/undefined or a safe string SDK user message id, and `linkUserMessage` requires a safe string SDK user message id.
- Files touched: `apps/api_server/src/repositories/model_provenance_repository.ts`, `apps/api_server/src/__tests__/issue_1576_b1_ledger.test.ts`, and this required run log. `docs/ai/contracts/issue-1576-b1.json` was unchanged because c4 was already `pass` and is now covered by the focused assertions.
- RED: `cd /private/tmp/rhythm-1576-b1/apps/api_server && npx vitest run src/__tests__/issue_1576_b1_ledger.test.ts -t 'c4 rejects non-string outcome and link identifiers'` → 1 test failed, 12 skipped; numeric `sdkUserMessageId` did not throw.
- GREEN: `cd /private/tmp/rhythm-1576-b1/apps/api_server && npx vitest run src/__tests__/issue_1576_b1_ledger.test.ts` → 1 test file passed, 13 tests passed, 0 failed.
- TypeScript: `cd /private/tmp/rhythm-1576-b1/apps/api_server && npx tsc --noEmit -p tsconfig.json` → exit 0, no diagnostics.
- No sandbox, api_server, Playwright, Flutter, live test, package install, commit, stash, or other git-state mutation was performed.
