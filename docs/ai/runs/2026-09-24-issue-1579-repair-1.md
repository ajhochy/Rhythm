---
date: 2026-09-24
repo: Rhythm
branch: swarm/issue-1579
pr: null
issues: [1579]
status: blocked
tags: [run, Rhythm]
---

## Files

- Repaired U1/U2 only: `apps/electron/src/{main.mjs,preload.cjs}`, `apps/electron/test/{issue-1579-contract.test.mjs,post-m1-phase-7-native-notifications.test.mjs}`, `apps/web/src/{store.tsx,components/Transcript.tsx}`, `apps/web/tests/electron/issue-1579-notifications.spec.ts`, approved plan and issue contract. Existing U1/U2 files, prior run notes, and unrelated worktree changes were preserved. No API/fork/Flutter/dependency/U3 seam changes.

## Checks

- Entry `pwd && git branch --show-current && git status --short && git rev-parse --show-toplevel` confirmed `/private/tmp/rhythm-swarm-1579`, branch `swarm/issue-1579`, existing dirty U1/U2 files and prior untracked contracts/plans. Astra decision `8eabf939-d30d-49e3-b365-4619d982b110` authorizes Option A/local sign-in boundary and targeted clipboard policy; no other approval implied.
- Phase 0: added regression contracts before repair. `node --experimental-vm-modules --test test/issue-1579-contract.test.mjs` RED (13 pass/9 fail): cloud bearer sent in lookup, overflow lost fifth ask, abandoned 404 stream, clipboard denied. `npm exec -- playwright test --config tests/issue-1579-playwright.config.ts` RED (1 fail/1 pass): Flutter-equivalent bell label absent.
- Phase 1 GitNexus `impact` in Rhythm: `FixtureProvider` LOW, 0 direct/0 processes; `Transcript` LOW, 1 direct caller `AgentsWorkspace`, 0 processes; `rebuildMainWindow` LOW, 0 direct/0 processes. New `syncAgentNotifications` absent from stale index (UNKNOWN; no HIGH/CRITICAL); earlier U1/U2 impact report covers `invalidateAuthentication` LOW. `detect_changes(scope=all, worktree=/private/tmp/rhythm-swarm-1579)` LOW, seven tracked indexed source/test files, no affected processes; untracked files audited via git status.
- Focused host: `node --experimental-vm-modules --test --test-concurrency=1 test/issue-1579-contract.test.mjs test/e12a-auth-boundary.test.mjs test/post-m1-phase-7-native-notifications.test.mjs` first 36/37 with stale source-string check; updated the legacy assertion to reflect the closed clipboard exception. `npm run typecheck` initially failed on JSDoc; corrected. `npm run typecheck` now PASS. Browser `npm exec -- playwright test --config tests/issue-1579-playwright.config.ts`: 2/2 PASS. `npm run build` in web: PASS (1696 modules; existing bundle warning).
- Full Electron `env -u RHYTHM_RELAY_URLS -u RHYTHM_RELAY_BEARER -u RHYTHM_PRODUCTION_API_URL -u RHYTHM_LIVE_API_URL -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_OPENCODE_ENGINE_PORT npm test`: **168/168 PASS**. Contract file is excluded from `npm test`; focused `node --experimental-vm-modules --test test/issue-1579-contract.test.mjs` additionally run. `npm run test:dist-smoke` is a **web** script, not Electron (attempt in Electron: missing script); web command passed below.
- Full web first `RHYTHM_E2E_PORT=6399 RHYTHM_DIST_PORT=6398 npm test`: default 460 pass/49 skip/7 fail (six inherited `RHYTHM_API_BASE=http://localhost:4001` trust-boundary fixture failures, one unrelated Hermes splitter). Second run with `env -u RHYTHM_LIVE_API_URL -u RHYTHM_LIVE_ENGINE_URL -u RHYTHM_RELAY_URLS -u RHYTHM_RELAY_BEARER RHYTHM_API_BASE=http://127.0.0.1:65534 RHYTHM_ENGINE_BASE=http://127.0.0.1:65533 RHYTHM_E2E_PORT=6399 RHYTHM_DIST_PORT=6398 npm test`: default **466 pass/49 skip/1 fail** (unrelated Hermes splitter). `RHYTHM_E2E_PORT=6399 RHYTHM_DIST_PORT=6398 npm exec -- playwright test --config tests/bucket-a-rendered-repair-playwright.config.ts`: **11 pass/4 fail** (pre-existing screenshot, Gallery video, duplicated auto-promotion selectors); no unrelated code edited. `RHYTHM_E2E_PORT=6399 RHYTHM_DIST_PORT=6398 npm run test:electron-slices`: all discovered slice stages passed, three live-only skips. `RHYTHM_E2E_PORT=6399 RHYTHM_DIST_PORT=6398 npm run test:session-opening`: **13/13 PASS**. `npm run test:dist-smoke` in web: PASS, index plus two relative assets. All stages were invoked on non-U3 ports, but **full web is not green**.
- `git diff --check`: PASS. `git status --short` reports tracked generated `docs/ai/runs/evidence/electron-m1-shell.png` changed by existing Electron smoke; prior attempt to restore had permission denied. No commit/push/merge/stash/clean or U3 work.
- Final host focused command `node --experimental-vm-modules --test --test-concurrency=1 test/issue-1579-contract.test.mjs test/e12a-auth-boundary.test.mjs test/post-m1-phase-7-native-notifications.test.mjs && npm run typecheck`: **39/39 PASS**, typecheck PASS. The remote-origin test initially failed because the test harness lacked Electron's `--allow-test-runtime-ports` flag and production correctly overrode its injected URL; harness corrected and remote configured URL is now actually exercised. `git diff --check`: PASS. The separately executed web slices also regenerated five tracked screenshot evidence files under `docs/ai/runs/artifacts/{e25b,e27,e50,e52a}`; these artifacts were clean at entry, are not product edits, and must not be included in integration. They were not restored because worktree restoration was previously permission-denied. All run/contract statuses preserve U3 as unverified.

## Notes / handoff

- Local request is signed-in gated in main but **anonymous from its first/only loopback GET**; existence, not cloud ownership. Remote origins rejected without fallback. Notifications-only permission denied at request and check, with owned trusted-document clipboard-sanitized-write exception; other permissions remain denied. 100 retained entries, FIFO overflow, four active lookups, 20/min admission; live asks do not TTL-expire, only tombstones do. Unknown resolve is limited; known resolves still withdraw. Body cancellations, response shape, Cc/Cf-safe name, extra IPC args, preload stringify guard, window-scoped close, Flutter bell text, explicit successful idle transition were repaired.
- **BLOCKED**, not READY_FOR_VERIFICATION: U3 packaged bridge and real Notification Center/click/System Settings remain not_tested pending Astra code re-review. Full web default Hermes splitter and bucket-A unrelated failures remain red; screenshot restoration needs authorized handling. No packaged U3 harness or alternate runtime was started. Ask Astra to review this U1/U2 repair before any U3 work.

## Evidence-only reconciliation

- WAIVED: documentation/contract-status reconciliation only; verification is JSON parsing and checking the scoped diff against the supplied evidence, with no product behavior changed.
- Corrected the earlier contradictory Astra block/status hygiene: c1, c2, and c4 are `pass` for automated U1/U2 host+renderer behavior only; their reasons explicitly do not claim packaged/native U3 or visible OS delivery. c3 and c5–c8 remain `pass`.
- c9–c12 are `not_tested` with explicit reasons; `not_tested` lists exactly c9–c12 and no `UNVERIFIED` status remains. U3 remains frozen until U1/U2 PASS; this reconciliation does not authorize packaged U3 work. Contract waiver remains null; test paths and no-waiver contract posture are preserved.
