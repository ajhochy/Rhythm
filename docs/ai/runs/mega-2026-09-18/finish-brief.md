# Finish the 2026-09-18 Rhythm mega PR: computer-control smoke + remaining work

You are a Codex agent with computer control of this Mac. Nobody will answer questions: decide, act, record every decision under a `Decisions` heading. Work only in `/Users/ajhochhalter/Documents/Rhythm/.mega-wt/integration` (branch `mega/2026-09-18-mobile-electron-hermes`, draft PR ajhochy/Rhythm#1544) and, for the fork, `/Users/ajhochhalter/Documents/hermes-rhythm-plugin` (branch `mega/2026-09-18-rhythm-plugin-finish`, draft PR ajhochy/hermes-rhythm-plugin#17 — always pass `-R ajhochy/hermes-rhythm-plugin` to gh there). Commit on those branches with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; push; never merge either PR.

## Hard rules
- Never type a password, token or key into any field; `RHYTHM_LIVE_TOKEN` is already in your environment for test commands only — never print or persist it.
- Data: you may CREATE rows named `MEGA-SMOKE-2026-09-18-<runId>` and must delete them before finishing; never edit, complete, archive or delete rows you did not create.
- Do not quit/kill the Flutter "Rhythm" app, Hermes Desktop, or the local services on ports 4001/4096; use the isolated sandbox procedure in `docs/ai/testing-guide.md` (`tools/dev/sandbox.sh`) if you need a disposable backend.
- Review against the ORIGINAL issue expected behavior (`docs/ai/runs/mega-2026-09-18/issues/<n>.md`), never against the earlier proposal. Never loosen an assertion that reflects expected behavior.
- Screenshots go to `.proof/mega-2026-09-18/` as `NN-<surface>-<state>.png`; the log is `.proof/mega-2026-09-18/VISUAL-SMOKE.md` (table: step, expected, observed, PASS/FAIL/BLOCKED, screenshot).

## Part 1 — computer-control visual smoke (the brief is `docs/ai/runs/mega-2026-09-18/visual-smoke-brief.md`; follow it step by step)
1. Build and launch: `cd apps/web && env -u VITE_RHYTHM_GATEWAY_MODE -u VITE_RHYTHM_API_BASE -u VITE_RHYTHM_ENGINE_BASE -u VITE_RHYTHM_LIVE_TOKEN VITE_RHYTHM_GATEWAY_MODE=live npm run build`, then `cd apps/electron && RHYTHM_SHELL_USER_DATA=/private/tmp/rhythm-mega-electron-user-data RHYTHM_LIVE_API_URL=http://127.0.0.1:4001 RHYTHM_LIVE_ENGINE_URL=http://127.0.0.1:4096 RHYTHM_PRODUCTION_API_URL=https://api.vcrcapps.com RHYTHM_HERMES_ENABLED=1 RHYTHM_HERMES_PORT=9121 npx electron . --interactive-smoke --allow-test-runtime-ports &`. Wait for `hermes: ready` on stdout and `curl http://127.0.0.1:9121/api/health` = 200.
2. Walk all ten steps of the visual-smoke brief in the real window (list-and-inspector pages vs Agents → Tasks side by side, View options, Load subagents, Add project via the native folder picker with a fresh /tmp folder, Profiles editor, reading comfort light+dark with appearance restored, three splitter drags + reload + Reset layout, Hermes tab starting→ready→embedded themed dashboard→"Ask Hermes about my dashboard" opens a draft and never sends, Finder icon of `apps/electron/dist/Rhythm.app` (run `npm run package:mac` first if dist is missing) including Get Info, 200% zoom then Cmd+0).
3. Any FAIL: fix the product on the branch (smallest diff meeting the issue's expected behavior), add/adjust the rendered spec that should have caught it, re-verify visually, record before/after screenshots.

## Part 2 — hosted live Playwright smoke (write paths included)
`cd apps/web && RHYTHM_LIVE_E2E=1 RHYTHM_LIVE_API_URL=http://127.0.0.1:4001 RHYTHM_LIVE_ENGINE_URL=http://127.0.0.1:4096 npx playwright test --config tests/live-smoke-playwright.config.ts --reporter=list` (uses `RHYTHM_LIVE_TOKEN` from the environment). Expected: the 19 hosted-domain tests now run (create → read-back → delete of marker rows); Deep Research runs if the research service exists. Fix real defects in product code; fix the spec only where it is wrong about the rendered contract; afterwards `GET` each hosted collection and confirm zero `MEGA-SMOKE-2026-09-18-*` rows remain.

## Part 3 — fork live gate (Rhythm plugin inside Hermes)
Follow `plugins/rhythm/packaging/LIVE-GATE.md` in the fork: install/enable/reload/disable/uninstall cycle, `hermes plugins doctor rhythm`, `hermes doctor`, then the hosted checks against https://api.vcrcapps.com only (Rhythm sidebar destination renders in Hermes Desktop, dashboard + task list read, `rhythm_get_dashboard` / `rhythm_list_tasks` / `rhythm_complete_task` pass approval with read-back on a task YOU created, "Ask Hermes about this" opens a draft and never sends, network trace shows zero hosted POST/PATCH/PUT/DELETE in the read-only slice). The connection uses the plugin's M3 flow; if it needs an interactive sign-in, do it in the visible browser window that the flow opens and record it as done by the operator's session. Update fork PR #17: turn `Refs #N` into `Closes #N` only for milestones you proved; leave the rest as Refs with the exact gap.

## Part 4 — remaining code work (each: failing test first, then fix, then gate)
- #1373: PTY over the relay (`apps/api_server/src/routes/relay_gateway_routes.ts` currently returns 501), sanitized relay diagnostics (`services/relay_uplink_client.ts`, `relay_uplink_server.ts` leak raw exceptions/paths), credential freshness on established uplinks. Gates: `cd apps/api_server && npm run build && npx vitest run`, `cd apps/mobile && npm run typecheck && npm run lint && npx jest`.
- Adversarial review still missing for the shared `ListInspector` primitive and #1509: write a checklist of expected behaviors from issues 1513 and 1509, verify each against `apps/web/src/components/ListInspector.tsx/.css`, `ToolWorkspace.tsx` (LiveSchedulesTool), `pages/tasks/styles.css`, `components/Transcript.*`, `styles.css`; fix MISSING items; record the checklist in `docs/ai/runs/2026-09-18-review-primitive-and-comfort.md`.
- Harden the flaky Electron test "Hermes readiness rejects HTTP 401 until the deadline" in `apps/electron/test/hermes-server.test.mjs` with injected fake timers (it failed 2 of 3 isolated runs); `cd apps/electron && npm test` must be 163/163 three times in a row.
- Full gates before pushing: `cd apps/web && npm run typecheck && npm run build && npx playwright test && npm run test:electron-slices`; `cd apps/electron && npm run typecheck && npm test`; `cd apps/mobile && npm run typecheck && npm run lint && npm test && npm run contract:check`. A test that fails once and passes alone is a flake — say so, do not "fix" it.

## Part 5 — records
- Update `docs/ai/project-state.md`, append to `docs/ai/runs/2026-09-18-mega-mobile-electron-hermes.md`, and edit the PR #1544 body: visual smoke table summary, hosted live smoke numbers, fork live gate result, what AJ still must do (only: audible #1510 check on a physical iPhone, TestFlight/device items, Cloudflare/NAS deploys).
- Record the run: `cd /Users/ajhochhalter/Documents/dev-dashboard && node publish-to-rhythm.mjs run '{"agent":"codex","session":"mega/2026-09-18-mobile-electron-hermes","task":"Rhythm: mega PR finish — visual smoke, hosted live smoke, fork live gate","status":"pass|repairing|pending","note":"<counts only>"}'`.
- Clean up: quit only the Electron window you launched, remove marker rows, `tools/dev/sandbox.sh down` if you started a sandbox, remove any worktrees you created.

Final message: PASS/FAIL/BLOCKED counts per part, both PR links, every decision, and what still needs AJ. No questions.
