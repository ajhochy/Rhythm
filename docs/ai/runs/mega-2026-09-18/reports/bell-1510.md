## Summary

Implemented mobile opt-in processing audio, one-time saved-preference migration, and single-player lifecycle handling. Reproduced and repaired an Electron native-notification dedupe defect in a socket-free test. The reported audible problem is not yet attributed or physically verified on either client.

Branch: `mega/ws-1510-bell`; base: `648f8d5885b1767bcb53a95c7e8aa7eadce2c5f2`; Node: `v22.23.0`. Changes remain uncommitted. No services or applications were launched.

## Files changed

- `apps/mobile/providers/opencode-provider-utils.ts` — default off and one-time migration helper/marker.
- `apps/mobile/providers/use-opencode-persistence.ts` — migration on initial and host-switch hydration.
- `apps/mobile/lib/voice/working-sound.ts` — serialized operations, invalidated pending starts, one player, idempotent stops and failure cleanup.
- `apps/mobile/providers/opencode-provider.tsx` — selected-session/foreground playback; stop on completion, cancellation, failure, preference disable, disconnect, switch and unmount.
- `apps/mobile/tests/provider-utils.test.mjs` — assert default-off behavior.
- `apps/mobile/tests/contract/issue-1510-working-sound.test.mjs` — 9 migration, persistence, fake-player race and rendered lifecycle tests.
- `apps/electron/src/main.mjs` — retain pending approval dedupe entries after notification dismissal/expiry.
- `apps/electron/test/e12a-auth-boundary.test.mjs` — reproduce and prevent repeated alerts for a dismissed pending approval.
- `docs/ai/runs/2026-09-18-issue-1510-electron-audio-investigation.md` — source findings, exact grep evidence, runtime limits and audible qualification checklist.
- `REPORT.md` — this handoff, written locally; the existing repository exclude rule ignores this filename.

## Checks run

All commands below ran in this worktree, starting from its root unless shown otherwise.

1. **PASS** — `cd /Users/ajhochhalter/Documents/Rhythm/.mega-wt/ws-1510-bell && pwd && git rev-parse --abbrev-ref HEAD`; output: expected worktree, `mega/ws-1510-bell`. `echo ok > .write-probe && rm .write-probe` exited 0.
2. **PASS, final rerun** — `cd apps/mobile && npm run typecheck && npm run lint && npm run test:provider-utils && node --test tests/contract/issue-1510-working-sound.test.mjs`; exit 0. Output tail: lint `0 errors, 3 warnings`; `provider utility tests passed`; `# tests 9`, `# pass 9`, `# fail 0`.
3. **PASS** — `cd apps/electron && npm run typecheck && node --experimental-vm-modules --test test/e12a-auth-boundary.test.mjs test/post-m1-phase-7-native-notifications.test.mjs test/post-m1-phase-7-packaged-notifications.test.mjs`; exit 0. Output tail: `# tests 13`, `# pass 13`, `# fail 0`.
4. **PASS** — `cd apps/mobile && npm run test:connection-persistence && node --test tests/contract/msp-001-session-profile-contract.test.mjs tests/contract/msp-002-profile-first-sessions.test.mjs`; exit 0. Output tail: `connection persistence tests passed`; `# tests 15`, `# pass 15`, `# fail 0`.
5. **EXPECTED FAIL before Electron fix** — `node --experimental-vm-modules --test --test-name-pattern='issue-1510' apps/electron/test/e12a-auth-boundary.test.mjs`; exit 1. Output: `a dismissed pending ID must stay deduplicated across repeated snapshots`, `2 !== 1`; `# pass 0`, `# fail 1`. The same regression passes after the change.
6. **Initial failures repaired** — `cd apps/mobile && npm run typecheck` initially reported `TS2741` for a legacy fixture missing the new marker; the marker is now optional in the type. The first full command in item 2 then reported `'Buffer' is not defined`; the test now imports `node:buffer`. Final item 2 passes.
7. **PASS** — `git diff --check`; exit 0, no output. GitNexus upstream impact calls for edited production functions reported LOW risk; details and source-search commands are in the run log.

The three lint warnings are in unchanged files: `providers/services/agent-chat-service.ts:19`, `tests/e2e/issue-1237-paired-reachability.spec.mjs:259`, and `tests/global-event-stream.test.ts:13`. Test output also reports existing React test-renderer deprecation and Node experimental-VM warnings.

No Playwright/native launch, socket-bound test, backend build/start, or audible smoke was run, per the worker restrictions. The rendered mobile test mounts the real sound-related provider statements extracted from its AST; it does not mount the entire app. Native audio and Electron notification objects are fakes. These results qualify the tested boundaries only.

## Acceptance criteria

- **Reproduce and identify sources/builds separately → partial.** Mobile's synthesized loop/default-enabled trigger is source-backed; Electron's dismissal/re-sync defect reproduced red then green. Neither client's reported audible sound or affected installed build is confirmed. No Electron renderer audio loop was found, and the current renderer has no sender for the native approval-sync event.
- **Disabled normal turns remain quiet through streaming, tools, reconnect and repeated turns → partial.** Default/off and lifecycle contracts pass; source contains no equivalent Electron processing loop. Actual streaming/tool activity and audible silence require device/app verification.
- **Processing audio opt-in; saved preferences documented → done.** Default is false. Legacy storage has no explicit-choice provenance: every unmarked true resets off once, including indistinguishable intentional legacy choices. `workingSoundDefaultMigrated: 1` persists with the result; subsequent explicit on/off choices survive hydration, host switches and relaunch. Other preferences remain intact.
- **Disable mid-turn promptly; off survives relaunch → partial.** Rendered lifecycle and real persistence-hook remount tests pass; pending-load invalidation prevents late playback. Physical-device timing remains unverified.
- **Retained loop stops on completion/cancel/failure/session/lifecycle changes; no duplicate/orphan playback → partial.** All 9 mobile tests pass, including duplicate events, cancellation before its HTTP response, failure, unmount and late native-load/play races. Native-device qualification remains outstanding.
- **Intentional voice and configured alerts preserved → partial.** Speech/completion-notification implementation is unchanged, processing audio excludes listening/speaking, and first approval alerts retain their original options. Dedupe, click routing and authentication-isolation tests pass. Audible speech/completion/approval confirmation remains outstanding.

## Decisions

- Used the brief's one-time-reset fallback because stored defaults and explicit legacy opt-ins cannot be distinguished; rejected inventing provenance or repeatedly resetting later opt-ins.
- Migrated both persistence hydration paths; rejected changing the default alone, which would leave saved true values enabled.
- Scoped processing audio to the selected connected foreground session; rejected allowing unrelated background sessions to keep it ringing.
- Serialized native player operations and invalidated pending starts; rejected relying on effect cleanup alone during asynchronous loads.
- Retained pending approval IDs after notification closure; rejected globally silencing actionable notifications.
- Left xterm unchanged: installed v6 emits an unhandled bell event and has no supported `bellStyle` option/playback path; rejected unsupported options and stripping terminal output.
- Kept records in this report and the named run log; left shared project-state, other worktrees, commits and external tracker publication to the orchestrator.

## Follow-ups

- Keep #1510 open until actual client/device attribution and audible qualification are recorded. Test Flutter's client/runtime and other connected clients separately; macOS per-app notification settings remain an alternative explanation.
- The current renderer has no `rhythm:approval-notifications` sender. Native notification wiring is a separate follow-up, not expanded in this diff.
- Orchestrator handles commit/integration, broader runnable validation and Dev Dashboard publication. No external tracker write was attempted under the worktree-only restriction.
- Three unchanged mobile lint warnings remain.

## Needs a human

- Audible verification on a physical iPhone and the actual Electron candidate: long text/tool turns, off/on, mid-turn disable, cancellation, error/recovery, session switch, reconnect, a second turn after relaunch, and intentional speech/approval/completion alerts. Record build and emitting device/process, testing each client separately and together without restarting Flutter-owned services.
