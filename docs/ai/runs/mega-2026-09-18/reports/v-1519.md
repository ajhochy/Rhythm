# Summary

Adopted the shared `ListInspector` for Integrations. Five compact rows now select provider/tool details, while connection actions, provider settings, Gmail signals, Planning Center filters, assistant consent, and AI Import remain explicit inspector actions. Selection is URL-backed with `integrationId`, legacy section deep links still select the matching row, and missing IDs render the primitive's not-found state.

# Files changed

- `apps/web/src/pages/integrations/index.tsx` — replaced the page-specific provider split with the shared list/inspector, moved row actions into inspectors, and added URL-backed selection/state handling.
- `apps/web/src/pages/integrations/styles.css` — added scoped shared-inspector sizing and detail styles.
- `apps/web/tests/pages/integrations-list-inspector.spec.ts` — added the required fixture-mode selection, action, state, accessibility, narrow/zoom, refresh, and missing-ID coverage.
- `apps/web/tests/pages/integrations.spec.ts` — updated interaction flows to select shared rows before inspector actions.
- `apps/web/tests/contract/issue-2009-integrations.spec.ts` — replaced old card/button selectors with shared row helpers and inspector assertions.
- `docs/ai/contracts/issue-1519.json` — recorded seven executable UI acceptance criteria.
- `docs/ai/runs/2026-09-18-integrations-list-inspector.md` — recorded this run and its validation boundary.
- `REPORT.md` — this worker handoff.

# Checks run

- `cd apps/web && npm run typecheck` — pass, exit 0; output tail: `> tsc -b` / `TYPECHECK_EXIT=0`.
- `cd apps/web && npm run build` — pass, exit 0; output tail: `✓ 1682 modules transformed.` / `✓ built in 27.46s` / `STATIC_GATE_EXIT=0`. Vite emitted the pre-existing large-chunk warning.
- `git diff --check` — pass, no output.
- `jq empty docs/ai/contracts/issue-1519.json` — pass, no output.
- `gitnexus impact IntegrationsPage ...` and `gitnexus impact InspectorPortal ...` — inconclusive: the stale index returned no symbol graph output.
- `gitnexus detect-changes --repo /Users/ajhochhalter/Documents/Rhythm/.mega-wt/ws-1519-integrations --scope compare --base-ref main --limit 100` — unavailable: this worktree is not registered in GitNexus. A run against the main checkout was rejected as evidence because it reported unrelated changes.
- `cd apps/web && npm exec -- playwright test tests/pages/integrations-list-inspector.spec.ts` — not run by instruction; Playwright would bind the configured Vite/dist servers.
- `node /Users/ajhochhalter/Documents/dev-dashboard/publish-to-rhythm.mjs run ...` — failed; this worker could not resolve `api.vcrcapps.com` (`ENOTFOUND`).

# Acceptance criteria

- List each provider/account or integration section — done. The shared list exposes Google Calendar, Gmail, Planning Center, Assistant access, and AI Import with compact identity/status metadata.
- Selecting an entry shows details/settings/actions — done. Provider settings and tool descriptions render from the selected shared item.
- Preserve existing actions/settings/signals/filters/import tools — done. All existing actions are reachable from the matching inspector; Sync all remains globally visible in the page header.
- Keep assistant Google consent distinct and selection inert — done. Assistant access is a separate row/inspector, and row selection only updates `integrationId`.
- Mouse/keyboard selection and visible focus/selected state — partial. Implemented through the shared primitive and covered by the new helper-based spec; rendered execution is pending the orchestrator.
- Long metadata, empty/loading/error/read-only, narrow, zoom, and accessibility — partial. State adapters and coverage are written; the prohibited Playwright/axe run remains pending.
- Refresh/deep-link restoration and missing/deleted state — partial. URL-backed logic and regression coverage are written; rendered execution remains pending.

# Decisions

- Used stable row IDs prefixed with `integration-` and the `integrationId` query key; rejected page-local selection state because it would not restore on refresh.
- Kept Sync all in the page header; rejected placing it in the list rail because the rail is intentionally hidden while inspecting an item at narrow widths.
- Preserved the existing no-disconnect/no-revoke domain contract; rejected inventing a gateway mutation absent from the current page and API.
- Kept AI Import selection inert and retained `/integrations/import` as the explicit legacy deep link that opens the dialog.

# Follow-ups

- Orchestrator: run `cd apps/web && npm exec -- playwright test tests/pages/integrations-list-inspector.spec.ts tests/pages/integrations.spec.ts tests/contract/issue-2009-integrations.spec.ts` with the configured fixture servers, then capture the rendered screenshot/axe evidence.
- Orchestrator: run GitNexus `detect_changes` after this worktree is integrated into a registered/indexed checkout.
- Orchestrator: republish the Dev Dashboard run entry from a network-enabled environment.

# Needs a human

None.
