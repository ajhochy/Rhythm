# Summary

Migrated every in-scope Agent Tool in `ToolWorkspace.tsx` to the existing shared
`ListInspector` contract: Brain, Deep Research, Webhooks, Skills, Playbooks,
Cookbook, Review Queue, Report Card, Email, and Gallery, in both fixture and live
branches where both exist. Compact title/subtitle rows now select full inspector
content; global create/refresh/filter controls remain in the toolbar; item actions,
editors, report detail, and artifact previews live in the inspector.

Tasks remains the unchanged baseline. Profiles and all Agent Settings components
remain unchanged as required. The shared `ListInspector.tsx` and
`ListInspector.css` are also unchanged.

The permitted static gate is green. The overall verification gate is **blocked in
this worker** because the brief explicitly prohibits Playwright, Vite/Electron
launches, and socket binding. The authored Playwright suite covers those rendered
criteria and writes screenshot receipts for the orchestrator's authorized run.

# Files changed

- `apps/web/src/components/ToolWorkspace.tsx` — adopts `ListInspector` and URL-backed selection across every in-scope fixture/live Agent Tool while preserving domain actions.
- `apps/web/src/components/ToolWorkspace.css` — adds co-located preview, recipe-step, and inspector proposal styles.
- `apps/web/src/components/ToolWorkspace.contract.test.mjs` — adds inventory, shared-primitive, no-bespoke-shell, and selection-key structural contracts.
- `apps/web/tests/pages/agent-tools-list-inspector.spec.ts` — adds fixture-mode rendered coverage for two-item selection, keyboard use, all item actions, missing/deleted IDs, edge states, axe, narrow/zoom layouts, and screenshots.
- `apps/web/tests/tools.spec.ts` — updates Brain, Review Queue, and Gallery assertions for the shared row/inspector markup.
- `apps/web/tests/secondary-tool-paths.spec.ts` — updates Brain, Skills, and Playbooks search coverage for the shared search control.
- `apps/web/tests/bucket-a-rendered-repair.spec.ts` — updates Gallery fallback, Skills loading, and live Review Queue assertions for inspector rendering.
- `apps/web/tests/parity-edge-cases.spec.ts` — updates restored Brain-state assertions for the shared primitive.
- `docs/ai/contracts/issue-1513.json` — records all eight acceptance criteria and marks only the three executed structural criteria as pass.
- `docs/ai/runs/2026-09-18-issue-1513-agent-tools-inventory.md` — records the required one-row-per-tool before/after/function-preservation inventory.
- `REPORT.md` — records this worker handoff and verification boundary.

# Checks run

- `cd /Users/ajhochhalter/Documents/Rhythm/.mega-wt/ws-1513-agent-tools && pwd && git rev-parse --abbrev-ref HEAD` — pass; path matched and branch was `mega/ws-1513-agent-tools`.
- `echo ok > .write-probe && rm .write-probe` — pass; the worktree is writable.
- Per-symbol `gitnexus impact --target '<component>' --direction upstream --repo Rhythm` for the 17 edited Tool components — pass; each result was LOW risk with one direct caller and no affected execution process. The global index was noted as stale and rooted at the main checkout, so it was used only for the required pre-edit blast-radius check.
- Initial `cd apps/web && node --test src/components/ToolWorkspace.contract.test.mjs` — expected red; 9 passed and 2 new adoption contracts failed before implementation.
- Final `cd apps/web && npm run typecheck` — pass; `tsc -b` exited 0 with no diagnostics.
- Final `cd apps/web && npm run build` — pass; Vite 5.4.21 transformed 1,683 modules and built in 13.49s. Output tail: `dist/assets/index-CeE_E6dD.js 1,489.22 kB`; the existing over-500-kB chunk warning remains.
- Final `cd apps/web && node --test src/components/ToolWorkspace.contract.test.mjs` — pass; 13 tests passed, 0 failed, duration 246.143 ms.
- `cd apps/web && ./node_modules/.bin/esbuild tests/pages/agent-tools-list-inspector.spec.ts --bundle --platform=node --format=esm --external:@playwright/test --external:@axe-core/playwright --outfile=/tmp/issue-1513-agent-tools-list-inspector.mjs` — pass; the authored spec and helper imports bundled to 11.8 kB in 22 ms without launching Playwright.
- `git diff --quiet -- apps/web/src/components/ListInspector.tsx apps/web/src/components/ListInspector.css apps/web/src/components/Profiles.tsx` plus `diff -u` comparisons for the Tasks and Agent Settings component slices against `HEAD` — pass; all excluded/baseline areas are unchanged.
- `git diff --check` — pass; no whitespace errors.
- Not run: Playwright, axe execution, screenshot inspection, live gateway actions, Vite, or packaged Electron. These are verification-gate failures in this worker because the brief prohibits the required runtime/socket launches.
- Not run: `ai-workflow checks --level issue` because it runs the unrelated Dart formatter, and `ai-workflow checks --level pr` because it includes unrelated stacks and socket-backed mobile web e2e. The requested affected-package checks above were used instead.

# Acceptance criteria

- Inventory every exposed Tools entry — **done**. The inventory has one row for all 13 SessionRail entries, including unchanged Tasks and explicit Profiles/Agent Settings exclusions; the contract test enforces the list.
- Every tool uses compact rows plus a main inspector — **done for implementation/static evidence**. All 17 in-scope fixture/live component branches render exactly one `ListInspector`; the structural contract passed.
- Reuse the shared contract — **done**. The existing primitive is imported as-is, old page-specific rails/grids are absent from adopted sections, and the primitive files are unchanged.
- Keep Tasks as baseline and preserve scheduling/history/actions — **partial verification**. The Tasks source slice is byte-for-byte unchanged and its actions remain covered by the authored rendered spec, but the rendered test was not run here.
- Preserve editors, previews, review actions, and profile controls — **partial verification**. The controls are located in inspector content and the action-reachability spec covers them; Profiles is unchanged. Rendered execution remains pending.
- Mouse/keyboard selection, focus, and selected state — **partial verification**. The shared helper exercises two items on every in-scope tool plus keyboard selection, but Playwright/axe were not permitted to run.
- Long content, empty/loading/error/read-only, resize, 200% zoom — **partial verification**. The spec includes long-title, all state panels, 640px, 200% zoom, axe, and screenshot assertions; runtime evidence is pending.
- Refresh/deep-link restoration and clear missing/deleted states — **partial verification**. Every tool has a page-specific `useSelectedId` key and the static contract passed; the reload/deleted-ID rendered assertions are authored but unrun.

# Decisions

- Stored raw domain IDs in URL query keys while keeping prefixed DOM/test IDs; rejected persisting presentation-only prefixes in deep links.
- Kept create, refresh, search, and status/window filters in the primitive toolbar; rejected placing global operations inside a selected item's inspector.
- Retained a deleted selected ID so the primitive shows its not-found state; rejected silently selecting another row after deletion.
- Used the primitive's built-in local search for fixture catalogs while preserving live Brain's gateway-backed search; rejected duplicating page-specific fixture search shells.
- Kept report metrics and Gallery media previews as specialized inspector content; rejected preserving their former standalone grid/detail layouts.
- Left Tasks, Profiles, `SettingsTool`, `LiveSettingsTool`, and `AutoPromotionSettings` unchanged; rejected broad cleanup in areas owned by #1514/#1523 or designated as the baseline.

# Follow-ups

- Orchestrator: run `cd apps/web && npm exec -- playwright test tests/pages/agent-tools-list-inspector.spec.ts tests/tools.spec.ts tests/secondary-tool-paths.spec.ts tests/parity-edge-cases.spec.ts` and inspect the generated per-tool, narrow, and 200%-zoom screenshots against Tasks.
- Orchestrator: run `cd apps/web && npm exec -- playwright test --config tests/bucket-a-rendered-repair-playwright.config.ts` for the updated live Gallery, Skills, and Review assertions.
- Orchestrator: run the packaged Electron target and live-backed action checks before treating criteria 4–8 as verified; fixture rendering is not production/runtime evidence.
- Verification-gate status: `BLOCKED` in this worker by the explicit no-sockets/no-Playwright/no-Electron authorization boundary; no implementation failure was observed by the permitted checks.
- The Dev Dashboard tracker was not published because this worker was explicitly forbidden from leaving or writing outside the worktree; the parent orchestrator can publish the run after rendered verification.

# Needs a human

None. The remaining work is automated orchestrator/runtime verification; no password, physical-device step, signing, or deployment is required from AJ for this worker slice.
