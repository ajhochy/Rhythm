---
date: 2026-09-24
repo: Rhythm
branch: swarm/issue-1491
pr: null
issues: [1491]
status: pending
tags: [run, Rhythm]
---

## Files
- `apps/desktop_flutter/lib/features/agents/views/agents_view.dart`: removed the redundant `setState` when the post-frame draft callback finds an occupied composer. The existing text and staged webhook draft remain intact; clearing the composer still schedules a one-time placement.
- `apps/desktop_flutter/test/features/agents/agent_trigger_watcher_test.dart`: kept the Repair 4 occupied-composer contract and let the normal 200 ms transcript scroll animation finish before asserting no further frame is scheduled. Formatted this issue-owned file.
- `docs/ai/contracts/issue-1491.json`: marked c3 passing on the full Flutter watcher suite; c1, c2, and c4 retain their prior statuses and evidence.

## Checks
- Entry: `/private/tmp/rhythm-swarm-1491`, `swarm/issue-1491`, HEAD `957c73c7`; eight pre-existing dirty issue files preserved.
- Prior contract RED: `flutter test test/features/agents/agent_trigger_watcher_test.dart --plain-name 'occupied composer keeps a webhook draft pending until empty'` failed after six 1 ms pumps (`hasScheduledFrame` true; `/private/tmp/rhythm-repair4/1491-contract-red.log`). A diagnostic run with only the product fix still failed because the normal 200 ms scroll animation remained active. With a 250 ms animation allowance, the original product code still failed the no-scheduled-frame assertion; the one-line fix passed it.
- GitNexus upstream impact, indexed older `fix/bridge-stream-reliability` branch: `_maybeConsumeComposerDraft` LOW, one direct caller (`_TranscriptPanelState.build`), one Views module and one indexed process. The index is stale relative to this worktree, so its counts are advisory.
- `/Users/ajhochhalter/development/flutter/bin/flutter test --no-pub test/features/agents/agent_trigger_watcher_test.dart` from `apps/desktop_flutter`: **18/18 passed**, including occupied draft retention, one-time consume, and scheduled-trigger compatibility.
- `/Users/ajhochhalter/development/flutter/bin/dart format --output=none --set-exit-if-changed lib/features/agents/views/agents_view.dart test/features/agents/agent_trigger_watcher_test.dart`: clean after formatting the test file, 0 changes.
- `/Users/ajhochhalter/development/flutter/bin/flutter analyze --no-pub --no-fatal-infos`: exit 0, 319 existing infos.

## Notes
- No backend/engine/sandbox/native app/installation/commit/push was run. Prior real API sandbox c4 evidence is in the 2026-09-23 run; it was not repeated for this Flutter-only callback repair.
- PostgreSQL production additive ALTER still requires manual review; PG16 was not run in the prior gate. No fresh PostgreSQL or installed desktop verification is claimed by this Repair 4 run.

## Integrated source checks

Watcher suite 18/18; full Flutter format check 525 files, 0 changed; analysis 319 infos and exit0. Combined prompt/webhook focused API tests 22/22. Fresh integrated sandbox signed webhook and existing-chat prompt live tests 2/2 (6.26s). PostgreSQL16 and installed/native qualification remain separate.
