# MEGA post-#1241: remaining open-issue sweep (stacked)

**Stacked on PR #1241** (`mega/run-2026-07-28`). Merge order: #1241 first, then this PR. Do not merge this PR before #1241.

Status: **COMPLETE — all 13 implementable issues integrated and proven; fresh-agent smoke verdict RELEASE READY; independent security review's 1 Critical + 7 Important findings remediated and re-verified live.** Awaiting human review + manual smoke per the repo working agreement. Merge order: PR #1241 first, then this PR.. This body is a living document, updated after every batch.

Closes #1076

## Exclusion set (covered by #1241)
#1208, #1213, #1214, #1215, #1216, #1217, #1218, #1220, #1221, #1222, #1223, #1232, #1233, #1234, #1236, #1239 — each named in #1241's closing keywords; diff verified.

## SCOPE (this PR)
| # | Title | Area | UI |
|---|---|---|---|
| 1240 | Flaky sandbox_foreground test | API tests | no | ✅ integrated — 3632/0 full suite on loaded machine, proof `.proof/i1240/` |
| 1209 | BM25 skill scorer + wider deferred dispatcher | API+fork | no | ✅ integrated — replay-measured swap (recall@5 0.083→1.0), threshold 0.45 after gate-caught injection regression; fork rebuilt clean; suite 3639/0 |
| 1219 | Memory provenance & lifecycle | API+DB+MCP+Flutter | yes | ✅ integrated — design-first decision record; additive migration + Postgres parity; live E2E 1/1; suite 3644/0; 2 gate-caught regressions fixed (log-write race, exponential audit snapshots) |
| 1178 | In-Rhythm privacy-safe transcript sharing | API+DB | no | ✅ integrated (backend) — sanitized immutable snapshots, fail-closed per-read authz, audit; live E2E 1/1; suite 3659/0; Flutter UI drafted follow-up |
| 1237 | iOS authoritative paired-Mac status | mobile | yes | ✅ integrated — one reachability source, bounded offline convergence; round-2 gate caught all-15-flows hang; final e2e 67/67 |
| 1238 | iOS keyboard-safe multiline composer | mobile | yes | ✅ integrated — contract 0/3→3/3 red-green, e2e 59 pass (+4 new), screenshots `.proof/i1238/ui/` |
| 1235 | iOS compact screen headers | mobile | yes | ✅ integrated — contract 0/7→7/7; full e2e 62/62; 4 gate rounds (6 shipped-flow regressions caught+fixed pre-merge; issue-1174 spec entry migrated to Chat menu — logged contract conflict) |
| 1231 | Session history sync mobile↔desktop | mobile+API | yes | ✅ integrated — ownership upsert on desktop creation; live two-surface E2E caught AGENT_LOCAL req.auth gap fixtures missed; 1175 security contracts intact |
| 1170 | Authenticated SSE + PTY gateway proxies | API gateway | no | ✅ integrated — audit-and-prove: proxies already shipped (#1165); landed the missing matrix (reconnect w/ fresh single-use ticket, reused-ticket rejection, revocation-mid-PTY 4401, resource release); live 2/2 |
| 1171 | Desktop Mobile Access flow + pairing UI | Flutter+mobile | yes | ✅ integrated — audit: 5/6 criteria pre-existing+tested; real gap fixed (QR payload leaked hostId); live E2E 1/1 |
| 1172 | Agents navigation/chats/recovery/Activity (delta vs #1232) | mobile | yes | ✅ integrated — 7/9 criteria mapped to shipped work; gaps fixed (lifecycle views incl. Completed, list states); fork + six-kind Activity proven; e2e 69/69 |
| 1173 | Dedicated Rhythm tool screens (delta vs #1234) | mobile | yes | ✅ integrated — audit-and-prove: 14 tools × 6 criteria matrix, no production gaps; pinning test 6/6 |
| 1037 | Leantime concepts exploration → decision + adoption plan | docs+Flutter | yes | ✅ integrated — decision record (verified upstream source); Kanban board shipped; flutter 1014/1014; 2 latent layout bugs fixed; follow-ups #1243–#1246 |
| 1176 | OCU-35A v2 adoption trigger evaluation | fork evidence | no | ✅ recorded — trigger NOT met (v2 create/prompt are `{} as any`); stays open/blocked per its own contract |
| 1177 | OCU-35B remote-workspace trigger evaluation | docs | no | ✅ recorded — no product trigger attached; stays deferred per its own clause |
| 1178 | see above | | |
| 1076 | OCU-35 tracking — supersession closure proof | docs | no | ✅ closes with this PR — superseded by #1176/#1177/#1178 (its only close condition) |
| 1197 | Independent review of iOS RC | review gate | no | ⛔ candidate SHA invalidated by this run; re-review targets post-merge SHA (disposition record committed) |
| 1175 | iOS aggregate gates + release prep | release gate | no | ◐ automatable aggregate gates ran green on this branch; signing tail with #1198–#1200 |
| 1198 | Signed dev build | release | no | ⛔ blocked at credential gate (eas: Not logged in — human step); read-only profile/project config verified |
| 1199 | Physical iPhone matrix | release | no | ⛔ physical device required — bounded gap per the issue's own stop clause |
| 1200 | TestFlight production | release | no | ⛔ requires #1199 green + AJ's explicit authorization |

## Batch plan

Batching rule: file/schema-disjoint within a batch; migrations serialized (one schema-toucher per batch); foundations before consumers.

- **Batch 1 (running):** #1240 (api test), #1209 (api services), #1237 (mobile providers/status), #1238 (mobile chat composer)
- **Batch 2:** #1219 (memory schema — sole migration toucher), #1235 (mobile headers — after #1238, same chat surface), #1170 (gateway PTY/SSE untested matrix), #1171 (audit-and-prove vs shipped #1165/#1239)
- **Batch 3:** #1178 (sharing schema — sole migration toucher), #1231 (session-catalog ownership asymmetry), #1172 (delta vs shipped #1232), #1037 (Leantime decision + adoption plan)
- **Batch 4:** #1173 (per-tool-id completeness delta vs #1234), OCU-35 records (#1176/#1177/#1076)
- **Batch 5 (gates):** #1197 fresh-context review, #1175 aggregate gates, #1198–#1200 automatable preflight + bounded-gap documentation
- **Phase 6:** fresh-agent black-box smoke → RELEASE READY

## Decision log

8. **GitNexus skipped for the entire run (user directive)** — sandboxes block external calls; blast radius inferred locally (grep callers / trace imports) and recorded per-issue in `.proof/i*/impact.md` / result.json.

1. **Worktrees live in-repo (`.worktrees/`)** — Codex sandboxes cannot write outside the repo root. Alternative (sibling dirs per protocol template) rejected: agents couldn't commit.
2. **GitNexus gate waived inside issue worktrees; orchestrator runs impact/detect_changes at integration.** Alternative (per-worktree 67k-symbol reindex) rejected: minutes of setup per worktree duplicating a gate the integrator already owns. First two Codex dispatches correctly fail-loudly stopped on this gate (C2-environment) and were re-dispatched with the waiver.
3. **PR #1165 is MERGED (head a9c765d02), not draft** — issues #1170–#1173 written against the draft are stale; reinterpreted as deltas on shipped code: #1170 → the untested PTY disconnect/reconnect + revocation-mid-PTY matrix; #1171 → audit-and-prove (likely already satisfied by shipped Mobile Access + pairing; close requires passing E2E per doctrine); #1172/#1173 → deltas vs shipped #1232/#1234.
4. **#1176 stays blocked** — v2 `create`/`prompt` are `return {} as any` (src/v2/session.ts:170,290); `shell`/`skill`/`compact` empty. Evidence: docs/ai/runs/2026-07-28-ocu35-trigger-evaluations.md.
5. **#1177 stays deferred** — its own product-trigger clause requires a concrete named workflow; none exists.
6. **#1076 closes as superseded** by #1176/#1177/#1178 — its only defined close condition.
7. **#1231 root cause** — desktop-created sessions have no `mobile_opencode_ownership` row, so mobile's proxy/SSE filters hide them; two disjoint catalogs (desktop: agent_sessions_routes; mobile: raw engine via gateway).

## Decision log
(accumulates)

## Phase 6 — fresh-agent verification & remediation

- **Black-box smoke (fresh context, docs-only setup): RELEASE READY, no blockers.** api 3664/0, flutter 1015/1015, mobile e2e 69/69; live journeys for memory provenance, transcript sharing, pairing/catalog sync, BM25 retrieval — all with DB-row assertions; adversarial checks fail-closed; sandbox restart clean; live ports untouched. Report: docs/ai/runs/2026-07-28-phase6-smoke.md.
- **Independent security review (fresh Codex, full diff):** 1 Critical (share DDL gated behind agentExecutionEnabled while routes mount unconditionally → prod 500) + 7 Important — ALL remediated on-branch and re-verified: server-side category derivation (caller can't misclassify), extended secret redaction (PEM/JWT/AWS/Google/cookies/DB URLs/bearer/home paths), same-workspace recipient enforcement (fail-closed 403), DB-level append-only triggers on snapshots/audit/provenance ledger, transactional ledger appends with same-source rollback validation, mutating reconcile failures surfaced as scrubbed 5xx, paired-host probe backoff (5→60s capped, resets on recovery/foreground). The 1 Minor (AGENT_LOCAL tokenless identity not an authz boundary) is the documented intentional localhost-only model (CLAUDE.md) — accepted, recorded here.
- Smoke findings fixed: malformed JSON → 400 envelope on general routes; GET /agent-memory/:id resolves POST-returned ULIDs; testing-guide documents gateway port + human-approval env.
- Final tip verification: api suite 3691/0; live share + catalog E2Es 2/2 against a fresh isolated sandbox (the hardened flows re-proven end-to-end).

## CI remediation (branch stayed red-then-green through these)
- fork typecheck: tsc/tsgo disagree on typed-EventEmitter override assignability → instance-level emit wrapper (pre-existing latent error in fadeaf1d0, exposed when #1209 first triggered the path-filtered fork job)
- Case F dispatcher contract: full-deferred sessions keep the empty-catalog dispatcher; per-server deferral materializes only with entries
- dart format: CI wanted exactly 2 files; a formatter run without .dart_tool/package_config mass-rewrote 329 files tall-style (reverted; pub get first, then format)
- generated artifacts: fork SDK + vendored api_server SDK + mobile contract manifest + pinned fingerprint regenerated for `deferredServers`
- Mobile CI "permission requests" e2e failure: 3/3 locally, CI retry issued (flake)

## Deferred follow-ups (filed)
- #1243 Goals as first-class concept · #1244 tags+priority · #1245 dopamine loop · #1246 milestones (from the #1037 decision record)
- #1247 flaky 'permission requests' e2e on CI (filed during this run; 2 CI-only occurrences, passes locally 3x)
- Flutter share UI for #1178 (draft body in .proof/i1178/followup.md — to be filed at merge)
- Mobile-created transcript hydration for #1231 (draft in .proof/i1231/followup.md — to be filed at merge)

## Migrations
- #1219 memory provenance/audit (additive SQLite + role-gated Postgres parity; rollback: columns/tables additive, ignore on downgrade)
- #1178 transcript sharing: shared_transcripts + share_audit_log (additive SQLite + Postgres, NOT gated by agentExecutionEnabled, append-only triggers; rollback: drop triggers then tables — audit rows must be exported first if retention matters)

## Manual setup / ops notes for the deployer
- No new env vars or secrets required at deploy. New Postgres DDL applies automatically via postgres_bootstrap on boot.
- After merge + hosted deploy, #1241's legacy prod scheduler-row operator procedure still applies (see #1241's checklist).
- The iOS release train (#1197–#1200) restarts from a NEW candidate SHA after both PRs merge: human steps are EAS login + signed dev build, physical-iPhone matrix, then TestFlight with AJ's explicit authorization (docs/ai/runs/2026-07-28-ios-release-gates-disposition.md).

## Manual smoke checklist (human, before merge)
1. Desktop Tasks: toggle list ↔ Board; drag a card between columns; verify status persists (#1037)
2. Desktop Memory/Brain: memory list shows lifecycle + provenance read-only detail (#1219)
3. iPhone/simulator: chat composer grows multiline above the keyboard, transcript fully scrollable (#1238); single compact header per screen with Chat menu overflow (#1235); turn Tailscale off → Settings AND Agents converge to offline, reconnect restores (#1237); Chats show All/Active/Completed/Archived views and fork works (#1172)
4. Share a transcript to a same-workspace user; verify the recipient can read it and revocation blocks them (#1178)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
