---
date: 2026-09-18
repo: Rhythm
branch: mega/fix-1524-1514-gaps
pr: null
issues: [1524, 1514]
status: implemented-local-checks-pass-rendered-unrun
tags: [run, Rhythm]
---

# Review gap repair for issues 1524 and 1514

## Files

- Added shared splitters to Tasks detail, Rhythms detail, all six Planner day boundaries, Profiles rail/editor, and the Hermes status/workspace boundary.
- Expanded the pane inventory and the table-driven rendered splitter specification across every route, tab, and Agent Tool.
- Wired Agent Settings Accounts to the existing local Anthropic account service and completed the existing MCP service controls for add, credentials, OAuth, connection, and removal.
- Kept Behavior, Keybindings, and runtime mutation as explicit unsupported states, including the HTTP endpoints each Electron capability would require.
- Added stateful rendered specifications for account and MCP edit, save, and reload behavior.

## Checks

- `cd apps/web && npm run typecheck && npm run build` — pass; 1,695 modules transformed.
- `cd apps/web && npx tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --jsx react-jsx --skipLibCheck --types node tests/splitter.spec.ts tests/pages/agent-settings-list-inspector.spec.ts` — pass.
- `cd apps/web && node --test src/components/ToolWorkspace.contract.test.mjs src/components/profilePolicy.test.mjs` — pass, 24/24.
- `git diff --check` — pass.
- Rendered Playwright, Electron, screenshots, and live-service checks were not run because this worker was prohibited from binding sockets or launching those runtimes. The specifications were written for the permitted downstream runner.

## Notes

- GitNexus reported LOW risk for the page components and live session factory. The shared `SessionGateway` interface reported CRITICAL due to broad imports, so account operations were added as optional methods to avoid breaking fixtures and unrelated consumers.
- The current worktree is not registered in GitNexus. A compare against the registered root checkout described 303 unrelated files; an exact worktree compare was rejected, so neither output is accepted as scope evidence.
- No commits, stashes, checkouts, services, or sockets were created.
