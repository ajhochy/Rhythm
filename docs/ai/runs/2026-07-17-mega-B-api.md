---
date: 2026-07-17
repo: rhythm
branch: mega/B-api
pr: none (do-not-push per task)
issues: [1042, 1044, 1045, 1049, 1050, 1057, 1058, 1060, 1063, 1064, 1065, 1066, 1070]
status: complete (spine done; live e2e deferred — sandbox not running)
tags: [run, rhythm, mega, opencode-utilization, spine-B-api]
---

# Cluster B-api — api_server hot-zone spine (mega #1042–#1108)

Worktree: `/Users/ajhochhalter/Documents/rhythm-worktrees/mega-Bapi` on `mega/B-api`.
Sole-owner hot-zone files: `opencode_client_service.ts`, `opencode_stream_bridge.ts`,
`agent_sessions_controller.ts`, `routes/agent_sessions_routes.ts`, `database/migrations.ts`
(ws_gateway.ts untouched this run — no change required).

## Per-issue status (all 13)

| # | OCU | Status | Notes |
|---|-----|--------|-------|
| 1042 | OCU-01 | done (pre-existing commit `0312e004b`) | permission replies via POST /permission/:id/reply |
| 1044 | OCU-03 | done (pre-existing commit `c99d4d418`) | rehydrate pending permissions on (re)connect |
| 1045 | OCU-04 | **done** | `getSessionStatuses()` wrapper + `reconcileSessionStatuses()` on engine-ready + resubscribe; error rows never clobbered (listActive excludes them) |
| 1049 | OCU-08 | **done** | `resolveWebsearchConfig()` (env), spawn-env injection in `initialize`, `websearchConfigured` surfaced on `/opencode/health`; key never logged |
| 1050 | OCU-09 | **done** | `rhythm_managed_commands.ts` + `opencode_commands_routes.ts` (CRUD, kebab validation, 409 collision, 400 delete-builtin, reloadConfig per mutation); `listCommands` now returns `source` |
| 1057 | OCU-16 | **done** | worktree wrappers (list/create/remove/reset) + `opencode_worktrees_routes.ts` + typed `worktree.ready`/`worktree.failed` WS relay |
| 1058 | OCU-17 | **done** | `isolateWorktree` option on POST /agent-sessions; migration `issue_1058_worktree_fields`; worktree metadata on payload; hard-delete `removeWorktree` flag (default keep). **AgentRunner half SKIPPED** — agent_runner.ts is outside this cluster's ownership; interactive-session AC covered by the controller path |
| 1060 | OCU-19 | **done** | file/find wrappers + `/:id/files/{find-text,find-files,list,content,status}` routes; path-traversal guard (400); 2MB content cap (413); unknown session 404 |
| 1063 | OCU-22 | **done (API-half)** | `getVcs`/`getVcsStatus` wrappers + `/:id/vcs` + `/:id/vcs/status` routes + `vcs.branch.updated` typed WS relay |
| 1064 | OCU-23 | **done (API-half)** | `getVcsDiff(mode)` + `getVcsDiffRaw` wrappers + `/:id/vcs/diff?mode=` + `/:id/vcs/diff/raw` (text/x-diff) routes |
| 1065 | OCU-24 | **done (API-half)** | `sessionShell()` wrapper + POST `/:id/shell` route (agent resolved from session) |
| 1066 | OCU-25 | **done (API-half)** | `sessionInit()` wrapper + POST `/:id/init` route (provider/model fall back to session) |
| 1070 | OCU-29 | **done** | consolidated single `/global/event` subscription (`subscribeToGlobalEvents` SSE reader + envelope unwrap/route by directory+sessionID), 30s heartbeat watchdog → resubscribe + rehydrate, fallback flag `RHYTHM_SSE_GLOBAL=0`; guarded behind wrapper-presence so partial mocks / older engine binaries fall back to per-directory |

## Migration keys added
- `issue_1058_worktree_fields` (marker; additive nullable columns `worktree_name`,
  `worktree_path`, `worktree_branch` on `agent_sessions`, guarded ALTERs). Local SQLite
  only — `postgres_bootstrap.ts` not needed for agent sessions (verified).

## Build
- `cd apps/api_server && npm run build` (tsc) — **green** after every step and at end.

## Tests
- New spine tests (all green, 53 tests / 10 files):
  `issue_1045_session_status_resync`, `issue_1049_websearch_config`,
  `opencode_commands_routes`, `opencode_worktrees_routes`, `issue_1058_isolate_worktree`,
  `issue_1060_file_find_proxy`, `issue_1063_1066_vcs_shell_init`, `issue_1070_global_sse`,
  plus `migrations_replay_guard` + `migrations_self_heal` (green — replay guard confirms
  the new migration is idempotent and stomps no user data).
- Full suite: `npx vitest run` → 2910 passed, 26 failed, 35 skipped.
  - Remaining failures are **pre-existing, not from this spine**:
    - `memory_*` (6 files): filesystem/env ENOENT in the Memory-Vault tests — touch
      none of the cluster files (0 references to opencode_engine/streamSession).
    - `issue_736_contract`, `issue_818_contract`: their `opencode_engine` mock lacks
      `replyToPermission` (a method added by OCU-01 `0312e004b`, an already-done commit
      before this spine). Mock gap inherited from #1042, not introduced here.
  - Regressions introduced mid-spine and FIXED before final:
    - `agents_ws_e2e`, `opc_instant_new_session`, `opc_event_stream_bridge` — the
      OCU-29 global-stream default broke tests driving the legacy per-directory path.
      Fixed by (a) guarding the global path behind `typeof subscribeToGlobalEvents ===
      'function'` (also hardens against older engine binaries), and (b) pinning the
      legacy per-directory suites to `RHYTHM_SSE_GLOBAL=0`.
    - `opencode_client_typed_wrappers` #685 `as unknown as` guard — reworded the one
      offending comment; the SSE body iterator uses a single narrow structural cast.

## Live e2e (deferred — sandbox not running)
- Written + gated behind `RHYTHM_LIVE_E2E=1`:
  `live_e2e_1057_worktree` (real temp git repo worktree create/list/reset/remove),
  `live_e2e_1070_global_sse` (`/global/event` envelope + heartbeat).
- NOT executed: the dev sandbox (api :4098 / engine :4097) was **not running** and the
  task forbids running `tools/dev/sandbox.sh up/down`. Ports 4097/4098 have no listener;
  the only live engine present is the production Rhythm.app on :4096 (:4001 api) which
  must never be touched. Run once the sandbox is up:
  `RHYTHM_LIVE_E2E=1 RHYTHM_ENGINE_URL=http://127.0.0.1:4097 npx vitest run <file>`.

## git diff --name-only (this branch vs merge-base)
See `git -C <worktree> diff --name-only c965af68a..HEAD`.

## Do NOT
- No push, no PR, no merge (per task). Hand off to verification-gate.
