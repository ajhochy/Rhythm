---
date: 2026-07-17
repo: Rhythm
branch: mega/B-api
pr: (none — compiled into mega PR by orchestrator)
issues: [1042, 1044, 1045, 1049, 1050, 1057, 1058, 1060, 1063, 1064, 1065, 1066, 1070]
status: in-progress
tags: [run, rhythm]
---

# Spine B-api — api_server backend serial spine (mega/opencode-utilization-1042-1108)

Sole owner of hot-zone files: `opencode_client_service.ts`, `opencode_stream_bridge.ts`,
`agent_sessions_controller.ts`, `routes/agent_sessions_routes.ts`, `ws_gateway.ts`,
`database/migrations.ts`. Ordered spine; each step's writes land before the next.

Sandbox already running: api_server http://127.0.0.1:4098, engine :4097 (reused; never touched live :4001).
Live e2e gated behind `RHYTHM_LIVE_E2E=1`.

## Per-issue status

| Order | Issue | Status | Notes |
|---|---|---|---|
| 1 | #1042 OCU-01 | done | replyToPermission wrapper + controller allow/always/deny + bridge auto-deny→reject; engine contract live-verified |
| 2 | #1044 OCU-03 | pending | |
| 3 | #1045 OCU-04 | pending | |
| 4 | #1049 OCU-08 | pending | |
| 5 | #1050 OCU-09 | pending | |
| 6 | #1057 OCU-16 | pending | |
| 7 | #1058 OCU-17 | pending | migration key: issue_1058_worktree_fields |
| 8 | #1060 OCU-19 | pending | |
| 9 | #1063 OCU-22 | pending | api-half |
| 10 | #1064 OCU-23 | pending | api-half |
| 11 | #1065 OCU-24 | pending | api-half |
| 12 | #1066 OCU-25 | pending | api-half |
| 13 | #1070 OCU-29 | pending | LAST — rewrites bridge |

## Migration keys added

- (pending) `issue_1058_worktree_fields`

## Checks

(recorded per step below)

## Commands + observed output

### Environment note (sandbox build ownership)
The shared sandbox api_server (pid serving :4098) runs `dist/server.js` from the
MAIN repo `/Users/ajhochhalter/Documents/Rhythm`, not this worktree, and I am
barred from sandbox up/down. So worktree TS edits are NOT live on :4098 without a
restart I cannot do. Live e2e therefore targets the **engine directly at :4097**
(which is what the engine-facing acceptance criteria actually require) plus unit +
route contract tests for the api_server layer. node_modules symlinked from the
main repo (gitignored; never committed) so `tsc`/`vitest` run in-worktree.

### Step 1 — #1042 OCU-01 (permission replies via POST /permission/:id/reply)
- Prior WIP had the wrapper + controller + bridge migration; I completed the test
  migration (opencode_stream_bridge.test.ts #878 block + opc_agent_session_routes
  PATH 5 rewritten to spy replyToPermission with allow/always/deny/message cases).
- Build: `npm run build` (tsc) → GREEN.
- Tests: `vitest run opc_agent_session_routes opencode_stream_bridge` → 36 passed.
- LIVE (engine :4097):
  - `GET /doc` paths include `/permission/{requestID}/reply` (operationId permission.reply) with body schema `{reply: once|always|reject, message?: string}` — EXACTLY matches the wrapper contract.
  - `POST /permission/nonexistent-req-id/reply {reply:once}` → HTTP 400 validation (route WIRED, not a 404 route-miss ⇒ deprecated fallback correctly never triggers on this engine).
  - `GET /permission` → `[]` HTTP 200 (rehydration source for #1044 confirmed live).
