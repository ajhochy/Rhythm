---
date: 2026-09-19
repo: Rhythm
branch: mega/2026-09-18-mobile-electron-hermes
pr: 1544
issues: [1373, 1510, 1520, 1175]
status: partial
tags: [run, Rhythm]
---

# Device and release readiness preflight

## Files

- This note records the initial read-only preflight and the later authorized local Electron qualification preparation. This agent did not sign, install, launch, deploy, authenticate, or interact with a device or app. Protected local API/engine services were untouched.

## Decisions

- Keep physical audio (#1510), installed icon (#1520), off-LAN relay (#1373), and TestFlight/device acceptance open. A paired device listing, public health response, green CI, local bundle, or older release cannot qualify the current draft source.
- Use the exact PR source SHA, a recorded signed artifact hash/build ID, and a non-sensitive device model label for physical checks. Do not put device identifiers, credentials, pairing codes, or tokens in evidence.
- Inspect the deployed API and relay separately. The public API health response identifies its commit; the relay health response does not identify an image revision. `macOnline=false` also prevents an end-to-end off-LAN assertion. The LAN-first uplink topology in the runbook is a configuration plan, not observed proof of a cellular phone route.
- Gate production EAS auto-submit behind the full signed-development-build physical matrix and AJ's explicit release authorization in `docs/testing/manual-smoke.md:350-493`. Do not infer that available local Developer ID certificates prove EAS remote Apple credentials or notarization credentials.

## Checks and evidence (2026-09-19 15:03 UTC)

- Current checkout/PR: `git rev-parse HEAD` → `5faab5899688300c1006ffc0efdb894044c49c97`; `gh pr view 1544 --json ...` → open draft, CLEAN, same head, base `648f8d5885b1767bcb53a95c7e8aa7eadce2c5f2`. Three PR checks at this head were SUCCESS: Mobile CI foundation, Server CI server-checks, and live-postgres-bootstrap. These are code checks, not release artifacts.
- Auth-free `curl https://api.vcrcapps.com/health` → HTTP 200, `status=ok`, `commit=b804842a2be70b6d7b8c5fffa2ed1dabcd99bcd4`, built `2026-09-16T21:42:13Z`. This is behind PR #1544 and the current `main` SHA; no current-branch production deployment is observed. `curl https://api.vcrcapps.com/relay/health` → HTTP 200, `status=ok`, `role=relay`, `macOnline=false`, no commit field. The public path reaches a relay-role process, but neither its exact image nor off-LAN mobile SSE/PTY/reconnect behavior is established. The route's LAN-first Mac uplink and Cloudflare `/relay*` path are documented in `docs/release/hosted_deployment_synology_cloudflare.md:227-281`.
- Local device inventory: `xcrun devicectl list devices` lists an available, paired iPhone 13 mini and iPad (10th generation). This does not establish physical attachment, app installation, account sign-in, network path, or audible behavior. `xcrun simctl list devices booted` listed no booted simulator. Xcode 26.5 is installed. No device identifier is recorded here.
- `security find-identity -v -p codesigning` listed five valid identities, including Developer ID Application and Apple Development. No certificate private key, password, EAS login, App Store Connect session, or notarization credential was inspected. The repo's `apps/mobile/eas.json` pins EAS CLI 21.2.0, remote production credentials, and the App Store Connect app ID; `release-preflight-ios.mjs` still requires authenticated EAS identity/project and the approved cloud origin. Their current authenticated state is unverified.
- `apps/electron/dist/Rhythm.app` is a 574 MB local candidate, modified September 18 22:55 PDT, bundle `com.rhythm.desktop` version `0.1.0`, with `Rhythm.icns`. `codesign --verify --deep --strict` exited 0, but signature is ad hoc with no team; `spctl --assess --type execute` exited 3 (rejected), `xcrun stapler validate` exited 65 (no ticket). It is not a signed/notarized release artifact or proven exact-current-source package. Existing Finder/Get Info screenshots in `.proof/mega-2026-09-18/` qualify only those two icon surfaces for that staged package.
- `/Applications/Rhythm.app` is the shipping Flutter bundle (`org.visaliacrc.rhythm`, version 154), modified September 16, with `AppIcon.icns` and Developer ID authority. Strict `codesign --verify` exited 1 with a sealed-resource error; `spctl` exited 1 and stapler exited 65. This installed copy cannot be used as a valid signed-package acceptance sample without investigating its seal. No installed app was opened or modified.
- Latest Electron Release workflow success is August 27 at source `0bc46a5ece1a937c484c0054493c75b0299eafef`; latest Electron GitHub release is `electron-v0.18.63` with `Rhythm.zip` SHA-256 `bd2016462bf2bb05bdc1371163f165b03be5ddd0c46107e219da3d7ca030e243`. Its Actions artifact is expired, while the GitHub release asset remains listed. It predates this PR and is not current-branch qualification. The latest Flutter release `v0.18.64` is also September 16, not this branch.
- `docs/ai/evidence/issue-1175-release.json` records an older tested head `6dd2516f...`; its signed development build, physical device, production build, and TestFlight fields are all pending. The September 13 local iOS recovery candidate proved only its exact installed recovery criterion, not the current PR's physical matrix. No current-source IPA or `.xcarchive` was found under `apps/mobile`.

## Gated execution plan for the parent

1. **Release provenance and deployment readback.** After an authorized merge/image publication/deployment, compare the expected merged SHA with `curl -fsS https://api.vcrcapps.com/health | jq '{status,commit,builtAt}'`. The NAS operator must read the actual `rhythm-api` and `rhythm-relay` image digests/tags and verify the relay path rule, uplink environment, and data-volume checksum procedure from `docs/release/hosted_deployment_synology_cloudflare.md`; the public relay response cannot supply its revision. Recheck `curl -fsS https://api.vcrcapps.com/relay/health | jq '{status,role,macOnline}'` only as a readiness probe.
2. **Off-LAN relay on an authenticated physical phone.** With the intended paired Mac online and an approved account/device, place the iPhone on cellular with Wi-Fi and Tailscale disconnected. Confirm the paired transport reports the canonical HTTPS relay URL, then perform prompt/SSE, PTY input/output, reconnect to the same session after an uplink interruption, second-user/wrong-project denial, device revocation, and recovery without duplicate turns. Record method/status/timing/session marker, build ID, and device model only. The existing `apps/api_server/src/__tests__/issue_1373_relay_pty_live.test.ts` passed against a local synthetic sandbox; it does not substitute for this hosted phone check. Do not use production rows for destructive smoke.
3. **Physical #1510 audio.** Use a current-source signed development iPhone build and the actual Electron candidate, identify which physical device/process emits sound, and record foreground/background, voice mode, working-sound preference, native notification setting, and exact build. Audibly exercise a long text turn, tool activity, mid-turn disable, cancellation, error/recovery, session switch, intended approval/completion sounds, and a second turn after relaunch. The existing mobile contract uses fake audio and the Electron contract uses fake notifications, so both remain insufficient for audible acceptance.
4. **Signed candidate and icon.** For a current-source Electron release candidate, run the repository's `apps/electron` `npm run package:mac` then `npm run sign:mac` only in a release environment with the required Apple credentials, or use the `Electron Release` workflow. Record source SHA and ZIP SHA-256. On the exact signed/stapled bundle, run `codesign --verify --deep --strict --verbose=2 <Rhythm.app>`, `spctl --assess --type execute --verbose <Rhythm.app>`, and `xcrun stapler validate <Rhythm.app>`. Install in a controlled candidate location without replacing the shipping app, then inspect Finder, Dock, Command-Tab, and Get Info at normal/Retina size. Do not modify the bundle after signing. The current staged ad hoc bundle is useful only for unsigned visual checks.
5. **iOS/TestFlight.** First record exact source SHA and signed development build ID/hash. Run `cd apps/mobile && npm run release:preflight:ios` in an authenticated secure EAS environment with `EXPO_PUBLIC_RHYTHM_CLOUD_URL=https://api.vcrcapps.com`, then `npm run eas:development:ios` and the physical matrix in `docs/testing/manual-smoke.md:350-493`. Only after that matrix passes and AJ explicitly authorizes release, run `npm run eas:production:ios`; this script already auto-submits with the pinned production profile, so do not separately submit `--latest` unless the auto-submit demonstrably did not occur. Read back EAS/App Store Connect build and submission IDs and match the submitted artifact hash to the recorded source and production artifact. Do not use the older #1175 evidence file as current-source proof.

## Missing inputs and remaining limits

- A current-source signed development iOS artifact, verified EAS project/login and remote signing availability, intended Apple test account, and physical operator for the iPhone matrix.
- A deployed API/relay revision matching the intended branch after authorized release, NAS image-digest readback, active Mac uplink, approved paired credentials, and a cellular/off-LAN test path. Public relay health alone cannot verify the NAS image or phone path.
- A current-source Developer ID signed/notarized Electron package, notarization credentials or CI release authorization, candidate install location, and human icon/audio observation. The existing local and installed bundles cannot close signed-package acceptance.
- No credentials were fetched or printed, no hosted rows changed, and no production/deployment workflow was dispatched in this preflight.

## Local qualification preparation (later September 19)

- Parent confirmed the shipping Flutter services were restored and must remain untouched. `:4001` and `:4096` were not probed, restarted, or adopted in this preparation.
- Key-name/presence check found local `APPLE_SIGNING_IDENTITY`, `APPLE_TEAM_ID`, `APPLE_ID`, `APPLE_APP_SPECIFIC_PASSWORD`, `APPLE_CERTIFICATE_BASE64`, and `APPLE_CERTIFICATE_PASSWORD` absent. The current Electron signer calls `notarytool` with explicit Apple ID/password/team variables; the repo contains no documented `--keychain-profile` path. Local Developer ID identities are available, but no supported local notarization credential source was established. GitHub workflow references the same repository secret names plus `GOOGLE_DESKTOP_CLIENT_ID`; their values/presence were not inspected.
- The installed Flutter framework contained exactly one **unique** public desktop OAuth client ID (two byte occurrences). A local Electron package process can receive it in a child-only `GOOGLE_DESKTOP_CLIENT_ID` environment; the value is neither printed nor written into this note. The package intentionally embeds this public ID in its generated build config.
- Read-only EAS check: `eas whoami` with pinned CLI 21.2.0/`CI=1` exited 0 (identity suppressed). `eas project:info` initially exited 1 because this shell omitted `EXPO_PUBLIC_RHYTHM_CLOUD_URL`, which production `app.config.ts` requires. With only the public `EXPO_PUBLIC_RHYTHM_CLOUD_URL=https://api.vcrcapps.com`, it exited 0 and matched the repository's configured EAS project ID. This proves current account/project lookup, not availability of remote Apple signing credentials or an approved production build.
- Read-only iPhone app inventory through `xcrun devicectl device info apps --bundle-id org.visaliacrc.rhythm.agents` failed before listing apps: the paired iPhone was locked, so the developer disk image could not mount (`kAMDMobileImageMounterDeviceLocked`). No installed app ID/version is claimed; the device was not unlocked, launched, installed to, or retried.
- A `npm run package:mac` attempt began at HEAD `5faab5899688300c1006ffc0efdb894044c49c97` after parent opened the shared-dist window, with dirty API source hashes `messages_controller.ts=f5151f53...`, `messages_repository.ts=a2989a2c...`, `messages_routes.ts=572dcb82...`. It reached detached-payload native-module compilation after copying API dist. A reviewer then found a malformed DELETE ID guard that changed the API source. The owned attempt was stopped with exit 130 before producing a current candidate; no package or node-gyp child remained. A fresh formal package build is required after the reviewer's final API build, and its postbuild source hashes must be recorded. Do not qualify the interrupted output.

### Electron Release workflow: qualification-only branch dispatch

The active `.github/workflows/electron_release.yml` is already registered on `main` with `workflow_dispatch` and is viewable at the draft branch. It builds, tests, signs, notarizes, staples, verifies, smokes, and uploads per-architecture Actions artifacts, then previously published `electron-v<version>` unconditionally through `softprops/action-gh-release`. The authorized workflow edit adds optional Boolean `qualification_only` defaulting to `false` and `if: ${{ !inputs.qualification_only && github.ref == 'refs/heads/main' }}` to the final **Publish GitHub release** step. With `qualification_only=true`, the earlier signing and artifact-upload steps remain, but this job cannot create a release/tag. The branch guard also prevents an accidental published release from a branch if the input is omitted. The workflow was edited locally, not dispatched. `git diff --check` passed; an `actionlint` attempt was interrupted after more than two minutes without output (exit 130), so a completed actionlint result is not claimed.

The packager accepts only one to three numeric version components (`RELEASE_VERSION` regex in `apps/electron/scripts/package-mac.mjs:126-128`), so a qualification run still needs a numeric candidate version. After the workflow edit and intended source are committed/pushed, an authorized operator can dispatch the branch version with `gh workflow run electron_release.yml --ref mega/2026-09-18-mobile-electron-hermes -f version=<numeric-version> -f qualification_only=true`; then verify the returned run's `headSha` equals the intended branch commit and download/verify both Actions artifact hashes. Uncommitted local API edits will not be in that CI run. `gh workflow run --help` confirms `--ref`; [GitHub's manual-run documentation](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/manually-run-a-workflow) states the dispatch workflow must exist on the default branch and `--ref` selects another branch. The default-branch requirement is satisfied for the existing workflow; the branch's new input must be committed remotely before dispatch. The API's acceptance of the new branch-only input remains to be verified by an actual authorized dispatch.

### Local Electron package, startup, and scope isolation

- A fresh local `npm run package:mac` completed (exit 0) on September 19 at 08:22 PDT after the final API source/build. It used the public desktop OAuth client ID found in the installed Flutter binary through a child-only environment variable; the value is not recorded here. The resulting unsigned candidate is `apps/electron/dist/Rhythm.app`. Its bundled `app/src/main.mjs` SHA-256 matched source at `de28c7fda8ab8660e392b5eb1b2e17362605fb46aa65ba55c96c88511052c4ba`; bundled `app/package.json` matched source at `52bf9e0e9f2bb2a9d62f1dbf68e3609a9ce1140178e17ba82c4760f25de412d8`. Bundled API controller, repository, and route compiled hashes matched the final reviewed build: `27a5b40221396ca98f2da6dd22295117341ed99d78ed38b2dc2cf5b5e54f34e0`, `82a13a708f1879942ba7b27b17cd79a9426f267115319e9860ac7e7591936e2c`, and `fa41b0c10888b8c25ce7b43126fd70c46c7a00a1697a085ef6316b30d802660b`. This was a dirty-tree package, not a final committed or signed artifact.
- Parent's authorized native launch reached the Google sign-in window after a one-line startup repair: `restoreAuthentication` now checks whether `auth-session.bin` exists before consulting Electron `safeStorage`. The prior candidate stalled in `SecKeychainFindGenericPassword` on a fresh userData directory. A red regression observed one Keychain availability call for an absent file; after the change, the focused auth-boundary file passed 12/12, including proof that an existing encrypted session still uses safeStorage and restores. GitNexus exact impact on `restoreAuthentication`: LOW, one startup caller, zero indexed processes. This regression was added to the required `apps/electron/package.json` `npm test` manifest.
- Parent stopped at Google consent after an existing account's screen displayed Gmail read/send and full calendar edit/delete. The desktop login URL declares only `openid`, `email`, `profile`, and `calendar.readonly`, but has `include_granted_scopes=true`. [Google's OAuth documentation](https://developers.google.com/identity/protocols/oauth2/web-server) says that setting combines prior grants into the new access token. A test-first local change to `false` passed the URL test, but was **reverted before handoff** when source review found that the deployed desktop-exchange endpoint would store a narrower login token over existing broad integration credentials. Parent used the original consent flow to restore the affected hosted Google Calendar and Gmail connection state, with no email/calendar content mutation; the non-sensitive recovery receipt is `.proof/mega-2026-09-18/google-integration-recovery.json`. No further provider OAuth should run until the additive login-only endpoint is deployed and capability-checked.
- The final Electron routine sign-in probes the new API capability before generating PKCE state, opening a loopback listener, or launching Google. It fails closed with an actionable error on an old API. Only after the probe succeeds does it request `openid email profile` with `include_granted_scopes=false`, without offline access or forced consent, and exchange at the login-only endpoint. A red regression first proved the old implementation reached the listener before discovering a 404; the final focused Electron auth tests passed 20/20. Existing integrations remain separately owned. The native session storage still hits the system Keychain approval boundary, and no native OAuth was attempted against this new source.

### Additive desktop login-only API (local, not deployed)

- Source review found `AuthController.googleDesktopExchange` always called `storeDesktopIntegration`, and `upsertGoogleAccountAsync` overwrote both Google Calendar and Gmail access-token/scope/expiry fields while keeping only the old refresh token. A successful identity-only login could therefore leave a credential row with a broad refresh token paired to narrow access scope. Three regressions first failed: existing broad rows were overwritten, identity-only tokens created false connected rows, and the proposed capability endpoint returned 404.
- The local API now exposes `GET /auth/google/desktop-login-capability` returning `{loginOnlyDesktopExchange:true}` with `Cache-Control: no-store`, and `POST /auth/google/desktop-login-exchange` accepting the legacy desktop code/PKCE body and returning the same session shape without storing integrations. The legacy desktop exchange preserves existing provider rows through insert-only, conflict-safe writes; it initializes only the provider rows for actual returned Calendar/Gmail scopes and creates none for identity-only scopes. Explicit integration/agent OAuth still uses the normal upsert path. GitNexus upstream impacts were LOW for `storeDesktopIntegration`, the repository upsert methods, the existing desktop controller, and the auth routes (at most two indexed modules, zero indexed processes).
- Focused API route/preservation test `npx vitest run src/__tests__/google_desktop_exchange.test.ts --maxWorkers=1` passed 9/9, zero failures, including an HTTP missing-body 400 and a broad-scoped token that creates no rows on the login-only endpoint; `npm run build` passed; `git diff --check` passed. GitNexus exact impacts for the new Electron flow were LOW: the OAuth runner and URL/exchange helpers each have at most two indexed upstream callers and zero processes. The public deployed API remains older; Hermes and Electron both fail closed when its capability check sees 404 until the new API is deployed. No hosted OAuth or production integration write was used to validate this local source.

### Final local gates and package receipt

- API full `cd apps/api_server && npx vitest run --maxWorkers=2` exited 1 with **6,185 passed, 251 skipped, one failure** across 792 files. The sole failure was an unrelated `pty_proxy.test.ts` WebSocket case timing out at 15 seconds with an `ECONNRESET` during concurrent verification; isolated `npx vitest run src/__tests__/pty_proxy.test.ts --maxWorkers=1` immediately passed 2/2 in 819 ms. This is classified as an observed full-suite timing flake, not a full-suite green result. The bounded duplicate full rerun was stopped at the parent's direction before completion. Full-suite receipt is `/tmp/rhythm-google-login-api-vitest-20260919.log` (local, disposable).
- The new gated live `RHYTHM_LIVE_E2E=1 RHYTHM_LIVE_E2E_ISOLATED=1 RHYTHM_LIVE_URL=http://127.0.0.1:4198 npx vitest run src/__tests__/google_desktop_login_live.test.ts --maxWorkers=1` passed 1/1 against the real isolated API/engine, asserting engine ready, capability 200/body/no-store, and malformed exchange 400. `tools/dev/sandbox.sh up --foreground` used a generated synthetic, read-only SQLite/config fixture under `/private/tmp`, with API 4198, engine 4197, gateway 4299 and no relay. `tools/dev/sandbox.sh down` exited 0, removed the sandbox, and preserved sanitized diagnostics at `/private/tmp/rhythm-google-login-sandbox-20260919.evidence.xdmNQD`; the synthetic source fixture was then removed and reserved ports were free. This live gate does not perform an actual Google provider exchange or prove production deployment.
- Final Electron `cd apps/electron && npm run typecheck` exited 0 and required `npm test` passed **176/176**, zero failures; the Keychain regression is in the required manifest. Local `npm run package:mac` exited 0 with a single unique public desktop client ID injected only into the child process. The ad-hoc candidate is `apps/electron/dist/Rhythm.app`, based on checkout HEAD `5faab5899688300c1006ffc0efdb894044c49c97` plus uncommitted source. Source-to-bundle SHA-256 matches: Electron `main.mjs` `de28c7fda8ab8660e392b5eb1b2e17362605fb46aa65ba55c96c88511052c4ba`, `google-oauth-core.mjs` `41d6498da421ae0d1926fd1b0aee90a709a23f8d9aba666681c6fbd89125a6b3`, `desktop-google-oauth.mjs` `ed4d2f87cc8d4db2103a325f945e07a6c983b097eedb4ca6f67b787f34092e36`, and package manifest `52bf9e0e9f2bb2a9d62f1dbf68e3609a9ce1140178e17ba82c4760f25de412d8`; API auth controller `31b58c762d023427ac49f0ff62382d040ff761593f260d3107d3669180656870`, auth routes `dfb8320e0e04ef60a8b530cbdc11f57aab32a7a9c6dbea434ff76c0935431240`, Google service `202bf36b966df9db2d4d13fdc17e1b7c9e78a846be5e0a9ab4f1b403ebd5df81`, and integration repository `20eb74509d2365a27052eb5de919e99f341fa297fb7ddd7b07d9cedbeb54d0d3`. `codesign --verify --deep --strict` exited 0 for the ad-hoc seal; `spctl --assess --type execute` exited 3, so this is not the requested signed/notarized qualification artifact.
- Parent ran `actionlint` on the edited Electron Release workflow and reported exit 0 in 0.45 seconds. Read-only `gh secret list` names check found the referenced Apple and public OAuth client-ID secret names configured; no secret values were read. The first qualification-only CI dispatch and its fork-lock failure are recorded below; a repaired rerun is pending.

### Qualification CI fork-lock repair

- The first qualification-only [Electron Release run](https://github.com/ajhochy/Rhythm/actions/runs/35452996081) at commit `aa07b4916709ffc1bd72502211dc0c98ef18efe1` failed in both macOS architecture jobs at `bun install --frozen-lockfile`, before signing or artifact upload. Both Bun 1.3.13 logs reported `lockfile had changes, but lockfile is frozen`. No release/tag was published.
- In a clean tracked-file snapshot, official Bun 1.3.13 reproduced the same failure. A lockfile-only resolution changed exactly one package: `ghostty-web` from locked commit `83c0a07` to current upstream `main` commit `6e24d04`. The manifest depended on the floating `github:anomalyco/ghostty-web#main`, while the checked-in lock held `83c0a07b8628b748aed073b232cb4b52a6ca11c1`. Both commits were verified through the public GitHub repository. The repair pins the manifest and workspace lock entry to the **existing locked commit**, preserving its integrity and dependency bytes; the resolved package entry and checksum are unchanged. The workflow's frozen-install requirement is unchanged.
- After that two-line repair, `bun install --frozen-lockfile` passed in a clean ARM snapshot using Bun 1.3.13, including install scripts; a clean x64/Rosetta snapshot passed `bun install --frozen-lockfile --ignore-scripts` using Bun 1.3.13 x64 baseline. The repo lockfile byte-matched both post-install snapshot locks. `git diff --check` passed. GitNexus file impact for `apps/opencode_fork/packages/app/package.json` was LOW with zero indexed upstream symbols. This is local evidence; the parent owns commit, push, and a new qualification CI run before signed-package acceptance.

### Attempted signed iOS development build

At source `aa07b4916709ffc1bd72502211dc0c98ef18efe1`, the supported development command `CI=1 EXPO_PUBLIC_RHYTHM_CLOUD_URL=https://api.vcrcapps.com npm run eas:development:ios -- --freeze-credentials --no-wait` reached EAS remote credential resolution, then exited1 before scheduling: “EAS CLI couldn't find any credentials suitable for internal distribution” in noninteractive mode. **No build ID was issued.** Existing project/login access is therefore insufficient; compatible internal-distribution signing/provisioning must be configured before a current signed device artifact can be produced. Credentials/profiles were not changed, no device unlocked/installed, and no TestFlight submission occurred. Output was sanitized; account/device identifiers were not retained.
