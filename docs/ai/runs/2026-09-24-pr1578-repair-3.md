---
date: 2026-09-24
repo: Rhythm
branch: swarm/pr-1578-review
issues: [1577]
status: partial
tags: [run, rhythm]
---

# PR #1578 repair attempt 3

## Defects repaired

- D1: `AgentSessionsController.prompt` passes the target row's stored
  `profileId` through a server-only gateway context. `handleInputFrame` uses
  that value for profile scope while preserving the stored OpenCode engine
  identity. Request-supplied `agent` remains a 400, and a null stored profile
  retains the prior `agentKind` fallback.
- D2: c9 stores distinct `profileId` and `opencodeAgentId` values, asserts the
  restricted profile's empty MCP/skill scopes, and asserts dispatch retains
  the exact stored OpenCode agent id and SDK session.
- D3: `settle()` returns a typed `legacy_terminal_outcome` conflict for a
  legacy accepted/errored row with no outcomes-table entry. It inserts no
  contradictory outcome, so `listForSession` keeps reporting the legacy value.

## Files

- `apps/api_server/src/controllers/agent_sessions_controller.ts`
- `apps/api_server/src/services/ws_gateway.ts`
- `apps/api_server/src/repositories/agent_prompt_injections_repository.ts`
- `apps/api_server/src/__tests__/issue_1577_prompt_existing_session.test.ts`
- `apps/api_server/src/__tests__/issue_1577_audit_legacy_outcome.test.ts`
- `docs/ai/contracts/issue-1577.json`
- `docs/ai/runs/2026-09-24-pr1578-repair-3.md`

## Checks

- Launch discipline:
  `pwd && git rev-parse --abbrev-ref HEAD && git rev-parse --short HEAD`
  -> `/private/tmp/rhythm-swarm-pr1578`, `swarm/pr-1578-review`, `5e0e93ae`.
- Focused repaired behavior:
  `npx vitest run src/__tests__/issue_1577_prompt_existing_session.test.ts src/__tests__/issue_1577_audit_legacy_outcome.test.ts`
  -> 2 files passed, 12 tests passed.
- Required issue group, final run:
  `npx vitest run src/__tests__/issue_1577_prompt_existing_session.test.ts src/__tests__/issue_1577_agent_local_auth.test.ts src/__tests__/issue_1577_audit_legacy_outcome.test.ts`
  -> 3 files passed, 13 tests passed.
- Required wildcard group:
  `npx vitest run src/__tests__/ws_gateway*.test.ts src/__tests__/agent_sessions_controller*.test.ts`
  -> exit 1 before Vitest; zsh reported
  `no matches found: src/__tests__/ws_gateway*.test.ts`. A direct file
  inventory found 0 files matching either requested prefix, so this command
  ran 0 test files.
- TypeScript:
  `npx tsc --noEmit -p tsconfig.json` -> exit 0, no diagnostics.
- Static hygiene: `git diff --check` -> exit 0;
  `node -e "JSON.parse(require('fs').readFileSync('docs/ai/contracts/issue-1577.json','utf8'))"`
  -> exit 0.
- GitNexus upstream impact before edits: `AgentSessionsController` LOW (4
  direct, 17 total); `handleInputFrame` LOW (2 direct, 3 total). The indexed
  graph predates `AgentPromptInjectionsRepository`, so its method risk was
  UNKNOWN. Final `detect-changes` could not target this unindexed temporary
  worktree; the explicit attempt returned `Repository
  "/private/tmp/rhythm-swarm-pr1578" not found`.

## c9 falsification proof

After strengthening c9, the D1 controller handoff was temporarily removed and
only c9 was run:

`npx vitest run src/__tests__/issue_1577_prompt_existing_session.test.ts -t issue-1577-c9`

The expected pre-fix run failed 1/1 at
`src/__tests__/issue_1577_prompt_existing_session.test.ts:224`: the gateway
mock received `null` for the MCP allowlist instead of an object whose
`role` was `restricted-target` and whose `mcpServers` was `{}`. The D1 handoff
was restored, and the final required issue group passed 13/13.

## Not done

- No sandbox, api_server, OpenCode engine, Flutter, Electron, live E2E,
  packaged, native, or production verification was run. Contract c7 and its
  resumable-reattach, signed-malicious-override, and live skill-use
  sub-scenarios remain `UNVERIFIED`.
- No package install/update, package or lockfile edit, git-state mutation,
  commit, push, PR mutation, or Dev Dashboard publication was performed.
- No substitute was run for the unmatched wildcard suite.
