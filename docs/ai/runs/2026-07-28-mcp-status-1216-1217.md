---
date: 2026-07-28
repo: Rhythm
branch: mega/mcp-status-1216-1217
pr: null
issues: [1216, 1217]
status: blocked
tags: [run, Rhythm]
---

# MCP preflight and doctor live status

## Files

- Expanded `AgentRunner` required-MCP preflight for failed, disabled, missing,
  and needs-auth states with state-specific remediation.
- Added a bounded, secret-minimizing live `/opencode/mcp` reader for
  `rhythm doctor`, with an explicitly labeled config-only fallback.
- Added issue contracts, focused regression tests, and a gated sandbox live
  test that drives scheduler `trigger-now` plus the built doctor CLI.

## Checks

- Failing first:
  `cd apps/api_server && npx vitest run
  src/__tests__/issue_1216_mcp_preflight.test.ts
  src/cli/issue_1217_doctor_live_mcp.test.ts --no-file-parallelism`
  — 6 failed, 2 passed before implementation.
- Focused:
  `npx vitest run src/__tests__/issue_1216_mcp_preflight.test.ts
  src/__tests__/issue_892_mcp_preflight.test.ts
  src/cli/issue_1217_doctor_live_mcp.test.ts src/cli/doctor.test.ts
  src/__tests__/live_e2e_1216_1217_mcp_status.test.ts
  --no-file-parallelism`
  — 16 passed, 2 live tests skipped behind `RHYTHM_LIVE_E2E`.
- `apps/api_server/node_modules/.bin/tsc --noEmit` — passed.
- `cd apps/api_server && npm run build` — passed.
- `ai-workflow checks --level issue` — api_server and mcp_server TypeScript
  passed; Flutter analyze/format could not write the external Flutter SDK
  cache (`Operation not permitted`).
- `cd apps/api_server && npm test` — attempted; unrelated real-server suites
  consistently failed/timed out because loopback `listen` is denied by the
  managed environment. The run was stopped after the systemic failure was
  isolated as `EPERM listen`.

## Live verification

- Required command:
  `cd apps/opencode_fork/packages/opencode && bun run build --single`
  failed because `@opentui/solid/preload` was not installed.
- Attempted dependency recovery:
  `cd apps/opencode_fork && bun install` failed because Bun could not create
  temporary install files under the managed permission profile.
- Lowest-layer reproduction:
  `node -e "require('net').createServer().listen(4112,'127.0.0.1')"`
  produced `EPERM listen`.
- The dev sandbox and live test therefore could not start. No production
  endpoint, real user database, or ports 4000/4001 were touched.
- Built CLI fallback evidence:
  `RHYTHM_API_URL=http://127.0.0.1:4113
  RHYTHM_DOCTOR_MCP_TIMEOUT_MS=100 node dist/cli/index.js doctor`
  rendered `MCP status (config-only fallback)` and completed without exposing
  credential values.

## Notes

- `gh issue view 1216 --comments` and `gh issue view 1217 --comments` were
  attempted read-only but blocked by restricted network access. Contracts use
  the mission text supplied by the user.
- GitNexus query/impact wrapper calls returned no output; no risk rating was
  available. The edit remains restricted to AgentRunner preflight and doctor.
- Verification-gate status: FAIL. Failure-triage status: BLOCKED — rerun in an
  environment that permits Flutter SDK cache writes, Bun dependency install,
  and loopback listeners.
