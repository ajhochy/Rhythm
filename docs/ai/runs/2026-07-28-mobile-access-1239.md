---
date: 2026-07-28
repo: Rhythm
branch: mega/mobile-access-1239
pr: null
issues: [1239]
status: blocked
tags: [run, Rhythm]
---

# Issue #1239 — persistent Mobile Access Settings destination

## Files

- `apps/desktop_flutter/lib/features/settings/views/settings_view.dart`
- `apps/desktop_flutter/lib/features/agents/views/mobile_access_dialog.dart`
- `apps/desktop_flutter/test/features/settings/issue_1239_mobile_access_destination_contract_test.dart`
- `docs/ai/contracts/issue-1239.json`

## Checks

- Failing-first baseline source contract:
  `node` assertions against
  `git show HEAD:apps/desktop_flutter/lib/features/settings/views/settings_view.dart`
  — FAIL as expected, 3/3 required markers absent, exit 1.
- `flutter test test/features/settings/issue_1239_mobile_access_destination_contract_test.dart`
  — BLOCKED before execution because the managed sandbox denies Flutter's
  `127.0.0.1:0` test-runner socket bind (`OS Error: Operation not permitted`).
- Writable Flutter SDK clone, `flutter analyze --no-pub --no-fatal-infos`
  — PASS, exit 0; 272 pre-existing infos.
- `dart format . --output=none --set-exit-if-changed`
  — PASS, 435 files checked, 0 changed.
- `cd apps/api_server && npm run build`
  — PASS, TypeScript build and advisory copy completed.
- `cd apps/opencode_fork/packages/opencode && bun run build --single`
  — BLOCKED: missing `@opentui/solid/preload`.
- `cd apps/opencode_fork && bun install --frozen-lockfile`
  with writable temp/cache — BLOCKED by restricted network; package manifests
  could not be resolved.
- `ai-workflow checks --level issue`
  — BLOCKED because its Flutter invocation encounters the same managed SDK /
  test-runner restrictions.

## Notes

- No production URL, installed Rhythm database, or ports 4000/4001 were used.
- The isolated dev sandbox was not started: `tools/dev/sandbox.sh up` always
  rebuilds the fork, and the required fork dependencies could not be installed.
- No commit was created because live verification is a stated pre-commit gate.
- Remaining orchestrator smoke: run the widget suite and issue-level checks in
  an unrestricted shell, start the isolated sandbox on API 4123 / engine 4124
  with a throwaway SQLite database, exercise QR pairing/list/revoke endpoints,
  prove the revoked credential receives 401, then visually confirm the same
  Settings destination while the Agent Server is starting, failed, and ready.
