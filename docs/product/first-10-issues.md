# First 10 Issues (Prioritized)

1. Define initial persisted schema for core domain models (Task, RecurringTaskRule, WeeklyPlan).
2. Implement recurrence service for weekly/monthly/annual task instance generation.
3. Implement project template + offset step generation pipeline.
4. Build desktop app shell with true three-pane planner layout and responsive resizing.
5. Build weekly planner read model endpoint on API server.
6. Add local storage repository implementations (desktop) for tasks and weekly plans.
7. Implement Google Calendar shadow-event ingestion and normalization pipeline.
8. Add integration status screen with sync timestamps and connection state.
9. Add end-to-end tests for weekly plan generation across mixed input sources.
10. Add ADR + API contract update process automation in CI.
