# Smoketest: Agent Session Start & Output UX

Baseline: `state.plan` and `state.plan.coherenceRules`. Evidence is taken from the implemented diff against `origin/main`, targeted CLI checks, and targeted unit/widget tests.

| Status | Item | Expected | Evidence |
| --- | --- | --- | --- |
| Success | Bug 1 canonical session create | `POST /agent-sessions` accepts canonical IDs (`claude-code`, `codex`, `gemini-cli`, `opencode`) through the existing server config contract without changing `/agents/capabilities` or error middleware. | Diff keeps validation in `agent_sessions_controller.ts` against `AgentConfigsRepository.getById(normalizedAgentId)` and does not touch capabilities or error middleware. `npm test -- agent_sessions` passed after rebuilding local `better-sqlite3` for the active Node ABI. |
| Success | Bug 1 legacy alias normalization | Legacy aliases (`claude`, `claudeCode`, `gemini`, `codexCli`) normalize server-side to canonical IDs, persist canonical `agentKind`, and unknown IDs still return structured 400. | `agent_sessions_controller.ts` adds `AGENT_ID_ALIASES` and uses `normalizedAgentId` for lookup, disabled/configured errors, and persisted `agentKind`. `agent_sessions.test.ts` verifies `claude` persists as `claude-code` and `unknown-agent` returns `{error:{code:'BAD_REQUEST',message:"agent not configured: 'unknown-agent'"}}`; `npm test -- agent_sessions` passed 19/19. |
| Success | Bug 1 migration | Existing legacy `agent_configs` rows for `claude`/`gemini` are renamed or removed idempotently without changing the canonical seed contract. | `migrations.ts` adds an idempotent block after the existing agent session kind normalization: `UPDATE agent_configs SET id = 'claude-code' WHERE id = 'claude' ...`, deletes duplicate legacy rows when canonical rows exist, and mirrors the same `gemini` to `gemini-cli` handling. |
| Success | Bug 2 structured HTTP errors | Flutter `assertOk` parses `{error:{code,message}}`, preserves `AppError.code` and `statusCode`, and uses HTTP-aware fallbacks for empty/non-JSON bodies. | `http_utils.dart` extracts `error.message` and `error.code`, throws `AppError(..., statusCode: response.statusCode)`, and has HTTP-aware fallback strings. `flutter test test/app/core/utils/http_utils_test.dart test/features/agents/new_session_dialog_error_test.dart` passed 7/7. |
| Success | Bug 2 controller status propagation | `AgentsController` exposes `lastErrorStatus`, resets it before create, and captures `AppError.message/statusCode` for create/close/resume failures without changing `AppError.toString()`. | `agents_controller.dart` adds `_lastErrorStatus`/getter, resets it at create start, and captures `AppError.message` plus `e.statusCode` in create/close/resume. `app_error.dart` still has `String toString() => message;`. |
| Success | Bug 2 new-session dialog UX | 4xx create failures render the server message verbatim in red; 5xx failures render a generic server message with raw details behind a disclosure; no 4xx path shows hard-coded `Internal server error`. | `_NewSessionDialogState` stores `_errorStatus`, shows `_error!` directly for non-5xx, and uses `Something went wrong on the server.` plus an `ExpansionTile` for 5xx details. Widget tests verify the 4xx server message is visible and the 5xx raw message is hidden before disclosure; targeted Flutter test passed 7/7. |
| Success | Bug 3 server ANSI stripping | `TranscriptService.stripAnsi` expands cursor movement sequences to whitespace before stripping remaining ANSI and preserves normal spaces. | Diff adds `expandCursorMoves` before final ANSI stripping in `apps/api_server/src/services/transcript_service.ts`; `npm test -- transcript_service` passed with 17/17 tests, including cursor-right, cursor-column, absolute-position, and plain-space preservation cases. |
| Success | Bug 3 Flutter live ANSI stripping | `_MiniLiveBlock` uses the matching Dart `stripAnsi` before 500-character truncation; `_MiniMessageBlock` is left unchanged. | New `ansi_strip.dart` mirrors the TypeScript cursor regexes and final ANSI regex. `_MiniLiveBlock.build` calls `stripAnsi(text)` before tail truncation; `_MiniMessageBlock` still renders `message.strippedText`. `flutter test test/app/core/agents/ansi_strip_test.dart` passed 8/8. |
| Success | Targeted verification commands | Planned API and Flutter unit/widget checks pass, or any failures are documented as implementation drift/blockers. | Passed: `npm run build`; `npx tsc --noEmit`; `npm test -- agent_sessions` (19/19, after local `npm rebuild better-sqlite3` for Node ABI); `npm test -- transcript_service` (17/17); `/Users/ajhochhalter/development/flutter/bin/dart format --set-exit-if-changed lib test`; `/Users/ajhochhalter/development/flutter/bin/flutter test test/app/core/utils/http_utils_test.dart test/features/agents/new_session_dialog_error_test.dart` (7/7); `/Users/ajhochhalter/development/flutter/bin/flutter test test/app/core/agents/ansi_strip_test.dart` (8/8); `/Users/ajhochhalter/development/flutter/bin/flutter analyze --no-fatal-infos` exited 0 with existing info-level lint output. |

