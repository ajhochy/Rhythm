# Finish checklist

- [x] Read handoff; verify both assigned branches and preserve pre-existing changes.
- [x] Build live web and launch owned Electron; Hermes ready and health 200.
- [x] Attempt native sign-in; capture missing public Google client ID blocker.
- [x] Complete packaged Finder/Get Info icon check after API build repair.
- [x] Execute requested hosted Playwright command: setup fails without RHYTHM_LIVE_TOKEN.
- [x] Probe six hosted collections: each returns 403 without auth; zero created rows by this run, global zero-marker check blocked.
- [x] Reproduce and fix smoke cleanup crossing run ownership; 2/2 safety tests.
- [x] Fork lifecycle attempted and final package/tests passed; mounted route/M3/read/ACP blocked; both install halves removed with cached host registration disclosed.
- [x] #1373 red contracts -> implementation -> full API/mobile -> final-source live sandbox; hosted/physical contract remains partial.
- [x] #1513/#1509 issue-first review complete; fixture proof green; native/subjective items explicitly pending.
- [x] Electron readiness fake timers: three 163/163 runs and typecheck.
- [x] Full final web/desktop/mobile gates: web 459/47 skip; slices 155/4 skip; Electron 163; mobile 133; API 6180/250 skip; real relay 1.
- [x] GitNexus change analysis complete; publication uses both assigned branches with the required coauthor. Exact pushed heads are reported on the PRs.
- [x] Project/run records and PR bodies prepared from final evidence; exact remote-head CI is reported on the live PRs.
- [x] Publish Dev Dashboard run: `pending`, counts `1/0/9;0/1/33;5/1/5;6/0/1`, revision 4395 -> 4396; owned app/sandbox/fixture cleanup is complete.

## Decisions

1. Follow the top-level marker and Finder path, which supersede older visual-brief values.
2. Keep missing authentication/configuration and unreachable original API/engine as explicit blockers; never obtain or type credentials elsewhere.
3. Use workflow specialists with disjoint API/mobile, web review, and Electron/fork ownership; keep commits and final records with parent.
4. Cleanup identifies this invocation through name/title/label; it preserves other runs and marker mentions in notes.
5. Use injected readiness timing, retaining retry and deadline assertions; the identical invalid-JSON timing failure gets the same repair.
6. Preserve accessible status/date context while omitting redundant visual task metadata.
7. Leave both PRs draft and never merge.
8. Existing concurrent documentation commits 238438da and 86bfd1a7 are retained.

9. Update the E21 reconciliation/live tests to use the #1511 View options menu; keep the same archived-session outcome assertion.
10. Live typography checks measure all remaining meaningful metadata; empty redundant metadata is intentionally absent after #1509.
11. Stop and restart the owned full-web gate when the issue-first review discovers another product fix, so final evidence runs after source settles.
12. Keep the M10 performance threshold unchanged; full-run failure plus isolated 5/5 is recorded as a flake.

13. Planner retains its density, unclipped titles, 44px actions, and 120px title-width assertions; reduce vertical padding and raise the initial day width to 184px.
14. Follow the redesigned Profiles disclosure before editing advanced policy JSON; preserve the policy round-trip and invalid-input assertions.
15. Dashboard Projects navigation now asserts the exact selected-project URL and selected row, preserving the shared inspector selection contract.
16. Adapt Integration specs to the live loading status/busy list, Back to list at narrow widths, and sentence-case consent copy; retain the behavior and permissions assertions.

17. Restore Messages domain behavior removed during the layout migration: title search, read-state hydration and receipts, deep links, and recovery; retain the selected inspector while filtering only list rows.
18. Extend shared ListInspector with optional filtering/no-results inputs, preserving defaults for its other callers; its critical blast radius is explicitly recorded.
19. Restore Facilities conflict/setup/external schedule signals and invalid-item recovery; select compact rows before inspector actions in tests.
20. Preserve strict E21 zero token-driven refresh. The failure reproduced alone: the two wall-clock waits crossed the existing two-second reconciliation timer. Use controlled test time and separately assert the scheduled two-list refresh; production polling is unchanged.
21. Replace the unavailable installed-host JSX shim through host React only; never bundle a second React runtime.
22. Remove the speculative SDK hook replacement because its throwing mock did not establish the actual failure cause.
23. Treat registered-but-unmounted Hermes routes as a host mounting blocker. Do not infer successful M3 authentication, hosted reads, draft handoff, or ACP approval.
24. Reload only the Hermes window for the authorized reload gate; never quit the app. Restore the temporary profile-visibility navigation and keep every draft unsent.
25. Serialize the final browser gates after concurrent builds changed dist during preliminary checks; preserve all preliminary failure evidence.

26. Move the Facilities conflict live announcement onto its description element to preserve valid definition-list semantics; retain the accessibility assertion.
27. Correct Messages specs for the shared wrapper alert and original auto-read-on-selection contract; resetting the route before reload preserves the intended unread-hydration test.
28. Preserve the running Hermes app after uninstall; filesystem/CLI removal passed but cached in-memory native-tool disposal remains unverified.
29. Keep every fork milestone as Refs. The initial cleanup preceded final artifact qualification; Decision 41 records the subsequent exact-package live attempt.
30. Report configuration, authentication, mounted-route, and hosted-write limitations even though the requested final remainder listed only device/deployment work. Evidence takes precedence over an unsupported completion claim.
31. Preserve the fork's four pre-existing dashboard-dist deletions and both repositories' prior commits; stage only this run's authorized changes.
32. Keep all drafts unsent and all production rows untouched; use only synthetic sandbox rows and remove the owned sandbox after its exact-source live test.
33. Keep preliminary failures and timing flakes in the record; final full-suite results are separately identified.

34. Keep generated fixture screenshot churn out of the source commit; retain the requested native screenshots and exact gate receipts.
35. E21 is an anonymous test callback that GitNexus could not resolve by test title; record UNKNOWN indexed impact and limit the change to test clock control.

36. Publish the requested data-only Dashboard receipt through its script; this explicit request authorizes that path and does not establish automatic-hook capture.
37. Keep retrospective findings in the assigned repository; do not change global skills or unrelated worktrees.

38. Repair the reproduced sandbox boot crash by resolving default storage inside the existing preference-reader try/catch. Preserve sandbox restrictions and assertions; HIGH impact (five direct callers, no indexed flows) was reported before editing. Add a throwing-storage regression and rerun full web/slice gates.

39. Preserve persistence truth at Settings and View options boundaries: catch unavailable-storage writes/resets, display failure, and retain prior saved state or unsaved draft. Do not invent temporary persistence or weaken iframe restrictions.

40. Keep the #1174 terminal-menu timeout as a flake: isolated spec 3/4, exact failed case 1/1 on immediate rerun. Preserve both results and make no source/assertion change.

41. Reinstall the exact final JSX-only fork package to replace diagnostic-candidate ambiguity. Confirm final bundle fingerprint and successful element creation, record zero mounted workspace roots, then disable/remove again without quitting Hermes or changing auth/backups.

42. Stop only the proven orphan from our interrupted E22 test: Vite PID 56675 and npm parent 56626 on 4189 matched the prior log, start time, and integration path. Keep application/service processes untouched and rerun the manifest.

43. Trim reporter trailing whitespace from committed test logs so the staged whitespace gate passes; retain untouched raw copies locally. Test output content and outcomes are unchanged.
