# Rhythm prototype agent guidance

Read `docs/ai/project-state.md`, `docs/ai/current-plan.md`, and the relevant page plus its tests before editing.

- Preserve deterministic local fixtures and endpoint receipt language.
- Preserve read-only, forbidden, source-owned, and unavailable boundaries.
- Keep user-visible changes within the requested surfaces; do not rewrite unrelated pages.
- Use existing design tokens and component vocabulary from `src/styles.css` and page styles.
- Validate with TypeScript, the production build, targeted Playwright coverage when runnable, and `npm run test:dist-smoke`.
- This workspace copy has no Git metadata. Do not invent branches, commits, or PR state.

